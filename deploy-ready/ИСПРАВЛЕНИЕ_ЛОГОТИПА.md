# 🔧 Исправление перекрытия логотипа

## ❌ Проблема

**Симптомы:**
- Логотип "Nurkhonov_Academy" обрезается сверху темной полосой
- Текст перекрывается границей header
- Визуально некрасиво и нечитаемо

**Причина:**
- SVG viewBox не учитывал полную высоту текста
- Позиционирование текста (y="32") было слишком высоким
- Недостаточный padding в header
- Отсутствие overflow: visible

---

## ✅ Исправления

### Frontend (`frontend/src/components/Logo.tsx`):

1. **Исправлено позиционирование текста:**
   - Изменено `y="32"` на `y="28"` (смещение вниз)
   - Добавлен `dominantBaseline="hanging"` для правильного выравнивания
   - Уменьшен `fontSize` с 24 до 22 для лучшей читаемости

2. **Улучшен SVG:**
   - Добавлен `style={{ overflow: 'visible' }}` для предотвращения обрезки
   - Добавлен `preserveAspectRatio="xMidYMid meet"` для правильного масштабирования

3. **Исправлено подчеркивание:**
   - Изменено `y1="38"` на `y1="36"` для соответствия новому позиционированию текста

### Frontend (`frontend/src/components/Header.tsx`):

1. **Добавлен padding:**
   - Добавлен `py-2` к контейнеру для дополнительного вертикального пространства

2. **Улучшена ссылка логотипа:**
   - Добавлен `overflow-visible` для предотвращения обрезки

---

## 📝 Технические детали

### До исправления:
```tsx
<text x="0" y="32" fontSize="24" ...>
  Nurkhonov_Academy
</text>
```

### После исправления:
```tsx
<text 
  x="0" 
  y="28" 
  fontSize="22" 
  dominantBaseline="hanging"
  ...
>
  Nurkhonov_Academy
</text>
```

---

## ⚠️ Важно

**Что исправлено:**
- ✅ Логотип больше не обрезается сверху
- ✅ Текст полностью виден
- ✅ Правильное выравнивание по вертикали
- ✅ Улучшена читаемость

**Если проблема остается:**
- Очистите кэш браузера (Ctrl+Shift+R)
- Проверьте, что файлы обновлены на сервере

---

## 🚀 Что нужно сделать

1. **Обновить фронтенд:**
   - Загрузить файлы из `deploy-ready/`
   - Удалить старые файлы из `assets/`
   - Очистить кэш браузера

2. **Проверить:**
   - Открыть сайт
   - Убедиться, что логотип полностью виден
   - Проверить на разных размерах экрана

---

**Дата исправления:** $(date '+%Y-%m-%d %H:%M:%S')

