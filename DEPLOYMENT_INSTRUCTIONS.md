# Инструкция по внедрению изменений

## 📋 Обзор изменений

1. **Безопасность паролей**: Замена слабого пароля по умолчанию `'123456'` на безопасную генерацию случайных паролей
2. **Атомарность операций**: Добавлена транзакция для операций одобрения/отклонения доступа к курсу

## ✅ Шаги для внедрения

### 1. Проверка зависимостей

Убедитесь, что все необходимые файлы на месте:

```bash
# Проверьте наличие файла генератора паролей
ls backend/src/utils/passwordGenerator.ts

# Проверьте импорты в контроллерах
grep -n "generateSecurePassword" backend/src/controllers/curatorController.ts
grep -n "generateSecurePassword" backend/src/controllers/studentController.ts
```

### 2. Проверка синтаксиса TypeScript

```bash
cd backend
npm run build
# или
npx tsc --noEmit
```

Если есть ошибки компиляции, исправьте их перед продолжением.

### 3. Проверка функции createNotification

✅ **ИСПРАВЛЕНО**: Функция `createNotification` обновлена для поддержки транзакций.

Функция теперь принимает опциональный параметр `tx` для работы внутри транзакции Prisma.

### 4. Тестирование локально

```bash
# Запустите сервер в режиме разработки
cd backend
npm run dev

# Или в production режиме
npm run build
npm start
```

### 5. Тестирование функциональности

#### Тест 1: Создание студента без пароля
```bash
# Должен сгенерироваться безопасный пароль
POST /api/students
{
  "firstName": "Test",
  "lastName": "User",
  "phone": "+998901234567"
}
```

#### Тест 2: Создание учителя без пароля
```bash
# Должен сгенерироваться безопасный пароль
POST /api/curators
{
  "firstName": "Test",
  "lastName": "Teacher",
  "phone": "+998901234568"
}
```

#### Тест 3: Одобрение доступа к курсу
```bash
# Должна выполниться транзакция атомарно
PUT /api/students/{studentId}/courses/{courseId}/access?action=approve
```

### 6. Проверка логов

После запуска проверьте логи на наличие ошибок:

```bash
# Если используете PM2
pm2 logs

# Или просто смотрите вывод консоли
```

### 7. Миграции базы данных

⚠️ **Проверьте**: Нужны ли миграции базы данных?

Если изменения касаются только логики (без изменения схемы), миграции не нужны.

Если вы изменили схему Prisma, выполните:

```bash
cd backend
npx prisma migrate dev --name your_migration_name
# или для production
npx prisma migrate deploy
```

### 8. Деплой на production

#### Если используете Git:

```bash
# 1. Закоммитьте изменения
git add .
git commit -m "feat: улучшена безопасность паролей и атомарность операций"

# 2. Отправьте на сервер
git push origin main

# 3. На сервере
git pull origin main
cd backend
npm install  # если были новые зависимости
npm run build
pm2 restart all  # или ваш способ перезапуска
```

#### Если деплоите вручную:

1. Скопируйте измененные файлы на сервер
2. Установите зависимости (если нужно): `npm install`
3. Соберите проект: `npm run build`
4. Перезапустите сервер

### 9. Мониторинг после деплоя

После деплоя проверьте:

1. ✅ Сервер запускается без ошибок
2. ✅ API endpoints работают корректно
3. ✅ Создание студентов/учителей генерирует безопасные пароли
4. ✅ Одобрение доступа к курсу работает атомарно
5. ✅ Нет ошибок в логах

## 🔍 Возможные проблемы и решения

### Проблема 1: Ошибка компиляции TypeScript

**Решение**: Проверьте импорты и типы:
```bash
cd backend
npx tsc --noEmit
```

### Проблема 2: createNotification не работает в транзакции

**Решение**: Обновите функцию `createNotification` чтобы принимать транзакцию:

```typescript
// backend/src/controllers/notificationController.ts
import { Prisma } from '@prisma/client';

export const createNotification = async (
  userId: string,
  type: string,
  title: string,
  message: string,
  link?: string,
  tx?: Prisma.TransactionClient
) => {
  const prismaClient = tx || prisma;
  
  const notification = await prismaClient.notification.create({
    data: {
      userId,
      type,
      title,
      message,
      link: link || null,
    },
  });

  // Emit socket notification
  emitNotification(userId, notification);

  return notification;
};
```

### Проблема 3: Пароли не генерируются

**Решение**: Убедитесь, что файл `passwordGenerator.ts` существует и экспортирует функцию правильно.

## 📝 Чеклист перед деплоем

- [ ] Код компилируется без ошибок
- [ ] Все импорты корректны
- [ ] Функция `createNotification` поддерживает транзакции (если используется в транзакции)
- [ ] Локальное тестирование пройдено
- [ ] Миграции базы данных выполнены (если нужно)
- [ ] Резервная копия базы данных создана
- [ ] План отката подготовлен

## 🚀 Быстрый старт

```bash
# 1. Проверка
cd backend
npm run build

# 2. Если все ОК, перезапустите сервер
pm2 restart all
# или
npm run dev
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи сервера
2. Проверьте консоль браузера (для frontend)
3. Убедитесь, что все зависимости установлены
4. Проверьте версию Node.js (должна быть совместима)
