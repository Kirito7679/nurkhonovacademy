# Новые функции платформы

## 📋 Содержание

1. [Система ролей](#система-ролей)
2. [Логирование действий](#логирование-действий)
3. [Флеш-карточки](#флеш-карточки)
4. [Практические упражнения](#практические-упражнения)
5. [Интерактивные задания](#интерактивные-задания)
6. [Внешние интеграции](#внешние-интеграции)
7. [Многоязычность (i18n)](#многоязычность-i18n)
8. [Резервное копирование](#резервное-копирование)

---

## Система ролей

### Новые роли

- **MODERATOR** - Модератор: может проверять упражнения, просматривать логи, управлять контентом
- **ASSISTANT** - Ассистент: может помогать учителям в управлении курсами

### Использование

Роли проверяются через middleware `requireRole()`:

```typescript
router.post('/create', requireRole('TEACHER', 'ADMIN', 'MODERATOR'), createSomething);
```

---

## Логирование действий

Все важные действия пользователей автоматически логируются в таблицу `activity_logs`.

### Типы действий

- `CREATE`, `UPDATE`, `DELETE` - операции с сущностями
- `LOGIN`, `LOGOUT` - авторизация
- `VIEW` - просмотр контента
- `SUBMIT` - отправка ответов
- `REVIEW` - проверка работ

### Просмотр логов

**API Endpoint:** `GET /api/activity-logs`

Только для администраторов и модераторов.

**Параметры:**
- `userId` - фильтр по пользователю
- `action` - фильтр по действию
- `entityType` - фильтр по типу сущности
- `limit`, `offset` - пагинация

---

## Флеш-карточки

Система для запоминания с алгоритмом интервального повторения (spaced repetition).

### API Endpoints

- `GET /api/flashcards` - получить все колоды
- `GET /api/flashcards/:id` - получить колоду по ID
- `POST /api/flashcards` - создать колоду (учителя, админы, модераторы)
- `PUT /api/flashcards/:id` - обновить колоду
- `DELETE /api/flashcards/:id` - удалить колоду
- `POST /api/flashcards/:deckId/cards` - добавить карточку
- `PUT /api/flashcards/:deckId/cards/:cardId/progress` - обновить прогресс (студенты)
- `GET /api/flashcards/:deckId/review` - получить карточки для повторения

### Уровни сложности

- `NEW` - новая карточка (повтор через 1 день)
- `EASY` - легко (повтор через 4 дня)
- `MEDIUM` - средне (повтор через 2 дня)
- `HARD` - сложно (повтор через 1 день)

### Пример создания колоды

```json
{
  "title": "Английские слова",
  "description": "Базовый словарь",
  "courseId": "course-id",
  "isPublic": false,
  "flashcards": [
    {
      "front": "Hello",
      "back": "Привет"
    },
    {
      "front": "Goodbye",
      "back": "До свидания"
    }
  ]
}
```

---

## Практические упражнения

Упражнения для практики с возможностью автоматической или ручной проверки.

### API Endpoints

- `GET /api/practice/lessons/:lessonId` - получить упражнения урока
- `POST /api/practice/lessons/:lessonId` - создать упражнение (учителя, админы, модераторы)
- `POST /api/practice/:exerciseId/submit` - отправить ответ (студенты)
- `GET /api/practice/:exerciseId/results` - получить результаты студента
- `PUT /api/practice/results/:resultId/review` - проверить ответ (учителя, админы, модераторы)

### Типы упражнений

- `CODING` - программирование
- `WRITING` - письменные задания
- `SPEAKING` - устные задания
- `LISTENING` - аудирование
- `OTHER` - другие

### Автоматическая проверка

Если `autoCheck: true` и указано `solution`, система автоматически сравнивает ответ студента с решением.

### Пример создания упражнения

```json
{
  "title": "Напишите эссе",
  "description": "Опишите свой день",
  "type": "WRITING",
  "instructions": "Напишите эссе на 200 слов о своем обычном дне",
  "solution": null,
  "autoCheck": false,
  "maxAttempts": 3,
  "order": 0
}
```

---

## Интерактивные задания

Расширена система Quiz для поддержки новых типов вопросов.

### Новые типы вопросов

- `DRAG_DROP` - перетаскивание элементов
- `MATCHING` - сопоставление
- `FILL_BLANK` - заполнение пропусков

### Метаданные вопросов

Для сложных типов вопросов используется поле `metadata` (JSON):

**DRAG_DROP:**
```json
{
  "items": ["item1", "item2", "item3"],
  "targets": ["target1", "target2", "target3"],
  "correctMapping": {
    "item1": "target1",
    "item2": "target2",
    "item3": "target3"
  }
}
```

**MATCHING:**
```json
{
  "leftItems": ["left1", "left2", "left3"],
  "rightItems": ["right1", "right2", "right3"],
  "correctPairs": [
    {"left": "left1", "right": "right1"},
    {"left": "left2", "right": "right2"},
    {"left": "left3", "right": "right3"}
  ]
}
```

**FILL_BLANK:**
```json
{
  "text": "The capital of France is ___",
  "blanks": [
    {"position": 3, "correctAnswer": "Paris"}
  ]
}
```

---

## Внешние интеграции

Интеграция с внешними сервисами (Google Docs, Quizlet и др.).

### API Endpoints

- `GET /api/integrations` - получить интеграции
- `POST /api/integrations` - создать интеграцию
- `DELETE /api/integrations/:id` - удалить интеграцию

### Типы интеграций

- `GOOGLE_DOCS` - Google Документы
- `QUIZLET` - Quizlet
- `YOUTUBE` - YouTube
- `OTHER` - другие

### Пример создания интеграции

```json
{
  "type": "QUIZLET",
  "externalId": "quizlet-set-id",
  "externalUrl": "https://quizlet.com/...",
  "courseId": "course-id",
  "lessonId": "lesson-id",
  "metadata": {
    "title": "Vocabulary Set",
    "cardCount": 50
  }
}
```

---

## Многоязычность (i18n)

### Поддерживаемые языки

- `ru` - Русский (по умолчанию)
- `en` - English
- `uz` - O'zbek

### Настройка языка пользователя

Язык хранится в поле `language` таблицы `users`. Определяется автоматически из заголовка `Accept-Language` или настройками пользователя.

### Использование в backend

```typescript
import { t, getLanguageFromHeader } from './utils/i18n';

const lang = getLanguageFromHeader(req.get('accept-language'));
const message = t('error.unauthorized', lang);
```

---

## Резервное копирование

### Создание бэкапа

```bash
npm run backup
```

Создает сжатый архив с:
- Схемой базы данных
- Данными (для SQLite)
- Миграциями
- Метаданными

### Восстановление из бэкапа

```bash
npm run restore <path-to-backup>
```

### Автоматическое резервное копирование

Рекомендуется настроить cron job для регулярных бэкапов:

```bash
# Ежедневный бэкап в 2:00 ночи
0 2 * * * cd /path/to/backend && npm run backup
```

---

## Миграция базы данных

После добавления новых функций необходимо применить миграции:

```bash
cd backend
npx prisma migrate deploy
# или для разработки:
npx prisma migrate dev
npx prisma generate
```

---

## Примеры использования

### Создание флеш-карточек для урока

```typescript
const deck = await api.post('/api/flashcards', {
  title: 'Vocabulary Lesson 1',
  lessonId: 'lesson-id',
  isPublic: false,
  flashcards: [
    { front: 'Hello', back: 'Привет' },
    { front: 'Goodbye', back: 'До свидания' }
  ]
});
```

### Создание практического упражнения

```typescript
const exercise = await api.post('/api/practice/lessons/lesson-id', {
  title: 'Write an Essay',
  type: 'WRITING',
  instructions: 'Write 200 words about your day',
  autoCheck: false,
  maxAttempts: 3
});
```

### Просмотр логов активности

```typescript
const logs = await api.get('/api/activity-logs', {
  params: {
    userId: 'user-id',
    action: 'CREATE',
    limit: 50
  }
});
```

---

## Безопасность

- Все действия логируются
- Доступ к логам только для админов и модераторов
- Проверка прав доступа на всех уровнях
- Валидация всех входных данных

---

## Дальнейшее развитие

Планируемые улучшения:

1. Расширенная аналитика по флеш-карточкам
2. Более сложные алгоритмы автоматической проверки
3. Интеграция с дополнительными сервисами
4. Экспорт/импорт флеш-карточек
5. Групповые упражнения
6. Видео-интеграции для практики говорения
