# Руководство по использованию переводов (i18n)

## 📋 Обзор

Платформа поддерживает три языка:
- 🇷🇺 **Русский** (ru) - язык по умолчанию
- 🇺🇸 **English** (en)
- 🇺🇿 **O'zbek** (uz)

## 🔧 Технические детали

### Установленные библиотеки
- `i18next` - основная библиотека для i18n
- `react-i18next` - интеграция с React
- `i18next-browser-languagedetector` - автоматическое определение языка

### Структура файлов
```
frontend/src/
├── i18n/
│   ├── config.ts          # Конфигурация i18n
│   └── locales/
│       ├── ru.json        # Русские переводы
│       ├── en.json        # Английские переводы
│       └── uz.json        # Узбекские переводы
└── components/
    └── LanguageToggle.tsx # Переключатель языка
```

## 📝 Использование в компонентах

### Базовое использование

```tsx
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t('common.loading')}</h1>
      <button>{t('common.save')}</button>
    </div>
  );
}
```

### Переводы с параметрами

```tsx
const { t } = useTranslation();

// В JSON: "cardProgress": "Карточка {{current}} из {{total}}"
t('flashcards.cardProgress', { current: 1, total: 10 })
// Результат: "Карточка 1 из 10"
```

### Множественное число

```tsx
// В JSON можно использовать плюрализацию
t('courses.lessons', { count: 5 })
```

## 🎨 Структура переводов

Все переводы организованы по категориям:

- `common` - общие элементы (кнопки, действия)
- `auth` - аутентификация
- `dashboard` - панель управления
- `courses` - курсы
- `lessons` - уроки
- `modules` - модули
- `flashcards` - флеш-карточки
- `practice` - практические упражнения
- `quiz` - тесты
- `integrations` - интеграции
- `activityLogs` - логи активности
- `students` - студенты
- `profile` - профиль
- `chat` - чат
- `comments` - комментарии
- `notifications` - уведомления
- `errors` - ошибки

## 🔄 Переключение языка

Язык можно переключить через компонент `LanguageToggle` в Header.

При переключении языка:
1. Язык сохраняется в localStorage
2. Если пользователь авторизован, язык сохраняется в базе данных
3. Язык применяется ко всему интерфейсу

## 💾 Сохранение языка пользователя

Язык пользователя сохраняется в:
- **Frontend**: localStorage (для неавторизованных пользователей)
- **Backend**: поле `language` в таблице `users` (для авторизованных пользователей)

При входе пользователя язык автоматически загружается из базы данных.

## 📚 Примеры обновления компонентов

### До (без переводов):
```tsx
<h1>Курсы</h1>
<button>Сохранить</button>
```

### После (с переводами):
```tsx
import { useTranslation } from 'react-i18next';

function Component() {
  const { t } = useTranslation();
  
  return (
    <>
      <h1>{t('courses.title')}</h1>
      <button>{t('common.save')}</button>
    </>
  );
}
```

## 🚀 Добавление новых переводов

1. Откройте файлы переводов в `frontend/src/i18n/locales/`
2. Добавьте новый ключ во все три файла (ru.json, en.json, uz.json)
3. Используйте ключ в компоненте через `t('category.key')`

Пример:
```json
// ru.json
{
  "myFeature": {
    "title": "Моя функция",
    "description": "Описание функции"
  }
}

// en.json
{
  "myFeature": {
    "title": "My Feature",
    "description": "Feature description"
  }
}

// uz.json
{
  "myFeature": {
    "title": "Mening funksiyam",
    "description": "Funksiya tavsifi"
  }
}
```

## ⚙️ Конфигурация

Конфигурация находится в `frontend/src/i18n/config.ts`:

- **fallbackLng**: 'ru' - язык по умолчанию
- **detection**: автоматическое определение языка из браузера
- **caches**: сохранение выбора в localStorage

## 🔍 Отладка

Для проверки текущего языка:
```tsx
const { i18n } = useTranslation();
console.log(i18n.language); // 'ru', 'en', или 'uz'
```

Для принудительной смены языка:
```tsx
i18n.changeLanguage('en');
```

## 📝 Чеклист для обновления компонента

- [ ] Импортировать `useTranslation` из `react-i18next`
- [ ] Добавить `const { t } = useTranslation();` в компонент
- [ ] Заменить все текстовые строки на `t('category.key')`
- [ ] Добавить новые ключи во все три файла переводов
- [ ] Проверить работу на всех трех языках

## 🎯 Приоритетные компоненты для обновления

1. ✅ Login / Register
2. ✅ Header
3. ⏳ Dashboard
4. ⏳ Courses
5. ⏳ Lessons
6. ⏳ Profile
7. ⏳ Все формы
8. ⏳ Сообщения об ошибках

## 💡 Советы

1. **Используйте осмысленные ключи**: `courses.title` лучше чем `text1`
2. **Группируйте по категориям**: все переводы курсов в `courses.*`
3. **Переводите контекстно**: учитывайте культурные особенности
4. **Тестируйте на всех языках**: особенно длинные тексты
5. **Используйте параметры**: для динамических значений

## 🐛 Решение проблем

**Проблема**: Перевод не отображается
- Проверьте, что ключ существует во всех трех файлах
- Убедитесь, что используется правильный синтаксис `t('category.key')`

**Проблема**: Язык не сохраняется
- Проверьте, что пользователь авторизован
- Проверьте консоль на ошибки API

**Проблема**: Язык не применяется при входе
- Убедитесь, что поле `language` возвращается в ответе `/auth/me`
- Проверьте, что i18n config загружает язык из localStorage
