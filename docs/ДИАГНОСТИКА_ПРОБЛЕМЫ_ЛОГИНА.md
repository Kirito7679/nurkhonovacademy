# 🔍 Диагностика проблемы с логином

## ⚠️ Проблема: Не могу войти, нет ошибок в логах

---

## 📋 Пошаговая диагностика

### Шаг 1: Проверка API

Откройте в браузере:
```
https://api.academy.dilmurodnurkhonov.uz/api/health
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "message": "Server is running"
}
```

**Если не работает:**
- Проверьте DNS записи
- Проверьте логи в Railway

---

### Шаг 2: Проверка консоли браузера

1. Откройте сайт: `https://academy.dilmurodnurkhonov.uz/login`
2. Нажмите **F12** (или Cmd+Option+I на Mac)
3. Откройте вкладку **Console**
4. Попробуйте войти
5. **Что вы видите?** Запишите все ошибки

---

### Шаг 3: Проверка Network запросов

1. В браузере нажмите **F12**
2. Откройте вкладку **Network** (Сеть)
3. Попробуйте войти
4. Найдите запрос к `/api/auth/login`
5. **Что показывает статус?** (200, 401, 500, и т.д.)
6. Нажмите на запрос и посмотрите:
   - **Request** (что отправляется)
   - **Response** (что возвращается)

---

### Шаг 4: Проверка базы данных

Возможно, миграции не применены. Проверьте логи в Railway:

1. Откройте проект в Railway
2. Перейдите в **Deployments**
3. Выберите последний деплой
4. Откройте **View Logs**
5. Найдите строки про миграции:
   ```
   Applying migration `20251210094933_init`
   ```

**Если миграций нет:**
- Нужно применить миграции вручную

---

### Шаг 5: Проверка переменных окружения

В Railway проверьте переменные окружения:

1. Откройте проект в Railway
2. Перейдите в **Variables**
3. Убедитесь, что есть:
   - `DATABASE_URL=file:./prisma/dev.db`
   - `JWT_SECRET=ваш-секретный-ключ`
   - `NODE_ENV=production`

---

## 🔧 Быстрое решение

### Если миграции не применены:

**Через Railway CLI:**
```bash
railway login
railway link
railway run npx prisma migrate deploy
```

**Или через Railway Dashboard:**
1. Откройте проект
2. Deployments → последний деплой → View Logs
3. Найдите кнопку "Run Command" или "Shell"
4. Выполните: `npx prisma migrate deploy`

---

## 📝 Что нужно проверить и сообщить:

1. ✅ Что возвращает `/api/health`?
2. ✅ Какие ошибки в консоли браузера (F12 → Console)?
3. ✅ Какой статус у запроса `/api/auth/login` (F12 → Network)?
4. ✅ Что в Response у запроса логина?
5. ✅ Есть ли миграции в логах Railway?

---

**После проверки сообщите результаты, и я помогу решить проблему! 🔍**
