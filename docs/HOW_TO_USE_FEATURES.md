# Руководство по использованию функций Nurkhonov Academy

## 📚 Содержание

1. [Интерактивные упражнения](#интерактивные-упражнения)
2. [Флеш-карточки](#флеш-карточки)
3. [Интеграции](#интеграции)
4. [Тестирование функций](#тестирование-функций)
5. [Использование в уроках](#использование-в-уроках)

---

## 🎯 Интерактивные упражнения

### 1. Drag & Drop (Перетаскивание)

**Описание:** Упражнение, где студенты перетаскивают элементы в соответствующие зоны.

**Как использовать в коде:**

```tsx
import DragAndDrop from '../components/exercises/DragAndDrop';

// Подготовьте данные
const items = [
  { id: '1', content: 'Кот' },
  { id: '2', content: 'Собака' },
  { id: '3', content: 'Птица' },
];

const dropZones = [
  { id: 'zone1', label: 'Домашние животные', correctItemId: '1' },
  { id: 'zone2', label: 'Домашние животные', correctItemId: '2' },
  { id: 'zone3', label: 'Дикие животные', correctItemId: '3' },
];

// Используйте компонент
<DragAndDrop
  items={items}
  dropZones={dropZones}
  onComplete={(isCorrect) => {
    if (isCorrect) {
      alert('Правильно! ✓');
    } else {
      alert('Попробуйте еще раз');
    }
  }}
/>
```

**Параметры:**
- `items` - массив элементов для перетаскивания
  - `id` - уникальный идентификатор
  - `content` - текст элемента
- `dropZones` - массив зон для размещения
  - `id` - уникальный идентификатор зоны
  - `label` - название зоны
  - `correctItemId` - ID правильного элемента для этой зоны
- `onComplete` - функция обратного вызова при проверке (опционально)

**Как работает:**
1. Студент перетаскивает элементы из списка в зоны
2. Нажимает кнопку "Проверить"
3. Система показывает правильные (зеленые) и неправильные (красные) ответы
4. Можно попробовать снова

---

### 2. Matching (Сопоставление)

**Описание:** Упражнение, где студенты сопоставляют элементы из двух колонок.

**Как использовать в коде:**

```tsx
import Matching from '../components/exercises/Matching';

// Подготовьте данные
const matchingItems = [
  { id: '1', left: 'Красный', right: 'Red' },
  { id: '2', left: 'Синий', right: 'Blue' },
  { id: '3', left: 'Зеленый', right: 'Green' },
];

// Используйте компонент
<Matching
  items={matchingItems}
  onComplete={(isCorrect) => {
    if (isCorrect) {
      alert('Все правильно! ✓');
    } else {
      alert('Есть ошибки, попробуйте еще раз');
    }
  }}
/>
```

**Параметры:**
- `items` - массив пар для сопоставления
  - `id` - уникальный идентификатор пары
  - `left` - элемент левой колонки
  - `right` - элемент правой колонки (правильный ответ)
- `onComplete` - функция обратного вызова при проверке (опционально)

**Как работает:**
1. Студент нажимает на элемент в левой колонке
2. Затем нажимает на соответствующий элемент в правой колонке
3. Элементы соединяются
4. После сопоставления всех элементов нажимает "Проверить"
5. Система показывает правильные и неправильные пары

---

## 🃏 Флеш-карточки

**Описание:** Интерактивные карточки для изучения с интервальным повторением.

**Как использовать в коде:**

```tsx
import Flashcards from '../components/Flashcards';

// Подготовьте данные
const flashcards = [
  {
    id: '1',
    lessonId: 'lesson-123',
    front: 'Hello',
    back: 'Привет',
    order: 0,
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  },
  {
    id: '2',
    lessonId: 'lesson-123',
    front: 'Goodbye',
    back: 'До свидания',
    order: 1,
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  },
];

// Опционально: прогресс изучения
const progress = [
  {
    flashcardId: '1',
    level: 3,
    nextReview: new Date().toISOString(),
  },
];

// Используйте компонент
<Flashcards
  flashcards={flashcards}
  progress={progress}
  onReview={(flashcardId, correct) => {
    // Отправьте результат на сервер
    console.log(`Flashcard ${flashcardId}: ${correct ? 'correct' : 'incorrect'}`);
    // Здесь можно вызвать API для сохранения прогресса
  }}
/>
```

**Параметры:**
- `flashcards` - массив карточек
  - `id` - уникальный идентификатор
  - `lessonId` - ID урока
  - `front` - текст на лицевой стороне
  - `back` - текст на обратной стороне
  - `order` - порядок отображения
  - `createdAt`, `updatedAt` - даты создания и обновления
- `progress` - массив прогресса (опционально)
  - `flashcardId` - ID карточки
  - `level` - уровень изучения (1-5)
  - `nextReview` - дата следующего повторения
- `onReview` - функция обратного вызова при ответе (опционально)

**Как работает:**
1. Студент видит лицевую сторону карточки
2. Нажимает на карточку, чтобы перевернуть
3. Видит обратную сторону
4. Нажимает ✓ (правильно) или ✗ (неправильно)
5. Карточка автоматически переходит к следующей
6. После всех карточек начинается новый цикл

**Навигация:**
- Стрелки влево/вправо - переход между карточками
- Кнопка с иконкой поворота - перевернуть текущую карточку

---

## 🔗 Интеграции

### 1. Google Docs Embed

**Описание:** Встраивание документов Google Docs в уроки.

**Как использовать в коде:**

```tsx
import GoogleDocsEmbed from '../components/integrations/GoogleDocsEmbed';

<GoogleDocsEmbed
  docUrl="https://docs.google.com/document/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit"
  title="Пример документа"
  width="100%"
  height="600px"
/>
```

**Параметры:**
- `docUrl` - URL документа Google Docs (любой формат)
- `title` - заголовок документа (опционально)
- `width` - ширина iframe (по умолчанию "100%")
- `height` - высота iframe (по умолчанию "600px")

**Как работает:**
- Автоматически конвертирует URL в формат для встраивания
- Показывает документ в iframe
- При ошибке загрузки показывает кнопку "Открыть в новой вкладке"

---

### 2. Quizlet Import

**Описание:** Импорт флеш-карточек из Quizlet.

**Как использовать в коде:**

```tsx
import QuizletImport from '../components/integrations/QuizletImport';

<QuizletImport
  lessonId="lesson-123"
  onImportComplete={(flashcards) => {
    console.log('Импортировано карточек:', flashcards.length);
    // Обновите список карточек в уроке
  }}
/>
```

**Параметры:**
- `lessonId` - ID урока, куда импортировать карточки
- `onImportComplete` - функция обратного вызова после успешного импорта

**Как работает:**
1. Студент или учитель вставляет URL набора Quizlet
2. Нажимает "Импортировать карточки"
3. Система извлекает карточки из Quizlet
4. Создает карточки в уроке
5. Показывает сообщение об успехе

**Примечание:** Для работы требуется настройка API Quizlet на бэкенде.

---

## 🧪 Тестирование функций

В приложении есть специальная страница для тестирования всех функций.

**Как открыть:**
1. Войдите в систему
2. Перейдите по маршруту `/test-features` (если он настроен в роутинге)
3. Или добавьте ссылку в меню для разработчиков

**Доступные вкладки:**
- **Язык** - тестирование переключения языков интерфейса
- **Drag & Drop** - пример упражнения на перетаскивание
- **Matching** - пример упражнения на сопоставление
- **Флеш-карточки** - пример работы с карточками
- **Интеграции** - примеры интеграций с внешними сервисами

---

## 📖 Использование в уроках

### Добавление упражнений в урок

Чтобы добавить упражнения в урок, используйте компоненты в `LessonView.tsx`:

```tsx
import DragAndDrop from '../components/exercises/DragAndDrop';
import Matching from '../components/exercises/Matching';
import Flashcards from '../components/Flashcards';

// В компоненте урока
<div className="lesson-content">
  {/* Обычный контент урока */}
  <div dangerouslySetInnerHTML={{ __html: lesson.content }} />
  
  {/* Добавьте упражнение */}
  <div className="my-8">
    <h3>Упражнение: Сопоставление</h3>
    <Matching
      items={lessonMatchingItems}
      onComplete={(isCorrect) => {
        // Сохраните результат
      }}
    />
  </div>
</div>
```

### Получение данных упражнений из API

Рекомендуется хранить данные упражнений в базе данных и получать их через API:

```tsx
// Получение упражнений для урока
const { data: exercises } = useQuery(
  ['lessonExercises', lessonId],
  async () => {
    const response = await api.get(`/lessons/${lessonId}/exercises`);
    return response.data.data;
  }
);

// Использование
{exercises?.matching && (
  <Matching items={exercises.matching} />
)}
```

### Сохранение результатов

Сохраняйте результаты выполнения упражнений:

```tsx
const saveResult = async (exerciseId: string, isCorrect: boolean) => {
  await api.post(`/exercises/${exerciseId}/results`, {
    isCorrect,
    timestamp: new Date().toISOString(),
  });
};

<Matching
  items={items}
  onComplete={(isCorrect) => {
    saveResult('matching-1', isCorrect);
  }}
/>
```

---

## 💡 Примеры использования

### Пример 1: Полный урок с упражнениями

```tsx
export default function LessonView() {
  const { lessonId } = useParams();
  
  // Получаем данные урока
  const { data: lesson } = useQuery(['lesson', lessonId], ...);
  
  // Получаем упражнения
  const { data: exercises } = useQuery(['exercises', lessonId], ...);
  
  return (
    <div>
      {/* Видео урока */}
      <ReactPlayer url={lesson.videoUrl} />
      
      {/* Контент урока */}
      <div dangerouslySetInnerHTML={{ __html: lesson.content }} />
      
      {/* Упражнение 1: Сопоставление */}
      {exercises?.matching && (
        <div className="my-8">
          <h2>Упражнение 1: Сопоставьте слова</h2>
          <Matching
            items={exercises.matching}
            onComplete={(isCorrect) => {
              // Сохранить результат
            }}
          />
        </div>
      )}
      
      {/* Упражнение 2: Drag & Drop */}
      {exercises?.dragDrop && (
        <div className="my-8">
          <h2>Упражнение 2: Распределите по категориям</h2>
          <DragAndDrop
            items={exercises.dragDrop.items}
            dropZones={exercises.dragDrop.zones}
            onComplete={(isCorrect) => {
              // Сохранить результат
            }}
          />
        </div>
      )}
      
      {/* Флеш-карточки */}
      {exercises?.flashcards && (
        <div className="my-8">
          <h2>Изучите карточки</h2>
          <Flashcards
            flashcards={exercises.flashcards}
            onReview={(flashcardId, correct) => {
              // Сохранить прогресс
            }}
          />
        </div>
      )}
    </div>
  );
}
```

### Пример 2: Интеграция Google Docs

```tsx
<div className="lesson-content">
  <h2>Дополнительные материалы</h2>
  <GoogleDocsEmbed
    docUrl="https://docs.google.com/document/d/YOUR_DOC_ID/edit"
    title="Грамматические правила"
  />
</div>
```

---

## 🔧 Настройка на бэкенде

Для полноценной работы функций необходимо настроить API endpoints:

### Endpoints для упражнений

```typescript
// GET /lessons/:lessonId/exercises
// Возвращает все упражнения для урока

// POST /exercises/:exerciseId/results
// Сохраняет результат выполнения упражнения
```

### Endpoints для флеш-карточек

```typescript
// GET /lessons/:lessonId/flashcards
// Возвращает карточки для урока

// POST /flashcards/:flashcardId/review
// Сохраняет результат изучения карточки
```

### Endpoints для интеграций

```typescript
// POST /flashcards/import-quizlet
// Импортирует карточки из Quizlet
```

---

## 📝 Примечания

1. **Производительность:** Компоненты оптимизированы для работы с большим количеством элементов
2. **Адаптивность:** Все компоненты адаптивны и работают на мобильных устройствах
3. **Доступность:** Компоненты поддерживают клавиатурную навигацию
4. **Локализация:** Все тексты поддерживают многоязычность через i18n

---

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что API endpoints настроены правильно
3. Проверьте формат данных, передаваемых в компоненты

---

**Последнее обновление:** 2024

