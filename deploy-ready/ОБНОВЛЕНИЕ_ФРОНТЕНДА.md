# 🚀 Инструкция по обновлению фронтенда

## ✅ Статус: Готово к обновлению

**Дата обновления:** $(date)
**Версия:** С системой групповых классов

---

## 📦 Что нового в этом обновлении

### 🎓 Система групповых классов
- ✅ Создание и управление групповыми классами
- ✅ Добавление студентов в классы
- ✅ Управление статусами студентов
- ✅ Фильтрация и поиск классов
- ✅ Новый раздел в меню "Групповые классы"

### 🔧 Технические улучшения
- ✅ Обновленные зависимости
- ✅ Оптимизированная сборка
- ✅ Улучшенная производительность

---

## 📋 Файлы для обновления

### Основные файлы:
- ✅ `index.html` - обновлен
- ✅ `assets/index-COd7gx1G.js` - новый главный JS файл (1.16 MB)
- ✅ `assets/index-BfDhOTJv.css` - обновленные стили (71 KB)

### Дополнительные файлы (если их нет на сервере):
- `assets/Preview-D5JXwtDI.js` - компонент превью
- `assets/YouTube-UWZkib8z.js` - YouTube плеер
- `assets/Vimeo-CqVd_xmW.js` - Vimeo плеер
- И другие компоненты плееров

---

## 🎯 Пошаговая инструкция

### Шаг 1: Резервное копирование (РЕКОМЕНДУЕТСЯ)

1. Войдите в cPanel → File Manager
2. Перейдите в `public_html/academy/`
3. Создайте папку `backup_$(date +%Y%m%d)/`
4. Скопируйте туда:
   - `index.html`
   - Всю папку `assets/`

### Шаг 2: Удаление старых файлов

**ВАЖНО:** Удалите только файлы, которые есть в новой версии!

1. В `public_html/academy/assets/` удалите:
   - ❌ Старый `index-*.js` (например, `index-D-mKXtb1.js` или `index-COOPZO17.js`)
   - ❌ Старый `index-*.css` (например, `index-B4iBnszN.css` или `index-LLn-KSah.css`)
   - ❌ Старый `Preview-*.js` (например, `Preview-D5JXwtDI.js` или `Preview-J4hSyiZS.js`)

2. **НЕ удаляйте:**
   - ✅ `.htaccess` (если он есть)
   - ✅ Другие файлы, которых нет в списке выше

### Шаг 3: Загрузка новых файлов

Из папки `deploy-ready/` загрузите:

1. **`index.html`**
   - Загрузите в корень: `public_html/academy/index.html`
   - **Замените** существующий файл

2. **Файлы из `assets/`**
   - Загрузите все файлы из `deploy-ready/assets/` в `public_html/academy/assets/`
   - **Замените** существующие файлы с такими же именами
   - **Добавьте** новые файлы, которых еще нет

### Шаг 4: Проверка прав доступа

Убедитесь, что файлы имеют правильные права:
- Файлы: `644` (rw-r--r--)
- Папки: `755` (rwxr-xr-x)

В cPanel File Manager:
1. Выделите все загруженные файлы
2. Правый клик → Change Permissions
3. Установите: `644` для файлов, `755` для папок

### Шаг 5: Очистка кэша

**Обязательно очистите кэш браузера:**

- **Windows/Linux:** `Ctrl + Shift + R` или `Ctrl + F5`
- **Mac:** `Cmd + Shift + R`
- Или откройте сайт в режиме инкогнито

**На сервере (если есть кэширование):**
- Очистите кэш в cPanel (если используется)
- Очистите CDN кэш (если используется Cloudflare и т.д.)

---

## ✅ Проверка после обновления

### 1. Проверка загрузки файлов

1. Откройте сайт: `https://academy.dilmurodnurkhonov.uz`
2. Откройте DevTools (F12) → вкладка **Network**
3. Обновите страницу (Ctrl+Shift+R)
4. Проверьте, что загружаются файлы с новыми именами:
   - ✅ `index-COd7gx1G.js` (новый)
   - ✅ `index-BfDhOTJv.css` (новый)
   - ❌ НЕ старые файлы

### 2. Проверка функциональности

Проверьте следующие функции:

- ✅ **Вход в систему** - работает
- ✅ **Дашборд** - загружается корректно
- ✅ **Меню** - отображается правильно
- ✅ **Новый раздел "Групповые классы"** - доступен для учителей
- ✅ **Создание класса** - работает
- ✅ **Все остальные страницы** - работают

### 3. Проверка консоли

1. Откройте DevTools (F12) → вкладка **Console**
2. Обновите страницу
3. **Не должно быть ошибок:**
   - ❌ 404 ошибки (файлы не найдены)
   - ❌ JavaScript ошибки
   - ❌ CORS ошибки

---

## 🔧 Решение проблем

### Проблема: Старые файлы все еще загружаются

**Решение:**
1. Убедитесь, что удалили старые файлы из `assets/`
2. Очистите кэш браузера (Ctrl+Shift+R)
3. Очистите кэш сервера/CDN
4. Проверьте `.htaccess` - возможно, там есть кэширование

### Проблема: Сайт не загружается / белый экран

**Решение:**
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что `index.html` загружен правильно
3. Проверьте, что все файлы из `assets/` загружены
4. Проверьте права доступа к файлам (644 для файлов, 755 для папок)
5. Восстановите из резервной копии, если нужно

### Проблема: Ошибка 404 для некоторых файлов

**Решение:**
1. Проверьте, что все файлы из `assets/` загружены
2. Проверьте имена файлов - они должны совпадать с теми, что в `index.html`
3. Проверьте права доступа

### Проблема: Раздел "Групповые классы" не виден

**Решение:**
1. Убедитесь, что вы вошли как учитель (TEACHER) или админ (ADMIN)
2. Проверьте консоль браузера на ошибки
3. Обновите страницу (Ctrl+Shift+R)

---

## 📊 Список файлов для загрузки

### Обязательные файлы:

```
public_html/academy/
├── index.html (465 байт)
└── assets/
    ├── index-COd7gx1G.js (1,157 KB) ⭐ ГЛАВНЫЙ ФАЙЛ
    ├── index-BfDhOTJv.css (71 KB) ⭐ СТИЛИ
    ├── Preview-DDcVWjQW.js (3 KB)
    ├── YouTube-C__JoubX.js (4.4 KB)
    ├── Vimeo-BCnvJHJ6.js (3.6 KB)
    ├── FilePlayer-d0ITJvi3.js (9 KB)
    └── [другие файлы плееров]
```

### Опциональные файлы (если их нет):

- Все остальные файлы из `assets/` (компоненты плееров)

---

## 🎉 После успешного обновления

1. ✅ Сайт работает корректно
2. ✅ Новый функционал доступен
3. ✅ Нет ошибок в консоли
4. ✅ Все страницы загружаются

**Готово! Система обновлена! 🚀**

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Восстановите из резервной копии при необходимости

---

**Дата создания инструкции:** $(date)
**Версия фронтенда:** С системой групповых классов
