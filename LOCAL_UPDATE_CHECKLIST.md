# ✅ Чеклист для проверки обновлений

## 🔄 Обновление завершено

### Что было обновлено:
1. ✅ Добавлена роль CURATOR (Куратор)
2. ✅ Ограничены права TEACHER (только свои курсы и студенты)
3. ✅ Создание студентов только для ADMIN
4. ✅ Групповые чаты в списке чатов
5. ✅ API для управления кураторами

## 🧪 Что проверить

### 1. Роли и права доступа

#### Как ADMIN:
- [ ] Создать куратора через API: `POST /api/curators`
- [ ] Просмотреть список кураторов: `GET /api/curators`
- [ ] Создать студента: `POST /api/students`
- [ ] Удалить студента: `DELETE /api/students/:id`
- [ ] Просмотреть все курсы (не только свои)
- [ ] Просмотреть всех студентов

#### Как TEACHER:
- [ ] Просмотреть только свои курсы (автоматическая фильтрация)
- [ ] Просмотреть только своих студентов (записанных на его курсы)
- [ ] Общаться только со своими студентами в чатах
- [ ] Попытаться создать студента (должна быть ошибка 403)
- [ ] Попытаться удалить студента (должна быть ошибка 403)
- [ ] Экспортировать только своих студентов

### 2. Групповые чаты

#### Как TEACHER:
- [ ] Открыть страницу чатов `/teacher/chats`
- [ ] Увидеть групповые чаты классов в списке
- [ ] Кликнуть на групповой чат - должен открыться `/classes/:id/chat`
- [ ] Отправить сообщение в групповой чат
- [ ] Увидеть сообщения в реальном времени (Socket.IO)

### 3. Групповые классы

#### Как TEACHER:
- [ ] Создать класс: `/teacher/classes`
- [ ] Добавить студентов в класс
- [ ] Открыть детали класса
- [ ] Открыть групповой чат класса
- [ ] Управлять статусами студентов (одобрить/отклонить)

## 🚀 Запуск приложения

### Backend:
```bash
cd backend
npm run dev
```
Сервер запустится на `http://localhost:5001`

### Frontend:
```bash
cd frontend
npm run dev
```
Приложение запустится на `http://localhost:3000` (или другой порт)

## 📝 Тестовые данные

### Войти как ADMIN:
- Телефон: `+998900350151`
- Пароль: `admin123`

### Создать TEACHER:
1. Зарегистрировать нового пользователя
2. В базе данных изменить роль на `TEACHER`
3. Создать курс от имени этого учителя
4. Записать студента на курс
5. Войти как учитель и проверить ограничения

## 🔍 Проверка API

### Проверить ограничения TEACHER:
```bash
# Как TEACHER попытаться создать студента (должна быть ошибка 403)
curl -X POST http://localhost:5001/api/students \
  -H "Authorization: Bearer <TEACHER_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"firstName":"Test","lastName":"Student","phone":"+998901234567"}'

# Как TEACHER получить только свои курсы
curl -X GET http://localhost:5001/api/courses \
  -H "Authorization: Bearer <TEACHER_TOKEN>"

# Как TEACHER получить только своих студентов
curl -X GET http://localhost:5001/api/students \
  -H "Authorization: Bearer <TEACHER_TOKEN>"
```

### Создать куратора (только ADMIN):
```bash
curl -X POST http://localhost:5001/api/curators \
  -H "Authorization: Bearer <ADMIN_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Иван",
    "lastName": "Кураторов",
    "phone": "+998901234568",
    "email": "curator@example.com",
    "password": "curator123"
  }'
```

## ✅ Готово к тестированию!

Все изменения применены, база данных синхронизирована, зависимости установлены.



