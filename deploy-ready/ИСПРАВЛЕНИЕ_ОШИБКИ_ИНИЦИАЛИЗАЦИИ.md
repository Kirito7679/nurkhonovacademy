# Исправление ошибки инициализации

## 🔧 Проблема

Ошибка: `ReferenceError: Cannot access 't' before initialization`

Эта ошибка возникала из-за неправильного порядка загрузки модулей при сборке. Все vendor библиотеки были собраны в один большой чанк, что вызывало проблемы с инициализацией переменных.

## ✅ Решение

Обновлена конфигурация сборки (`vite.config.ts`) для правильного разделения vendor библиотек на отдельные чанки:

1. **vendor-react-core** - React и React DOM (загружаются первыми)
2. **vendor-react-router** - React Router
3. **vendor-react-query** - React Query
4. **vendor-i18n** - i18next и react-i18next (важно для переводов)
5. **vendor-other** - остальные библиотеки

Это гарантирует правильный порядок загрузки и инициализации модулей.

## 📦 Что изменилось

### До:
- Все vendor библиотеки в одном чанке `vendor-react-DpKlTPJd.js` (1.0 MB)
- Проблемы с порядком инициализации

### После:
- Разделение на логические чанки
- Правильный порядок загрузки
- Исправлена ошибка инициализации

## 🚀 Обновление на сервере

1. **УДАЛИТЕ** все старые файлы из `assets/` на сервере
2. Загрузите **ВСЕ** новые файлы из этой папки
3. Особенно важно загрузить новые vendor чанки:
   - `vendor-react-core-*.js`
   - `vendor-react-router-*.js`
   - `vendor-react-query-*.js`
   - `vendor-i18n-*.js`
   - `vendor-other-*.js`
4. Загрузите обновленный `index.html`
5. Очистите кеш браузера (Ctrl+Shift+R)

## ✅ Проверка

После загрузки:
1. Откройте сайт
2. Перейдите на страницу курса
3. Ошибка `Cannot access 't' before initialization` должна исчезнуть
4. Проверьте консоль браузера (F12) - не должно быть ошибок

---

**Дата исправления:** 2024-12-16  
**Версия:** С исправлением ошибки инициализации
