# 💳 Требования для подключения платежных систем с автоплатежом

## 📋 Обзор

Данный документ описывает требования и процесс подключения платежных систем для платформы Nurkhonov Academy с поддержкой автоматического ежемесячного списания (recurring payments / subscriptions).

## 🎯 Поддерживаемые платежные системы

### 1. **Click** (Uzcard)
### 2. **Payme**
### 3. **Uzum Tezkor**
### 4. **Payze** (опционально)
### 5. **Apelsin** (опционально)

---

## 🔐 Общие требования для всех систем

### 1. **Юридические требования**

#### 1.1. Регистрация бизнеса
- ✅ Зарегистрированное юридическое лицо в Узбекистане
- ✅ ИНН (Идентификационный номер налогоплательщика)
- ✅ Банковский счет в узбекском банке
- ✅ Лицензия на образовательную деятельность (если требуется)

#### 1.2. Документы для подключения
- 📄 Копия устава организации
- 📄 Свидетельство о государственной регистрации
- 📄 Справка из налоговой
- 📄 Банковская справка о наличии счета
- 📄 Паспортные данные директора/уполномоченного лица
- 📄 Договор с платежной системой

#### 1.3. Технические требования
- ✅ HTTPS сертификат (обязательно для всех систем)
- ✅ Webhook endpoint для обработки уведомлений
- ✅ IP whitelist (для некоторых систем)
- ✅ Логирование всех транзакций
- ✅ Защита от мошенничества (fraud detection)

---

## 💰 Click (Uzcard) - Требования

### 2.1. Регистрация и получение доступа

#### Шаг 1: Подача заявки
1. Перейдите на сайт [Click.uz](https://click.uz)
2. Заполните форму регистрации как мерчант
3. Приложите необходимые документы (см. раздел 1.2)

#### Шаг 2: Получение учетных данных
После одобрения заявки вы получите:
- `merchant_id` - ID мерчанта
- `service_id` - ID сервиса
- `secret_key` - Секретный ключ для подписи запросов
- `merchant_user_id` - ID пользователя мерчанта
- `merchant_user_password` - Пароль пользователя мерчанта

### 2.2. API Endpoints

#### Основные endpoints:
```
POST https://my.click.uz/services/pay
POST https://my.click.uz/services/pay/check
POST https://my.click.uz/services/pay/complete
POST https://my.click.uz/services/pay/cancel
```

#### Webhook для уведомлений:
```
POST https://your-domain.com/api/payments/click/webhook
```

### 2.3. Автоплатеж (Recurring Payments)

#### Требования для автоплатежа:
- ✅ Токенизация карты (сохранение токена карты)
- ✅ Согласие пользователя на автоплатеж
- ✅ Уведомление пользователя перед списанием (минимум за 3 дня)
- ✅ Возможность отмены подписки

#### Процесс создания подписки:
1. Первая оплата через стандартный процесс
2. Сохранение `card_token` после успешной оплаты
3. Использование токена для последующих списаний
4. Ежемесячное автоматическое списание

### 2.4. Комиссии
- **Обычные платежи**: 1.5-2% от суммы
- **Автоплатеж**: 1.2-1.8% от суммы
- **Минимальная сумма**: 1,000 сум
- **Максимальная сумма**: зависит от лимитов карты

### 2.5. Переменные окружения
```env
CLICK_MERCHANT_ID=your_merchant_id
CLICK_SERVICE_ID=your_service_id
CLICK_SECRET_KEY=your_secret_key
CLICK_MERCHANT_USER_ID=your_user_id
CLICK_MERCHANT_USER_PASSWORD=your_password
CLICK_WEBHOOK_SECRET=your_webhook_secret
```

### 2.6. Документация
- Официальная документация: [docs.click.uz](https://docs.click.uz)
- Тестовое окружение: доступно после регистрации

---

## 💳 Payme - Требования

### 3.1. Регистрация и получение доступа

#### Шаг 1: Регистрация в Payme Business
1. Перейдите на [Payme.uz](https://payme.uz)
2. Зарегистрируйтесь как бизнес-клиент
3. Заполните анкету мерчанта
4. Приложите документы (см. раздел 1.2)

#### Шаг 2: Активация API
1. Войдите в Payme Dashboard
2. Перейдите в **Apps Marketplace** → **Integration**
3. Выберите **E-commerce API/SDK**
4. Нажмите **Setup** для активации

#### Шаг 3: Получение ключей
После активации вы получите:
- `merchant_id` - ID мерчанта
- `key` - API ключ
- `secret` - Секретный ключ
- `cashier_id` - ID кассира (для некоторых операций)

### 3.2. API Endpoints

#### Основные endpoints:
```
POST https://checkout.paycom.uz/api
```

#### Webhook для уведомлений:
```
POST https://your-domain.com/api/payments/payme/webhook
```

### 3.3. Автоплатеж (Subscriptions)

#### Возможности Payme API:
- ✅ Создание подписок (subscriptions)
- ✅ Управление подписками
- ✅ Автоматическое списание
- ✅ Токенизация карт
- ✅ Отложенные платежи

#### Процесс создания подписки:
1. Создание подписки через API
2. Первая оплата пользователем
3. Сохранение токена карты
4. Автоматическое ежемесячное списание

### 3.4. Комиссии
- **Обычные платежи**: 1.5-2% от суммы
- **Подписки**: 1.2-1.8% от суммы
- **Минимальная сумма**: 1,000 сум
- **Максимальная сумма**: зависит от лимитов карты

### 3.5. Переменные окружения
```env
PAYME_MERCHANT_ID=your_merchant_id
PAYME_KEY=your_api_key
PAYME_SECRET=your_secret_key
PAYME_CASHIER_ID=your_cashier_id
PAYME_WEBHOOK_SECRET=your_webhook_secret
```

### 3.6. Документация
- Официальная документация: [docs.payme.io](https://docs.payme.io)
- API для подписок: [docs.payme.io/docs/payments/s5v5chw9si9dj-recurring-payments](https://docs.payme.io/docs/payments/s5v5chw9si9dj-recurring-payments)

---

## 🛒 Uzum Tezkor - Требования

### 4.1. Регистрация и получение доступа

#### Шаг 1: Подача заявки
1. Перейдите на [Uzum.uz](https://uzum.uz)
2. Найдите раздел "Для бизнеса" или "Партнерам"
3. Заполните форму подключения
4. Приложите необходимые документы

#### Шаг 2: Получение учетных данных
После одобрения заявки вы получите:
- `merchant_id` - ID мерчанта
- `api_key` - API ключ
- `secret_key` - Секретный ключ

### 4.2. API Endpoints

#### Основные endpoints:
```
POST https://api.uzum.uz/v1/payments/create
POST https://api.uzum.uz/v1/payments/check
POST https://api.uzum.uz/v1/payments/complete
```

#### Webhook для уведомлений:
```
POST https://your-domain.com/api/payments/uzum/webhook
```

### 4.3. Автоплатеж

#### Особенности:
- ✅ Поддержка подписок
- ✅ Токенизация карт
- ✅ Автоматическое списание
- ⚠️ Требуется отдельное соглашение для автоплатежа

### 4.4. Комиссии
- **Обычные платежи**: 1.5-2.5% от суммы
- **Подписки**: 1.2-2% от суммы
- **Минимальная сумма**: 1,000 сум

### 4.5. Переменные окружения
```env
UZUM_MERCHANT_ID=your_merchant_id
UZUM_API_KEY=your_api_key
UZUM_SECRET_KEY=your_secret_key
UZUM_WEBHOOK_SECRET=your_webhook_secret
```

### 4.6. Документация
- Официальная документация: [developers.uzum.uz](https://developers.uzum.uz) (если доступна)
- Контакт для разработчиков: через форму на сайте

---

## 🔧 Техническая реализация

### 5.1. Структура базы данных

#### Необходимые таблицы:

```prisma
model Payment {
  id              String   @id @default(uuid())
  userId          String
  courseId        String?
  subscriptionId  String?
  amount          Float
  currency        String   @default("UZS")
  status          String   // PENDING, COMPLETED, FAILED, CANCELLED, REFUNDED
  paymentMethod   String   // CLICK, PAYME, UZUM
  transactionId   String?  @unique
  externalId      String?  // ID транзакции в платежной системе
  cardToken       String?  // Токен карты для автоплатежа
  metadata        Json?    // Дополнительные данные
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  user            User     @relation(fields: [userId], references: [id])
  course          Course?  @relation(fields: [courseId], references: [id])
  subscription    Subscription? @relation(fields: [subscriptionId], references: [id])

  @@index([userId])
  @@index([status])
  @@index([paymentMethod])
  @@map("payments")
}

model Subscription {
  id              String   @id @default(uuid())
  userId          String
  courseId        String
  paymentMethod   String   // CLICK, PAYME, UZUM
  amount          Float
  currency        String   @default("UZS")
  status          String   // ACTIVE, CANCELLED, EXPIRED, SUSPENDED
  cardToken       String?  // Токен карты
  nextBillingDate DateTime
  lastBillingDate DateTime?
  billingCycle    String   @default("MONTHLY") // MONTHLY, WEEKLY, YEARLY
  autoRenew       Boolean  @default(true)
  cancelledAt     DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  user            User     @relation(fields: [userId], references: [id])
  course          Course   @relation(fields: [courseId], references: [id])
  payments        Payment[]

  @@index([userId])
  @@index([status])
  @@index([nextBillingDate])
  @@map("subscriptions")
}
```

### 5.2. API Endpoints для платежей

#### Создание платежа:
```
POST /api/payments/create
Body: {
  courseId: string,
  paymentMethod: "CLICK" | "PAYME" | "UZUM",
  amount: number,
  saveCard?: boolean // для автоплатежа
}
```

#### Создание подписки:
```
POST /api/subscriptions/create
Body: {
  courseId: string,
  paymentMethod: "CLICK" | "PAYME" | "UZUM",
  amount: number,
  billingCycle: "MONTHLY" | "WEEKLY" | "YEARLY"
}
```

#### Webhook для уведомлений:
```
POST /api/payments/{paymentMethod}/webhook
```

#### Отмена подписки:
```
POST /api/subscriptions/{subscriptionId}/cancel
```

### 5.3. Сервисы для интеграции

#### Структура сервисов:
```
backend/src/services/payments/
  ├── paymentService.ts        // Основной сервис
  ├── clickService.ts          // Интеграция с Click
  ├── paymeService.ts          // Интеграция с Payme
  ├── uzumService.ts           // Интеграция с Uzum
  ├── subscriptionService.ts   // Управление подписками
  └── webhookService.ts        // Обработка webhook'ов
```

### 5.4. Контроллеры

```
backend/src/controllers/
  ├── paymentController.ts     // Управление платежами
  └── subscriptionController.ts // Управление подписками
```

---

## 🔄 Процесс автоплатежа

### 6.1. Создание подписки

1. **Пользователь выбирает курс и способ оплаты**
2. **Первая оплата:**
   - Создается платеж через выбранную систему
   - Пользователь вводит данные карты
   - После успешной оплаты сохраняется токен карты
   - Создается подписка со статусом `ACTIVE`
   - Устанавливается `nextBillingDate` (через месяц)

3. **Автоматическое списание:**
   - Cron job проверяет подписки с `nextBillingDate <= today`
   - Для каждой подписки создается платеж с использованием токена
   - При успешном списании:
     - Обновляется `lastBillingDate`
     - Обновляется `nextBillingDate` (+1 месяц)
     - Создается запись в таблице `Payment`
   - При неудачном списании:
     - Отправляется уведомление пользователю
     - Подписка переходит в статус `SUSPENDED`
     - Повторная попытка через 3 дня

### 6.2. Уведомления пользователя

#### Перед списанием:
- За 3 дня до списания: email/SMS уведомление
- За 1 день до списания: push-уведомление

#### После списания:
- При успешном списании: подтверждение
- При неудачном списании: уведомление с инструкциями

### 6.3. Отмена подписки

1. Пользователь отменяет подписку
2. Подписка переходит в статус `CANCELLED`
3. Устанавливается `cancelledAt`
4. Автоплатеж прекращается
5. Доступ к курсу сохраняется до конца оплаченного периода

---

## 🛡️ Безопасность

### 7.1. Требования безопасности

- ✅ **HTTPS обязателен** для всех endpoints
- ✅ **Валидация webhook'ов** через подпись
- ✅ **IP whitelist** для webhook endpoints (где возможно)
- ✅ **Шифрование токенов карт** в базе данных
- ✅ **Логирование всех операций**
- ✅ **Rate limiting** для API endpoints
- ✅ **Защита от SQL injection** (использование Prisma ORM)
- ✅ **CSRF защита** для форм оплаты

### 7.2. Хранение токенов

- Токены карт должны храниться в **зашифрованном виде**
- Использовать **environment variables** для ключей шифрования
- Никогда не логировать полные токены
- Регулярная ротация ключей шифрования

### 7.3. Обработка ошибок

- Все ошибки должны логироваться
- Пользователю показывать понятные сообщения
- Не раскрывать технические детали в ошибках

---

## 📊 Мониторинг и аналитика

### 8.1. Метрики для отслеживания

- Количество успешных платежей
- Количество неудачных платежей
- Средняя сумма платежа
- Конверсия (просмотр → оплата)
- Количество активных подписок
- Количество отмененных подписок
- Средний срок жизни подписки

### 8.2. Логирование

- Все транзакции должны логироваться
- Webhook'и от платежных систем
- Ошибки при обработке платежей
- Изменения статусов подписок

---

## 🧪 Тестирование

### 9.1. Тестовое окружение

Все платежные системы предоставляют тестовое окружение:
- **Click**: Тестовый мерчант ID
- **Payme**: Sandbox environment
- **Uzum**: Тестовый API (если доступен)

### 9.2. Тестовые карты

Каждая система предоставляет тестовые карты для проверки:
- Успешная оплата
- Неудачная оплата (недостаточно средств)
- Отклоненная карта
- Истекшая карта

### 9.3. Чек-лист тестирования

- [ ] Создание одноразового платежа
- [ ] Создание подписки
- [ ] Автоматическое списание
- [ ] Отмена подписки
- [ ] Обработка неудачного списания
- [ ] Webhook уведомления
- [ ] Обработка ошибок
- [ ] Валидация данных
- [ ] Безопасность (HTTPS, подписи)

---

## 📝 Чек-лист подключения

### Подготовка:
- [ ] Зарегистрировано юридическое лицо
- [ ] Открыт банковский счет
- [ ] Подготовлены все документы
- [ ] Настроен HTTPS сертификат
- [ ] Подготовлены webhook endpoints

### Регистрация в платежных системах:
- [ ] Подана заявка в Click
- [ ] Подана заявка в Payme
- [ ] Подана заявка в Uzum (если требуется)
- [ ] Получены все учетные данные

### Техническая реализация:
- [ ] Созданы модели в базе данных
- [ ] Реализованы сервисы для каждой системы
- [ ] Созданы контроллеры
- [ ] Настроены webhook handlers
- [ ] Реализован процесс автоплатежа
- [ ] Настроены уведомления
- [ ] Добавлены переменные окружения

### Тестирование:
- [ ] Протестированы все платежные системы
- [ ] Протестирован автоплатеж
- [ ] Протестированы webhook'и
- [ ] Проверена безопасность

### Запуск:
- [ ] Переключение на production API
- [ ] Мониторинг первых транзакций
- [ ] Настройка алертов
- [ ] Документация для команды

---

## 📞 Контакты и поддержка

### Click:
- Сайт: [click.uz](https://click.uz)
- Техподдержка: через личный кабинет
- Документация: [docs.click.uz](https://docs.click.uz)

### Payme:
- Сайт: [payme.uz](https://payme.uz)
- Техподдержка: [help.payme.io](https://help.payme.io)
- Документация: [docs.payme.io](https://docs.payme.io)

### Uzum:
- Сайт: [uzum.uz](https://uzum.uz)
- Техподдержка: через форму на сайте

---

## ⚠️ Важные замечания

1. **Сроки подключения**: Обычно 5-15 рабочих дней после подачи всех документов
2. **Комиссии**: Могут варьироваться в зависимости от оборота
3. **Лимиты**: Уточняйте лимиты на транзакции при регистрации
4. **Юридические аспекты**: Рекомендуется консультация с юристом
5. **Налогообложение**: Учитывайте НДС и другие налоги при расчете цен
6. **Возвраты**: Убедитесь в понимании политики возвратов каждой системы

---

## 🚀 Следующие шаги

1. **Подготовить документы** (см. раздел 1.2)
2. **Выбрать платежные системы** для подключения
3. **Подать заявки** в выбранные системы
4. **Дождаться одобрения** и получить учетные данные
5. **Реализовать интеграцию** (см. раздел 5)
6. **Протестировать** в тестовом окружении
7. **Запустить** в production

---

**Дата создания**: 2025-01-27  
**Версия**: 1.0  
**Автор**: Nurkhonov Academy Development Team



