# 📁 Загрузка файлов к урокам - Руководство

## ✅ Функционал готов!

Загрузка файлов к урокам полностью настроена и работает.

## 🎯 Что можно загружать:

### Поддерживаемые типы файлов:

1. **Документы:**
   - PDF (`.pdf`)
   - Word (`.doc`, `.docx`)
   - Excel (`.xls`, `.xlsx`)
   - PowerPoint (`.ppt`, `.pptx`)
   - Текстовые файлы (`.txt`)

2. **Изображения:**
   - JPEG (`.jpg`, `.jpeg`)
   - PNG (`.png`)
   - GIF (`.gif`)
   - WebP (`.webp`)

3. **Видео:**
   - MP4 (`.mp4`)
   - WebM (`.webm`)
   - OGG (`.ogg`)
   - QuickTime (`.mov`)
   - AVI (`.avi`)
   - WMV (`.wmv`)
   - FLV (`.flv`)
   - MPEG (`.mpeg`)

4. **Архивы:**
   - ZIP (`.zip`)

### Ограничения:
- **Максимальный размер файла**: 100 MB
- **Хранилище**: Supabase Storage (1 GB бесплатно)

## 📝 Как загрузить файл:

### Для учителей:

1. **Откройте форму редактирования урока:**
   - Перейдите в курс
   - Откройте урок для редактирования
   - Или создайте новый урок и сохраните его

2. **Загрузите файл:**
   - В правой колонке найдите секцию **"Файлы урока"**
   - Нажмите на область **"Загрузить файл"**
   - Выберите файл на компьютере
   - Файл автоматически загрузится в Supabase Storage

3. **Проверка:**
   - После загрузки файл появится в списке
   - Вы увидите название, размер и тип файла
   - Можно удалить файл, нажав на иконку ❌

### Для студентов:

1. **Просмотр файлов:**
   - Откройте урок
   - В разделе **"Файлы урока"** увидите все загруженные файлы
   - Файлы отображаются с иконками по типу:
     - 📄 Документы (синий)
     - 🎥 Видео (фиолетовый)
     - 🖼️ Изображения (зеленый)

2. **Скачивание:**
   - Нажмите кнопку **"Скачать"** (или **"Смотреть"** для видео)
   - Файл откроется в новой вкладке или скачается

## 🔧 Технические детали:

### Backend:
- **Эндпоинт загрузки**: `POST /api/files/upload`
- **Эндпоинт получения**: `GET /api/lessons/:id/files`
- **Эндпоинт скачивания**: `GET /api/files/download/:fileId`
- **Хранилище**: Supabase Storage → bucket `lessons`

### Frontend:
- **Загрузка**: `LessonForm.tsx` (для учителей)
- **Просмотр**: `LessonView.tsx` (для студентов)
- **Автоматическое обновление** списка файлов после загрузки

## ✅ Что уже работает:

1. ✅ Загрузка файлов к урокам (для учителей)
2. ✅ Отображение файлов в уроке (для студентов)
3. ✅ Скачивание файлов
4. ✅ Удаление файлов (для учителей)
5. ✅ Поддержка всех типов файлов (документы, видео, изображения)
6. ✅ Автоматическое сохранение в Supabase Storage
7. ✅ Проверка прав доступа
8. ✅ Валидация размера и типа файла

## 🎯 Примеры использования:

### Загрузка PDF презентации:
1. Создайте/откройте урок
2. В секции "Файлы урока" нажмите "Загрузить файл"
3. Выберите `presentation.pdf`
4. Файл загрузится и будет доступен студентам

### Загрузка видео урока:
1. Создайте/откройте урок
2. Загрузите видео файл (MP4, MOV и др.)
3. Видео будет доступно для скачивания/просмотра студентам

### Загрузка материалов:
1. Загрузите PDF, DOCX, XLSX файлы с дополнительными материалами
2. Студенты смогут скачать их для изучения

## ⚠️ Важно:

1. **Сначала сохраните урок** перед загрузкой файлов
2. **Максимальный размер**: 100 MB на файл
3. **Лимит Supabase**: 1 GB бесплатно (достаточно для ~10-20 видео)
4. **Файлы сохраняются в Supabase Storage** и не теряются при перезапуске

## 🚀 Готово к использованию!

Теперь вы можете:
- ✅ Загружать файлы к урокам
- ✅ Загружать видео файлы
- ✅ Загружать документы и материалы
- ✅ Студенты могут скачивать все файлы

**Всё работает!** 🎉
