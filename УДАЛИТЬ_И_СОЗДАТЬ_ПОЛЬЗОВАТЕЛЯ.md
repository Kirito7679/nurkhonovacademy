# 🔄 Удалить и создать пользователя заново

## 📋 Задача

1. Удалить пользователя с номером **+998900350151**
2. Создать его заново с ролью **ADMIN**

---

## ✅ Выполните команды в терминале:

### Шаг 1: Откройте терминал

На Mac: **Terminal** (Applications → Utilities)

### Шаг 2: Перейдите в папку backend

```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/backend"
```

### Шаг 3: Войдите в Railway (если еще не вошли)

```bash
npx @railway/cli login
```

### Шаг 4: Подключитесь к проекту (если еще не подключены)

```bash
npx @railway/cli link
```

Выберите ваш проект из списка.

### Шаг 5: Удалите пользователя

```bash
npx @railway/cli run npx tsx scripts/deleteUser.ts +998900350151
```

### Шаг 6: Создайте пользователя заново с ролью ADMIN

```bash
npx @railway/cli run npx tsx scripts/createUser.ts "Dilmurod" "Nurkhonov" "+998900350151" "ваш-пароль" "ADMIN"
```

**Замените `"ваш-пароль"` на нужный пароль!**

---

## 📝 Все команды одной строкой:

```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/backend" && \
npx @railway/cli login && \
npx @railway/cli link && \
npx @railway/cli run npx tsx scripts/deleteUser.ts +998900350151 && \
npx @railway/cli run npx tsx scripts/createUser.ts "Dilmurod" "Nurkhonov" "+998900350151" "ваш-пароль" "ADMIN"
```

---

## ✅ Ожидаемый результат:

После удаления:
```
📋 Информация о пользователе:
Имя: [Имя] [Фамилия]
Телефон: +998900350151
Роль: [Роль]
✅ Пользователь успешно удален
```

После создания:
```
✅ Пользователь успешно создан!
Имя: Dilmurod Nurkhonov
Телефон: +998900350151
Роль: ADMIN
ID: [uuid]
💡 Теперь вы можете войти с этими данными!
```

---

## 🔐 Важно про пароль

**Замените `"ваш-пароль"` на реальный пароль!**

Например:
```bash
npx @railway/cli run npx tsx scripts/createUser.ts "Dilmurod" "Nurkhonov" "+998900350151" "MyPassword123" "ADMIN"
```

---

## ✅ После создания

1. Войдите на сайте: `https://academy.dilmurodnurkhonov.uz/login`
2. Используйте:
   - Телефон: `+998900350151`
   - Пароль: тот, который указали при создании
3. У вас будут права администратора!

---

**Выполните команды по порядку! 🚀**
