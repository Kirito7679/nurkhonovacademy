# 🔧 Настройка cPanel для academy.dilmurodnurkhonov.uz

## 📋 Что нужно настроить в cPanel

### Структура:
- **academy.dilmurodnurkhonov.uz** → Фронтенд (файлы на вашем хостинге)
- **api.academy.dilmurodnurkhonov.uz** → API (Railway)

---

## 1️⃣ Настройка Document Root (для фронтенда)

### Текущая настройка:
- **Document Root:** `/academy.dilmurodnurkhonov.uz`

### Что нужно сделать:

**Вариант A: Если Document Root правильный (рекомендуется)**

1. **Оставьте Document Root как есть:** `/academy.dilmurodnurkhonov.uz`
2. **Загрузите файлы фронтенда** в эту папку через File Manager

**Вариант B: Если хотите использовать другую папку**

1. Измените **"New Document Root"** на нужную папку, например:
   ```
   /public_html/academy
   ```
   или
   ```
   /home/nurkh106/public_html/academy
   ```
2. Нажмите **"Update"**

---

## 2️⃣ Загрузка файлов фронтенда

### Шаг 1: Откройте File Manager

1. В cPanel найдите **"File Manager"**
2. Откройте его

### Шаг 2: Перейдите в папку домена

1. Перейдите в папку: `/academy.dilmurodnurkhonov.uz` (или ту, которую указали в Document Root)
2. Если папки нет - создайте её

### Шаг 3: Загрузите файлы

Загрузите **все содержимое** папки `frontend/dist/`:

```
frontend/dist/
├── index.html
├── assets/
│   ├── index-BBTLvlVm.css
│   ├── index-DWYk_lBG.js
│   └── Preview-73e6p_i-.js
└── .htaccess (если есть)
```

**Как загрузить:**
1. В File Manager выберите папку домена
2. Нажмите **"Upload"**
3. Перетащите файлы или выберите их
4. Дождитесь завершения загрузки

### Шаг 4: Проверьте структуру

После загрузки структура должна быть:
```
/academy.dilmurodnurkhonov.uz/
├── index.html
├── assets/
│   ├── index-*.css
│   └── index-*.js
└── .htaccess
```

---

## 3️⃣ Настройка DNS для поддомена api.academy

### Шаг 1: Найдите Zone Editor

1. В cPanel найдите раздел **"Zone Editor"** или **"DNS Management"**
2. Откройте его

### Шаг 2: Выберите домен

1. Найдите домен `dilmurodnurkhonov.uz` (не поддомен!)
2. Нажмите **"Manage"** или **"Edit"**

### Шаг 3: Добавьте CNAME запись

1. Нажмите **"Add Record"** или **"Add CNAME Record"**
2. Заполните:
   ```
   Name: api.academy
   TTL: 3600
   Type: CNAME
   Record: 02odqour.up.railway.app
   ```
3. Нажмите **"Add Record"** или **"Save"**

**Важно:** 
- Имя должно быть `api.academy` (без `.dilmurodnurkhonov.uz`)
- Значение: `02odqour.up.railway.app` (от Railway)

---

## 4️⃣ Настройка .htaccess для SPA роутинга

### Шаг 1: Создайте .htaccess файл

В папке `/academy.dilmurodnurkhonov.uz/` создайте файл `.htaccess`:

1. В File Manager выберите папку домена
2. Нажмите **"New File"**
3. Назовите файл: `.htaccess`

### Шаг 2: Добавьте содержимое

Откройте `.htaccess` и добавьте:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On

  # Если файл или директория существуют, используем их
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # Иначе перенаправляем на index.html для SPA роутинга
  RewriteRule . /index.html [L]
</IfModule>
```

Сохраните файл.

---

## 5️⃣ Проверка работы

### Проверка фронтенда:

1. Откройте: `https://academy.dilmurodnurkhonov.uz`
2. Должен открыться сайт
3. Проверьте в консоли браузера (F12), что запросы идут на:
   ```
   https://api.academy.dilmurodnurkhonov.uz/api/...
   ```

### Проверка API:

1. Откройте: `https://api.academy.dilmurodnurkhonov.uz/api/health`
2. Должно вернуть: `{"success": true, "message": "Server is running"}`

---

## ✅ Чеклист

- [ ] Document Root настроен правильно
- [ ] Файлы фронтенда загружены в папку домена
- [ ] .htaccess файл создан и настроен
- [ ] DNS запись CNAME для `api.academy` добавлена
- [ ] Фронтенд открывается по адресу `academy.dilmurodnurkhonov.uz`
- [ ] API работает по адресу `api.academy.dilmurodnurkhonov.uz`

---

## 🆘 Решение проблем

### Проблема: Файлы не загружаются

**Решение:**
- Проверьте права доступа к папке (должны быть 755)
- Убедитесь, что папка существует
- Попробуйте загрузить через FTP клиент (FileZilla)

### Проблема: DNS не работает

**Решение:**
- Проверьте правильность CNAME записи
- Убедитесь, что имя `api.academy` (без домена)
- Подождите 5-30 минут для распространения DNS
- Проверьте через: `dig api.academy.dilmurodnurkhonov.uz`

### Проблема: Сайт показывает ошибку 404

**Решение:**
- Проверьте, что `index.html` находится в корне папки
- Убедитесь, что `.htaccess` файл создан
- Проверьте права доступа к файлам (644 для файлов, 755 для папок)

---

## 📝 Важные моменты

1. **Document Root** - это папка, где находятся файлы фронтенда
2. **DNS записи** настраиваются в Zone Editor для основного домена `dilmurodnurkhonov.uz`
3. **Поддомен** `api.academy` указывает на Railway через CNAME
4. **Фронтенд** остается на вашем хостинге в папке домена

---

**Готово! После выполнения этих шагов все будет работать! 🚀**

