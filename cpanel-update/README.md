# Готовые файлы для обновления на cPanel

## 📁 Содержимое папки

Эта папка содержит готовые обновленные файлы для загрузки на cPanel:

1. **`useSocket.ts`** - обновленный файл для `frontend/src/hooks/useSocket.ts`
2. **`NotificationBell.tsx`** - обновленный файл для `frontend/src/components/NotificationBell.tsx`

---

## 🚀 Как использовать

### Способ 1: Через cPanel File Manager

1. Откройте cPanel → File Manager
2. Найдите файл `frontend/src/hooks/useSocket.ts`
3. Нажмите Edit
4. Удалите весь код
5. Скопируйте код из `cpanel-update/useSocket.ts`
6. Вставьте и сохраните
7. Повторите для `frontend/src/components/NotificationBell.tsx` с файлом `cpanel-update/NotificationBell.tsx`

### Способ 2: Через FTP

1. Подключитесь через FTP
2. Загрузите файлы, заменив существующие:
   - `useSocket.ts` → `frontend/src/hooks/useSocket.ts`
   - `NotificationBell.tsx` → `frontend/src/components/NotificationBell.tsx`

---

## ✅ Что изменилось

- Улучшена обработка socket-уведомлений (мгновенное обновление)
- Добавлено подключение к socket в NotificationBell
- Уведомления теперь приходят в реальном времени

---

**После обновления уведомления будут работать мгновенно!** 🎉
