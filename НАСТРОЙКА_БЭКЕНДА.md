# 🖥️ Настройка бэкенда на сервере

## ❓ Нужен ли бэкенд?

**Бэкенд нужен, если:**
- ✅ Вы хотите, чтобы API работало на том же сервере
- ✅ Нужна база данных для хранения курсов, студентов, прогресса
- ✅ Нужны функции авторизации, комментариев, уведомлений

**Бэкенд НЕ нужен, если:**
- ❌ API уже работает на другом сервере
- ❌ Используете только статический сайт

---

## 📋 Пошаговая настройка бэкенда

### Шаг 1: Загрузка файлов бэкенда

1. **В файловом менеджере** перейдите в папку `academy.dilmurodnurkhonov.uz`
2. **Создайте папку** `backend-api` (или используйте существующую `backend`)
3. **Загрузите содержимое папки** `UPLOAD_TO_SERVER/backend/`:
   - `dist/` (папка с кодом)
   - `prisma/` (папка с миграциями)
   - `package.json`

**Структура должна быть:**
```
academy.dilmurodnurkhonov.uz/
├── index.html          ← Фронтенд
├── assets/            ← Фронтенд
├── .htaccess          ← Фронтенд
└── backend-api/       ← Бэкенд (или backend/)
    ├── dist/
    ├── prisma/
    └── package.json
```

---

### Шаг 2: Создание файла .env

1. **В папке бэкенда** создайте файл `.env`
2. **Скопируйте** содержимое из `.env.example` (если есть)
3. **Заполните настройки:**

```env
# База данных (SQLite для начала)
DATABASE_URL="file:./prisma/dev.db"

# Секретный ключ для JWT (измените на свой!)
JWT_SECRET="ваш-секретный-ключ-минимум-32-символа"

# Порт сервера
PORT=5001

# Окружение
NODE_ENV=production
```

**Важно:** 
- `JWT_SECRET` должен быть длинным случайным ключом
- Можно сгенерировать: `openssl rand -base64 32`

---

### Шаг 3: Установка зависимостей и настройка

**Через SSH (если доступен):**

```bash
cd ~/academy.dilmurodnurkhonov.uz/backend-api
npm install --production
npx prisma migrate deploy
npx prisma generate
```

**Через терминал в панели управления хостингом:**

1. Найдите раздел **"Terminal"** или **"SSH Access"**
2. Выполните команды выше

**Если SSH недоступен:**
- Используйте **"Node.js приложения"** в панели управления
- Система сама установит зависимости при создании приложения

---

### Шаг 4: Настройка Node.js приложения

В панели управления хостингом:

1. **Найдите раздел "Node.js приложения"** или **"Node.js"**
2. **Создайте новое приложение:**
   - **Имя:** `academy-api` (или любое другое)
   - **Путь к файлу:** `backend-api/dist/app.js`
     - Или полный путь: `/home/nurkh106/academy.dilmurodnurkhonov.uz/backend-api/dist/app.js`
   - **Порт:** Оставьте автоматический (обычно назначается автоматически)
   - **Переменные окружения:** Добавьте из `.env`:
     ```
     DATABASE_URL=file:./prisma/dev.db
     JWT_SECRET=ваш-секретный-ключ
     PORT=5001
     NODE_ENV=production
     ```
   - **Корневая папка:** `backend-api` (или полный путь)

3. **Нажмите "Создать"** или **"Запустить"**

---

### Шаг 5: Настройка прокси для API

Чтобы API работало по адресу `https://academy.dilmurodnukhonov.uz/api`:

**Вариант 1: Через .htaccess (если используете Apache)**

Создайте или обновите файл `.htaccess` в корне поддомена:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Прокси для API запросов на Node.js сервер
  RewriteCond %{REQUEST_URI} ^/api [NC]
  RewriteRule ^api/(.*)$ http://localhost:ПОРТ/api/$1 [P,L]
  
  # SPA routing - все остальные запросы на index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

**Замените `ПОРТ`** на порт, который назначил хостинг для Node.js приложения.

**Вариант 2: Через настройки поддомена**

В панели управления найдите настройки поддомена и настройте проксирование `/api` на ваш Node.js сервер.

---

### Шаг 6: Проверка работы

1. **Проверьте API:**
   ```
   https://academy.dilmurodnukhonov.uz/api/health
   ```
   Должен вернуть: `{"success": true, "message": "Server is running"}`

2. **Проверьте сайт:**
   ```
   https://academy.dilmurodnukhonov.uz
   ```
   Должен открыться фронтенд

3. **Проверьте консоль браузера (F12):**
   - Не должно быть ошибок подключения к API
   - Запросы должны идти на `/api/...`

---

## 🔧 Альтернативный вариант: Без Node.js на хостинге

Если ваш хостинг не поддерживает Node.js, можно:

1. **Использовать внешний сервер** для API (например, Railway, Render, Heroku)
2. **Изменить URL API** в `.env.production` фронтенда на внешний сервер
3. **Загрузить только фронтенд** на текущий хостинг

---

## 📝 Чеклист настройки бэкенда

- [ ] Загружены файлы бэкенда (`dist/`, `prisma/`, `package.json`)
- [ ] Создан файл `.env` с правильными настройками
- [ ] Установлены зависимости (`npm install --production`)
- [ ] Применены миграции (`npx prisma migrate deploy`)
- [ ] Сгенерирован Prisma Client (`npx prisma generate`)
- [ ] Создано Node.js приложение в панели управления
- [ ] Настроен прокси для `/api` (если нужно)
- [ ] Проверена работа API (`/api/health`)

---

## 🆘 Решение проблем

### Проблема: Node.js приложение не запускается
- ✅ Проверьте путь к файлу (`dist/app.js`)
- ✅ Проверьте переменные окружения в настройках приложения
- ✅ Проверьте логи в панели управления

### Проблема: База данных не создается
- ✅ Проверьте путь к базе данных в `DATABASE_URL`
- ✅ Убедитесь, что папка `prisma/` загружена
- ✅ Выполните `npx prisma migrate deploy` вручную

### Проблема: API не доступно
- ✅ Проверьте, что Node.js приложение запущено
- ✅ Проверьте настройки прокси в `.htaccess`
- ✅ Проверьте порт в настройках приложения

---

## 💡 Быстрая команда для проверки

После настройки проверьте:

```bash
# Проверка API
curl https://academy.dilmurodnukhonov.uz/api/health

# Должен вернуть: {"success":true,"message":"Server is running"}
```

---

**Готово! Бэкенд настроен и готов к работе! 🚀**
