# 🚀 Деплой Frontend - Прямо сейчас!

## ✅ Backend готов!
Все изменения закоммичены. Railway автоматически пересоберет backend.

---

## 📦 ШАГ 1: Сборка Frontend

```bash
# Перейдите в папку frontend
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/frontend"

# Соберите проект (без проверки типов для быстрого деплоя)
npm run build:skip-check
```

**Или с проверкой типов (если есть время исправить ошибки):**
```bash
npm run build
```

---

## 📤 ШАГ 2: Загрузка на cPanel

### Через File Manager:

1. **Войдите в cPanel** → File Manager
2. **Перейдите в `public_html`**
3. **Сделайте бэкап** (выделите все → Compress)
4. **Удалите старые файлы** (кроме важных)
5. **Загрузите ВСЕ файлы из `frontend/dist/`**
   - Выберите все файлы в папке `dist/`
   - Загрузите в `public_html`
6. **Проверьте `.htaccess`** - должен быть в корне

### Структура после загрузки:

```
public_html/
├── index.html          ← Главный файл
├── .htaccess          ← Важно!
└── assets/
    ├── index-*.js     ← JavaScript файлы
    ├── index-*.css    ← CSS файлы
    └── ...
```

---

## ✅ ШАГ 3: Проверка

1. Откройте: https://academy.dilmurodnurkhonov.uz
2. Нажмите F12 → Console
3. Проверьте, что нет ошибок
4. Попробуйте зарегистрироваться/войти

---

## 🎯 Готово!

После загрузки файлов сайт должен работать! 🎉
