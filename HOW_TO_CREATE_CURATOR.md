# 📚 Как создать куратора или переназначить студента на куратора

## 🎯 Способы создания куратора

### 1. Через скрипт (рекомендуется)

#### Создать нового куратора:
```bash
cd backend
npx tsx scripts/createCurator.ts "Имя" "Фамилия" "+998901234567" "email@example.com" "password"
```

**Пример:**
```bash
npx tsx scripts/createCurator.ts "Анна" "Петрова" "+998901234567" "anna@example.com" "curator123"
```

**Параметры:**
- `Имя` - имя куратора (обязательно)
- `Фамилия` - фамилия куратора (обязательно)
- `Телефон` - номер телефона в формате +998... (обязательно)
- `Email` - email адрес (опционально)
- `Password` - пароль (опционально, по умолчанию "123456")

#### Переназначить существующего пользователя по телефону:
```bash
npx tsx scripts/createCurator.ts --phone "+998901234567"
```

#### Переназначить существующего пользователя по ID:
```bash
npx tsx scripts/createCurator.ts --id "user-id-here"
```

#### Показать список всех кураторов:
```bash
npx tsx scripts/createCurator.ts --list
```

### 2. Через API (только для ADMIN)

#### Создать куратора через API:
```bash
curl -X POST http://localhost:5001/api/curators \
  -H "Authorization: Bearer <ADMIN_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Имя",
    "lastName": "Фамилия",
    "phone": "+998901234567",
    "email": "email@example.com",
    "password": "password123"
  }'
```

**Пример с токеном:**
```bash
# Сначала получите токен админа
TOKEN=$(curl -s -X POST http://localhost:5001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"+998900350151","password":"admin123"}' \
  | python3 -c "import sys, json; d=json.load(sys.stdin); print(d.get('data', {}).get('token', ''))")

# Затем создайте куратора
curl -X POST http://localhost:5001/api/curators \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Анна",
    "lastName": "Петрова",
    "phone": "+998901234567",
    "email": "anna@example.com",
    "password": "curator123"
  }'
```

#### Получить список кураторов:
```bash
curl -X GET http://localhost:5001/api/curators \
  -H "Authorization: Bearer <ADMIN_TOKEN>"
```

### 3. Через базу данных (прямое изменение)

⚠️ **Внимание:** Используйте только если другие способы недоступны.

```sql
-- Переназначить пользователя на куратора по телефону
UPDATE users 
SET role = 'CURATOR' 
WHERE phone = '+998901234567';

-- Или по ID
UPDATE users 
SET role = 'CURATOR' 
WHERE id = 'user-id-here';
```

## 📋 Примеры использования

### Пример 1: Создать нового куратора
```bash
cd backend
npx tsx scripts/createCurator.ts "Иван" "Кураторов" "+998901234568" "ivan@example.com" "ivan123"
```

**Результат:**
```
✅ Куратор успешно создан!

📋 Информация о кураторе:
   ID: abc123-def456-...
   Имя: Иван Кураторов
   Телефон: +998901234568
   Email: ivan@example.com
   Роль: CURATOR
   Пароль: ivan123
```

### Пример 2: Переназначить студента на куратора
```bash
# Сначала найдем студента
cd backend
npx tsx scripts/createCurator.ts --list

# Затем переназначим по телефону
npx tsx scripts/createCurator.ts --phone "+998947771170"
```

**Результат:**
```
✅ Пользователь успешно переназначен на куратора!

📋 Информация о кураторе:
   ID: fc5349ca-d85f-4142-a07f-ec13d23cf2c5
   Имя: Anna Kurochitskaya
   Телефон: +998947771170
   Email: не указан
   Предыдущая роль: STUDENT
   Новая роль: CURATOR
```

### Пример 3: Проверить список кураторов
```bash
npx tsx scripts/createCurator.ts --list
```

**Результат:**
```
📋 Найдено кураторов: 2

1. Анна Петрова
   Телефон: +998901234567
   Email: anna@example.com
   ID: abc123-def456-...
   Создан: 15.12.2025, 14:30:00

2. Иван Кураторов
   Телефон: +998901234568
   Email: ivan@example.com
   ID: xyz789-uvw012-...
   Создан: 15.12.2025, 14:35:00
```

## 🔐 Требования

- **Создание кураторов**: Только пользователи с ролью `ADMIN`
- **Переназначение через скрипт**: Можно выполнить локально (не требует авторизации)
- **Переназначение через API**: Требуется токен администратора

## ⚠️ Важные замечания

1. **Уникальность телефона**: Номер телефона должен быть уникальным в системе
2. **Пароль по умолчанию**: Если пароль не указан, используется "123456"
3. **Переназначение**: При переназначении существующего пользователя его данные сохраняются, меняется только роль
4. **Права куратора**: В данный момент права куратора еще не определены полностью (см. ROLES_AND_PERMISSIONS.md)

## 🚀 Быстрый старт

1. **Создать куратора:**
   ```bash
   cd backend
   npx tsx scripts/createCurator.ts "Тест" "Куратор" "+998901234569"
   ```

2. **Проверить список:**
   ```bash
   npx tsx scripts/createCurator.ts --list
   ```

3. **Переназначить студента:**
   ```bash
   npx tsx scripts/createCurator.ts --phone "+998947771170"
   ```

## 📝 Следующие шаги

После создания куратора можно:
- Назначить куратора студентам (функционал в разработке)
- Настроить права доступа для кураторов
- Создать интерфейс управления кураторами в админ-панели


