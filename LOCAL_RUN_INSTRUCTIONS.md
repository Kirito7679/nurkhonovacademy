# 🚀 Инструкция по запуску локально

## Статус

✅ **Backend запущен** на порту 5001
✅ **Frontend запущен** на порту 5173

## ⚠️ Проблема с базой данных

Ошибка доступа к базе данных. Нужно:

### Вариант 1: Использовать Railway базу данных

Если у вас есть доступ к Railway базе данных, обновите `DATABASE_URL` в `backend/.env`:

```bash
DATABASE_URL="postgresql://user:password@host:port/database"
```

### Вариант 2: Запустить локальную PostgreSQL

1. Установите PostgreSQL (если не установлен)
2. Создайте базу данных:
```bash
createdb nurkhonov_academy
```

3. Обновите `DATABASE_URL` в `backend/.env`:
```bash
DATABASE_URL="postgresql://user:password@localhost:5432/nurkhonov_academy"
```

4. Примените миграцию:
```bash
cd backend
npx prisma db push
```

### Вариант 3: Использовать SQLite для разработки

Измените `backend/prisma/schema.prisma`:

```prisma
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}
```

Затем:
```bash
cd backend
npx prisma db push
```

## 🌐 Доступ к приложению

После решения проблемы с БД:

1. **Frontend**: http://localhost:5173
2. **Backend API**: http://localhost:5001/api
3. **Health check**: http://localhost:5001/api/health

## 📝 Тестирование функциональности классов

1. Откройте http://localhost:5173
2. Войдите как учитель
3. Перейдите в меню "Групповые классы"
4. Создайте новый класс
5. Добавьте студентов

## ✅ Что работает

- ✅ Backend сервер запущен
- ✅ Frontend сервер запущен
- ✅ API роуты зарегистрированы
- ⚠️ Нужно применить миграцию БД

## 🔧 Команды для проверки

```bash
# Проверить backend
curl http://localhost:5001/api/health

# Проверить frontend
curl http://localhost:5173

# Применить миграцию (после настройки БД)
cd backend
npx prisma db push
npx prisma generate
```

