# 🔧 Исправление 404 ошибки для /login

## ⚠️ Проблема

При переходе на `https://academy.dilmurodnurkhonov.uz/login` получаете 404 ошибку.

**Причина:** Файл `.htaccess` не работает или не загружен на сервер.

---

## ✅ Решение

### Шаг 1: Проверьте наличие .htaccess файла

В File Manager cPanel проверьте, есть ли файл `.htaccess` в папке `academy.dilmurodnurkhonov.uz/`.

**Важно:** Файл должен называться именно `.htaccess` (с точкой в начале), а не `htaccess`.

### Шаг 2: Если файла нет - загрузите его

**Способ 1: Через FTP**

1. Используйте FTP клиент (FileZilla, Cyberduck)
2. Подключитесь к серверу
3. Перейдите в папку `academy.dilmurodnurkhonov.uz/`
4. Загрузите файл `.htaccess` из папки `frontend/dist/`

**Способ 2: Создать через Terminal (если доступен)**

1. Откройте Terminal в cPanel
2. Выполните:
   ```bash
   cd academy.dilmurodnurkhonov.uz
   nano .htaccess
   ```
3. Вставьте содержимое:
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```
4. Сохраните (Ctrl+O, Enter, Ctrl+X)

**Способ 3: Создать через File Manager с другим именем**

1. Создайте файл `htaccess` (без точки)
2. Добавьте содержимое
3. Через Terminal переименуйте:
   ```bash
   mv htaccess .htaccess
   ```

### Шаг 3: Проверьте содержимое .htaccess

Файл должен содержать:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

### Шаг 4: Проверьте права доступа

Файл `.htaccess` должен иметь права `644`:
```bash
chmod 644 .htaccess
```

### Шаг 5: Проверьте, что mod_rewrite включен

В cPanel обычно mod_rewrite включен по умолчанию. Если не работает, обратитесь в поддержку хостинга.

---

## ✅ После исправления

1. Очистите кеш браузера (Ctrl+Shift+R)
2. Попробуйте открыть: `https://academy.dilmurodnurkhonov.uz/login`
3. Должно работать!

---

## 🆘 Если все еще не работает

1. **Проверьте логи Apache** в cPanel
2. **Проверьте, что файл называется именно `.htaccess`** (с точкой)
3. **Проверьте права доступа** к файлу
4. **Убедитесь, что mod_rewrite включен** на сервере

---

**После загрузки .htaccess файла все должно заработать! 🚀**

