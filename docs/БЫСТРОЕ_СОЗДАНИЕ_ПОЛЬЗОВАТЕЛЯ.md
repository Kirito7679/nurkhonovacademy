# ⚡ Быстрое создание пользователя на Railway

## 🚀 Самый простой способ (без установки CLI)

### Вариант 1: Через npx (рекомендуется)

Выполните команды в терминале на вашем компьютере:

```bash
# 1. Перейдите в папку backend
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/backend"

# 2. Войдите в Railway
npx @railway/cli login

# 3. Подключитесь к проекту
npx @railway/cli link

# 4. Создайте пользователя
npx @railway/cli run npx tsx scripts/createUser.ts "Дилмурод" "Нурхонов" "+998901234567" "ваш-пароль" "ADMIN"
```

**Замените:**
- `"Дилмурод"` - ваше имя
- `"Нурхонов"` - ваша фамилия  
- `"+998901234567"` - ваш номер телефона
- `"ваш-пароль"` - ваш пароль

---

### Вариант 2: Через регистрацию на сайте

1. **Откройте:** `https://academy.dilmurodnurkhonov.uz/register`
2. **Зарегистрируйтесь** с вашими данными
3. **Затем назначьте себе роль ADMIN:**

   ```bash
   cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/backend"
   npx @railway/cli login
   npx @railway/cli link
   npx @railway/cli run npx tsx scripts/makeAdmin.ts +998901234567
   ```
   
   (замените `+998901234567` на ваш номер телефона)

---

## 📋 Пошаговая инструкция

### Шаг 1: Откройте терминал

На Mac: **Terminal** (находится в Applications → Utilities)

### Шаг 2: Перейдите в папку backend

```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/backend"
```

### Шаг 3: Войдите в Railway

```bash
npx @railway/cli login
```

Откроется браузер - разрешите доступ.

### Шаг 4: Подключитесь к проекту

```bash
npx @railway/cli link
```

Выберите ваш проект из списка.

### Шаг 5: Создайте пользователя

```bash
npx @railway/cli run npx tsx scripts/createUser.ts "Ваше Имя" "Ваша Фамилия" "+998901234567" "ваш-пароль" "ADMIN"
```

**Пример:**
```bash
npx @railway/cli run npx tsx scripts/createUser.ts "Дилмурод" "Нурхонов" "+998901234567" "mypassword123" "ADMIN"
```

### Шаг 6: Проверьте результат

Должно показать:
```
✅ Пользователь успешно создан!
Имя: Дилмурод Нурхонов
Телефон: +998901234567
Роль: ADMIN
```

---

## ✅ После создания

1. Откройте: `https://academy.dilmurodnurkhonov.uz/login`
2. Войдите с созданными данными
3. Должно работать!

---

## 🆘 Если что-то не работает

### Проблема: "railway: command not found"

**Решение:** Используйте `npx @railway/cli` вместо `railway`

### Проблема: "Cannot find module"

**Решение:** Убедитесь, что вы в папке `backend`:
```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/backend"
```

### Проблема: "Project not found"

**Решение:** Выполните `npx @railway/cli link` и выберите проект из списка

---

**Выполните команды по порядку и создайте пользователя! 🚀**
