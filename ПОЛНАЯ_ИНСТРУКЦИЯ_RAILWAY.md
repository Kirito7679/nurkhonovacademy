# 🚂 Полная инструкция: Деплой на Railway с доменом academy.dilmurodnurkhonov.uz

## ✅ Да, можно использовать ваш домен!

Вы можете разместить API на Railway и использовать поддомен `api.academy.dilmurodnurkhonov.uz`

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка кода для Railway

1. **Создайте GitHub репозиторий** (если еще нет)
2. **Загрузите туда папку `backend/`**
3. **Убедитесь, что есть файлы:**
   - `package.json`
   - `railway.json` (уже создан)
   - `prisma/schema.prisma`
   - `prisma/migrations/`

### Шаг 2: Регистрация на Railway

1. Откройте: https://railway.app
2. Нажмите "Start a New Project"
3. Выберите "Deploy from GitHub repo"
4. Подключите ваш GitHub аккаунт
5. Выберите репозиторий с бэкендом
6. Выберите папку `backend/` (если весь проект в репозитории)

### Шаг 3: Настройка переменных окружения

В Railway:

1. Откройте ваш проект
2. Перейдите в "Variables"
3. Добавьте переменные:

```
DATABASE_URL = file:./prisma/dev.db
JWT_SECRET = ваш-секретный-ключ-минимум-32-символа
NODE_ENV = production
PORT = 5001
```

**Важно:** Railway автоматически назначит порт, но можно указать свой.

### Шаг 4: Настройка кастомного домена

1. **В Railway:**
   - Откройте ваш проект
   - Перейдите в "Settings" → "Networking"
   - Нажмите "Custom Domain"
   - Введите: `api.academy.dilmurodnurkhonov.uz`
   - Railway покажет DNS записи

2. **В панели управления доменом** (где вы управляете dilmurodnurkhonov.uz):

   **Вариант A: Через DNS записи**
   - Найдите раздел "DNS Management" или "Управление DNS"
   - Добавьте новую запись:
     ```
     Тип: CNAME
     Имя: api.academy (или просто api)
     Значение: [значение от Railway, например: xxxxxx.up.railway.app]
     TTL: 3600
     ```

   **Вариант B: Через поддомены**
   - Создайте поддомен `api` для `academy.dilmurodnurkhonov.uz`
   - Настройте CNAME на значение от Railway

3. **Дождитесь распространения DNS** (5-30 минут)

### Шаг 5: Применить миграции базы данных

После деплоя Railway автоматически запустит приложение, но нужно применить миграции:

1. **В Railway:**
   - Откройте "Deployments"
   - Найдите последний деплой
   - Откройте "View Logs"
   - Или используйте "Railway CLI" для выполнения команд

2. **Через Railway CLI** (если установлен):
   ```bash
   railway login
   railway link
   railway run npx prisma migrate deploy
   ```

3. **Или добавьте в package.json:**
   ```json
   "scripts": {
     "postinstall": "npx prisma generate",
     "postbuild": "npx prisma migrate deploy"
   }
   ```

### Шаг 6: Обновить фронтенд

1. **На вашем компьютере:**
   ```bash
   cd frontend
   echo "VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api" > .env.production
   npm run build:skip-check
   ```

2. **Загрузите обновленный фронтенд** на текущий хостинг:
   - Замените файлы в `public_html/academy/`
   - Обновите `index.html` и папку `assets/`

### Шаг 7: Проверка

1. **Проверьте API:**
   ```
   https://api.academy.dilmurodnurkhonov.uz/api/health
   ```
   Должно вернуть: `{"success": true, "message": "Server is running"}`

2. **Проверьте сайт:**
   ```
   https://academy.dilmurodnurkhonov.uz
   ```
   Должен работать и подключаться к API

---

## 🔧 Альтернатива: Vercel

Если Railway не подходит, можно использовать Vercel:

### Настройка Vercel:

1. **Создайте `vercel.json` в папке backend:**
   ```json
   {
     "version": 2,
     "builds": [
       {
         "src": "dist/app.js",
         "use": "@vercel/node"
       }
     ],
     "routes": [
       {
         "src": "/api/(.*)",
         "dest": "dist/app.js"
       }
     ]
   }
   ```

2. **Деплой:**
   - Подключите GitHub репозиторий
   - Vercel автоматически определит настройки
   - Добавьте кастомный домен в настройках

---

## 📝 Структура после настройки

```
academy.dilmurodnurkhonov.uz        → Фронтенд (текущий хостинг)
  ├── index.html
  ├── assets/
  └── .htaccess

api.academy.dilmurodnurkhonov.uz    → API (Railway/Vercel)
  └── /api/*
      ├── /auth
      ├── /courses
      ├── /students
      └── ...
```

---

## ✅ Преимущества этого подхода

- ✅ Бесплатно (Railway дает бесплатный тариф)
- ✅ Автоматический деплой из GitHub
- ✅ Используете свой домен
- ✅ Легко обновлять (просто push в GitHub)
- ✅ Автоматическое масштабирование
- ✅ Логи и мониторинг

---

## 🆘 Решение проблем

### Проблема: DNS не работает
- ✅ Проверьте правильность CNAME записи
- ✅ Дождитесь распространения DNS (может занять до 24 часов)
- ✅ Используйте `dig api.academy.dilmurodnurkhonov.uz` для проверки

### Проблема: API не запускается
- ✅ Проверьте логи в Railway
- ✅ Убедитесь, что переменные окружения настроены
- ✅ Проверьте, что `dist/app.js` существует после сборки

### Проблема: Миграции не применяются
- ✅ Добавьте `postbuild` скрипт в package.json
- ✅ Или выполните вручную через Railway CLI

---

**Готово! Теперь у вас API на Railway с вашим доменом! 🎉**
