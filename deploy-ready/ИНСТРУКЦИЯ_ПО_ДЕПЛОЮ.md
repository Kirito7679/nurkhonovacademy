# Инструкция по деплою Frontend на cPanel

## 📋 Подготовка

✅ Фронтенд собран и готов к деплою  
✅ Все файлы находятся в папке `deploy-ready/`  
✅ Включены все последние улучшения (уведомления, тесты, безопасность)

---

## 🚀 Шаги для деплоя

### 1. Подготовка на сервере

1. Войдите в **cPanel**
2. Откройте **File Manager**
3. Перейдите в папку вашего домена/поддомена (например: `public_html/academy`)

### 2. Очистка старых файлов

⚠️ **ВАЖНО:** Удалите старые файлы из папки `assets/`:

1. В File Manager перейдите в папку `assets/`
2. Выделите **ВСЕ** файлы (Ctrl+A или Cmd+A)
3. Нажмите **Delete** (удалить)
4. Подтвердите удаление

**Почему это важно:**
- Старые файлы с другими хешами могут вызывать конфликты
- Новые файлы имеют другие имена (с новыми хешами)
- Браузер может кешировать старые версии

### 3. Загрузка новых файлов

#### Вариант A: Через File Manager (для небольших файлов)

1. В File Manager нажмите **Upload**
2. Загрузите файлы из папки `deploy-ready/`:
   - `index.html` → в корень домена
   - `assets/` → все файлы из папки `assets/` в папку `assets/` на сервере
   - `.htaccess` → в корень домена (если его нет или нужно обновить)

#### Вариант B: Через FTP (рекомендуется для больших файлов)

1. Подключитесь через FTP клиент (FileZilla, WinSCP и т.д.)
2. Загрузите все файлы из `deploy-ready/`:
   - `index.html` → корень домена
   - Все файлы из `assets/` → в папку `assets/` на сервере
   - `.htaccess` → корень домена

### 4. Проверка прав доступа

Убедитесь, что файлы имеют правильные права:
- Файлы: `644` (rw-r--r--)
- Папки: `755` (rwxr-xr-x)

В File Manager: Выделите файлы → Permissions → Установите права

---

## ✅ Проверка после деплоя

### 1. Проверка загрузки страницы

1. Откройте сайт в браузере
2. Нажмите **F12** (открыть DevTools)
3. Перейдите на вкладку **Console**
4. Проверьте, нет ли ошибок загрузки файлов

### 2. Проверка уведомлений

1. Откройте платформу в двух браузерах:
   - Браузер 1: Учитель
   - Браузер 2: Студент
2. Учитель одобряет доступ студента к курсу
3. **Результат:** Студент должен **мгновенно** получить уведомление 🔔

### 3. Проверка создания тестов

1. Войдите как учитель
2. Перейдите в создание теста
3. Нажмите "Добавить вопрос"
4. **Результат:** Должно открыться красивое модальное окно

### 4. Очистка кеша

После загрузки:
- Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
- Или откройте в режиме инкогнито

---

## 📁 Структура файлов на сервере

```
public_html/academy/          (или ваш путь)
├── index.html               ← Главный файл
├── .htaccess                ← Конфигурация Apache
└── assets/                  ← Все JS и CSS файлы
    ├── index-CSuZTKD-.js
    ├── vendor-react-DpKlTPJd.js
    ├── index-rNDbRQqO.css
    ├── IntermediateTestForm-CDdbc3Ex.js  ← Новый улучшенный интерфейс
    ├── useSocket-*.js                     ← Обновленные уведомления
    └── ... (остальные файлы)
```

---

## ⚠️ Частые проблемы

### Проблема: Страница не загружается / 404 ошибка

**Решение:**
1. Проверьте, что файл `.htaccess` загружен в корень
2. Проверьте права доступа к файлам (644 для файлов, 755 для папок)
3. Проверьте, что путь к файлам правильный

### Проблема: Старые файлы все еще используются

**Решение:**
1. Удалите **ВСЕ** файлы из папки `assets/` на сервере
2. Загрузите новые файлы заново
3. Очистите кеш браузера (Ctrl+Shift+R)

### Проблема: Уведомления не работают

**Решение:**
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что backend работает и socket.io доступен
3. Проверьте, что файл `useSocket-*.js` загружен

---

## 📝 Чеклист перед загрузкой

- [ ] Фронтенд собран (`npm run build` выполнен успешно)
- [ ] Все файлы находятся в `deploy-ready/`
- [ ] Проверен файл `index.html` (правильные пути к assets)
- [ ] Файл `.htaccess` готов
- [ ] Готовы к удалению старых файлов из `assets/`

---

## 📝 Чеклист после загрузки

- [ ] Старые файлы из `assets/` удалены
- [ ] Новые файлы загружены
- [ ] `.htaccess` загружен в корень
- [ ] Права доступа установлены (644/755)
- [ ] Страница открывается без ошибок
- [ ] Уведомления работают в реальном времени
- [ ] Создание тестов работает

---

## 🎉 Готово!

После выполнения всех шагов ваша платформа будет обновлена со всеми улучшениями:
- ✅ Мгновенные уведомления
- ✅ Улучшенный интерфейс тестов
- ✅ Исправления безопасности

**Дата сборки:** 2024-12-16  
**Версия:** С улучшениями уведомлений и интерфейса
