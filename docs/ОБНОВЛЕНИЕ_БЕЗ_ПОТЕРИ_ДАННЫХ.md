# 🔄 Безопасное обновление без потери данных

## ✅ Краткий ответ

**Ваши данные НЕ исчезнут!** Курсы, студенты, доступы - все останется на месте.

## 📊 Что происходит при обновлении

### 1. Обновление фронтенда (React)

**Что обновляется:**
- HTML, CSS, JavaScript файлы
- Интерфейс и функции

**Что НЕ затрагивается:**
- ✅ База данных (остается нетронутой)
- ✅ Все курсы, студенты, доступы сохраняются
- ✅ Все данные остаются на месте

**Процесс:**
```bash
# Просто загружаете новые файлы из frontend/dist
# Старые файлы заменяются новыми
# База данных не трогается вообще
```

### 2. Обновление бэкенда (Node.js)

**Что обновляется:**
- Код сервера (логика, контроллеры, роуты)
- Структура базы данных (если были изменения в schema.prisma)

**Что сохраняется:**
- ✅ Все данные в базе данных
- ✅ Все курсы, студенты, уроки
- ✅ Все доступы и прогресс студентов
- ✅ Все комментарии и уведомления

**Процесс:**
```bash
# 1. Загружаете новый код
# 2. Запускаете миграции (если были изменения схемы)
# 3. Prisma применяет только НОВЫЕ изменения
# 4. Старые данные остаются на месте
```

## 🔐 Как работают миграции Prisma

### Что такое миграции?

Миграции - это способ безопасно изменять структуру базы данных **без потери данных**.

### Пример:

**Было:**
```prisma
model StudentCourse {
  id        String
  studentId String
  courseId  String
  status    String
}
```

**Стало (добавили поле):**
```prisma
model StudentCourse {
  id            String
  studentId     String
  courseId      String
  status        String
  accessEndDate DateTime?  // ← НОВОЕ поле
}
```

**Что делает миграция:**
1. ✅ Добавляет новое поле `accessEndDate`
2. ✅ Старые записи получают `NULL` в этом поле
3. ✅ Все существующие данные сохраняются
4. ✅ Ничего не удаляется

### Типы изменений:

| Изменение | Что происходит с данными |
|-----------|--------------------------|
| Добавление поля | ✅ Данные сохраняются, новое поле пустое или с дефолтом |
| Удаление поля | ⚠️ Поле удаляется, но данные в других полях сохраняются |
| Изменение типа | ⚠️ Может потребоваться преобразование данных |
| Добавление таблицы | ✅ Существующие таблицы не затрагиваются |
| Удаление таблицы | ❌ Данные удаляются (но это делается только вручную) |

## 📋 Правильный процесс обновления

### Шаг 1: Резервное копирование (рекомендуется)

```bash
# На сервере создайте копию базы данных
cp prisma/dev.db prisma/dev.db.backup-$(date +%Y%m%d)
```

### Шаг 2: Обновление фронтенда

```bash
# На вашем компьютере
cd frontend
npm install  # если добавили новые зависимости
npm run build

# Загрузите новые файлы из frontend/dist на сервер
# Замените старые файлы новыми
```

**Результат:** Интерфейс обновится, данные останутся.

### Шаг 3: Обновление бэкенда

```bash
# На вашем компьютере
cd backend
npm install  # если добавили новые зависимости
npm run build

# Загрузите на сервер:
# - backend/dist/ (новый код)
# - backend/prisma/ (новые миграции, если были)
# - backend/package.json (если изменились зависимости)
```

### Шаг 4: Применение миграций (если были изменения схемы)

```bash
# На сервере
cd backend
npx prisma migrate deploy  # Применяет только НОВЫЕ миграции
npx prisma generate         # Обновляет Prisma Client
```

**Важно:** `prisma migrate deploy` применяет только те миграции, которые еще не были применены. Старые данные не трогаются.

### Шаг 5: Перезапуск сервера

```bash
# Перезапустите Node.js приложение
# (зависит от того, как вы его запускаете)
```

## ⚠️ Когда данные МОГУТ быть потеряны

Данные могут быть потеряны **ТОЛЬКО** если вы:

1. ❌ Удалите файл базы данных вручную
2. ❌ Запустите `prisma migrate reset` (это удаляет все данные!)
3. ❌ Удалите таблицы вручную через SQL
4. ❌ Перезапишете базу данных старой версией

## ✅ Что гарантирует сохранность данных

1. **База данных хранится отдельно от кода**
   - Файл `prisma/dev.db` (SQLite) или отдельная БД (PostgreSQL)
   - Код и данные разделены

2. **Миграции применяются инкрементально**
   - Каждая миграция добавляет изменения
   - Старые данные не удаляются

3. **Prisma отслеживает примененные миграции**
   - В таблице `_prisma_migrations` хранится история
   - Не применяет миграции дважды

## 🔍 Проверка перед обновлением

### 1. Проверьте, есть ли изменения в схеме:

```bash
cd backend
npx prisma migrate status
```

Если видите "Database schema is up to date" - изменений нет, данные точно не пострадают.

### 2. Посмотрите, какие миграции будут применены:

```bash
npx prisma migrate deploy --dry-run
```

Покажет, что будет изменено, без применения.

## 📝 Чеклист безопасного обновления

- [ ] Создана резервная копия базы данных
- [ ] Проверены новые миграции (`migrate status`)
- [ ] Обновлен фронтенд (загружены новые файлы)
- [ ] Обновлен бэкенд (загружен новый код)
- [ ] Применены миграции (`migrate deploy`)
- [ ] Обновлен Prisma Client (`prisma generate`)
- [ ] Перезапущен сервер
- [ ] Проверена работа сайта

## 🎯 Примеры сценариев

### Сценарий 1: Обновление только интерфейса

**Что изменилось:** Добавили новую кнопку, изменили стили

**Действия:**
```bash
cd frontend
npm run build
# Загрузить frontend/dist на сервер
```

**Результат:** ✅ Интерфейс обновился, данные не тронуты

### Сценарий 2: Добавили новую функцию (без изменения БД)

**Что изменилось:** Добавили новую функцию в бэкенде

**Действия:**
```bash
cd backend
npm run build
# Загрузить backend/dist на сервер
# Перезапустить сервер
```

**Результат:** ✅ Новая функция работает, данные не тронуты

### Сценарий 3: Добавили новое поле в базу данных

**Что изменилось:** Добавили поле `accessEndDate` в `StudentCourse`

**Действия:**
```bash
# На компьютере создали миграцию
cd backend
npx prisma migrate dev --name add_access_dates

# Загрузили на сервер:
# - backend/dist/
# - backend/prisma/migrations/ (новая миграция)

# На сервере применили миграцию
cd backend
npx prisma migrate deploy
npx prisma generate
```

**Результат:** ✅ Новое поле добавлено, старые данные сохранены

## 💡 Рекомендации

1. **Всегда делайте резервную копию** перед крупными обновлениями
2. **Тестируйте миграции локально** перед применением на продакшене
3. **Используйте `migrate deploy`** на продакшене (не `migrate dev`)
4. **Проверяйте статус миграций** перед обновлением
5. **Храните базу данных отдельно** от кода (лучше использовать PostgreSQL на продакшене)

## 🆘 Если что-то пошло не так

1. **Остановите обновление**
2. **Восстановите из резервной копии**
3. **Проверьте логи миграций**
4. **Обратитесь за помощью**

## 📞 Итог

**Ваши данные в безопасности!** 

- Обновление фронтенда не затрагивает БД
- Обновление бэкенда сохраняет данные
- Миграции применяются безопасно
- Данные хранятся отдельно от кода

Просто следуйте правильному процессу обновления, и все будет хорошо! 🎉
