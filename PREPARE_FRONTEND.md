# 📦 Подготовка Frontend для деплоя

## ✅ Backend готов!
Все изменения закоммичены и отправлены на GitHub. Railway автоматически пересоберет backend.

---

## 🟢 Подготовка Frontend

### Шаг 1: Проверка настроек

Убедитесь, что файл `.env.production` содержит правильный API URL:

```env
VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api
```

### Шаг 2: Сборка проекта

```bash
# Перейдите в папку frontend
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/frontend"

# Установите зависимости (если нужно)
npm install

# Соберите проект для production
npm run build
```

После сборки все файлы будут в папке `frontend/dist/`

### Шаг 3: Проверка сборки

```bash
# Проверьте, что папка dist/ создана и содержит файлы
ls -la dist/

# Должны быть:
# - index.html
# - assets/ (папка с JS, CSS и другими файлами)
# - .htaccess (если есть)
```

---

## 📤 Загрузка на cPanel

### Вариант 1: Через File Manager (рекомендуется)

1. **Войдите в cPanel:**
   - Откройте ваш cPanel
   - Найдите "File Manager"

2. **Перейдите в public_html:**
   - Откройте папку `public_html` (или корневую папку вашего сайта)

3. **Сделайте бэкап (опционально, но рекомендуется):**
   - Выделите все файлы
   - Нажмите "Compress" → создайте архив

4. **Удалите старые файлы:**
   - ⚠️ ВНИМАНИЕ: Убедитесь, что у вас есть бэкап!
   - Выделите все файлы (кроме важных, например `.htaccess`, если хотите сохранить)
   - Нажмите "Delete"

5. **Загрузите новые файлы:**
   - Нажмите "Upload"
   - Выберите ВСЕ файлы из папки `frontend/dist/`
   - Загрузите их в `public_html`
   - Убедитесь, что `index.html` находится в корне

6. **Проверьте .htaccess:**
   - Убедитесь, что файл `.htaccess` существует в корне
   - Если нет, скопируйте его из `frontend/.htaccess`

### Вариант 2: Через FTP

1. **Подключитесь через FTP клиент** (FileZilla, Cyberduck и т.д.)

2. **Перейдите в public_html**

3. **Удалите старые файлы** (сделайте бэкап!)

4. **Загрузите все файлы из `frontend/dist/`**

---

## ✅ Проверка после деплоя

1. **Откройте сайт:**
   - https://academy.dilmurodnurkhonov.uz

2. **Проверьте консоль браузера (F12):**
   - Нет ошибок загрузки
   - API запросы идут на правильный URL
   - Нет 404 или других ошибок

3. **Проверьте основные функции:**
   - Регистрация/Вход
   - Просмотр курсов
   - Отправка сообщений
   - И т.д.

---

## 🐛 Решение проблем

### Сайт не загружается

1. Проверьте, что `index.html` находится в корне `public_html`
2. Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)
3. Проверьте `.htaccess` файл

### API запросы не работают

1. Проверьте, что `VITE_API_URL` правильный в `.env.production`
2. Пересоберите проект: `npm run build`
3. Проверьте консоль браузера на ошибки CORS

### Стили не загружаются

1. Проверьте, что папка `assets/` загружена
2. Проверьте пути к файлам в `index.html`
3. Очистите кэш браузера (Ctrl+Shift+R)

---

## 📝 Чеклист

- [ ] `.env.production` создан с правильным API URL
- [ ] Проект собран (`npm run build`)
- [ ] Папка `dist/` содержит все файлы
- [ ] Бэкап старых файлов сделан
- [ ] Старые файлы удалены из `public_html`
- [ ] Новые файлы загружены в `public_html`
- [ ] `.htaccess` файл на месте
- [ ] Сайт открывается в браузере
- [ ] Консоль браузера не показывает ошибок
- [ ] Основные функции работают

---

**Готово!** После выполнения всех шагов frontend должен работать в онлайн режиме. 🎉
