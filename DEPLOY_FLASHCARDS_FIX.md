# 🚀 Инструкция по деплою исправлений для Flashcards и Integrations

## ✅ Что было исправлено

1. **Добавлена недостающая связь в Prisma схему:**
   - Добавлена связь `progress FlashcardProgress[]` в модель `Flashcard`
   - Добавлена связь `card Flashcard` в модель `FlashcardProgress`
   - Добавлен индекс для `cardId` в `FlashcardProgress`

2. **Исправлен контроллер `getFlashcardsToReview`:**
   - Обновлен запрос для правильной работы с отношениями Prisma
   - Теперь корректно фильтрует карточки для повторения

3. **Обновлен Prisma Client:**
   - Сгенерированы новые типы с учетом изменений схемы

## 📋 Файлы, которые были изменены

- `backend/prisma/schema.prisma` - добавлены связи между моделями
- `backend/src/controllers/flashcardController.ts` - исправлен запрос для получения карточек

## 🔄 Процесс деплоя на Railway

### Вариант 1: Автоматический деплой через GitHub (рекомендуется)

Если у вас настроен автоматический деплой через GitHub:

1. **Закоммитьте изменения:**
   ```bash
   cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"
   git add backend/prisma/schema.prisma backend/src/controllers/flashcardController.ts
   git commit -m "fix: Add FlashcardProgress relation and fix flashcards controller"
   git push origin main
   ```

2. **Railway автоматически:**
   - Запустит `npm install`
   - Запустит `npm run build`
   - Запустит `npx prisma generate`
   - Запустит `npx prisma db push` (применит изменения схемы к БД)
   - Перезапустит сервер

3. **Проверьте деплой:**
   - Откройте Railway Dashboard
   - Проверьте логи деплоя
   - Убедитесь, что нет ошибок

### Вариант 2: Ручной деплой через Railway CLI

Если нужно деплоить вручную:

```bash
# 1. Установите Railway CLI (если еще не установлен)
npm install -g @railway/cli

# 2. Войдите в Railway
railway login

# 3. Перейдите в папку backend
cd backend

# 4. Свяжите проект с Railway (если еще не связано)
railway link

# 5. Деплой
railway up
```

### Вариант 3: Через Railway Dashboard

1. Откройте [Railway Dashboard](https://railway.app)
2. Выберите ваш проект
3. Нажмите "Deploy" или "Redeploy"
4. Railway автоматически применит изменения

## 🔍 Проверка после деплоя

После успешного деплоя проверьте:

1. **Проверьте API endpoints:**
   ```bash
   # Проверка flashcards endpoint
   curl https://api.academy.dilmurodnurkhonov.uz/api/flashcards \
     -H "Authorization: Bearer YOUR_TOKEN"
   
   # Проверка integrations endpoint
   curl https://api.academy.dilmurodnurkhonov.uz/api/integrations \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

2. **Проверьте в браузере:**
   - Откройте DevTools (F12)
   - Перейдите на страницу с flashcards или integrations
   - Проверьте Network tab - не должно быть 404 ошибок

3. **Проверьте логи Railway:**
   - В Railway Dashboard откройте "Logs"
   - Убедитесь, что нет ошибок при старте сервера
   - Проверьте, что `prisma db push` выполнился успешно

## ⚠️ Важные замечания

1. **База данных:**
   - Railway автоматически применит изменения схемы через `prisma db push`
   - Это безопасная операция, которая добавит только новые связи и индексы
   - Существующие данные не будут затронуты

2. **Время деплоя:**
   - Обычно занимает 2-5 минут
   - Сервер может быть недоступен на 10-30 секунд во время перезапуска

3. **Откат (если что-то пошло не так):**
   - В Railway Dashboard можно откатить к предыдущей версии
   - Или использовать `git revert` и задеплоить предыдущий коммит

## 🐛 Решение проблем

### Если все еще получаете 404:

1. **Проверьте, что изменения задеплоены:**
   ```bash
   # Проверьте последний коммит в Railway
   # Убедитесь, что он содержит изменения в schema.prisma
   ```

2. **Проверьте логи Railway:**
   - Убедитесь, что `prisma db push` выполнился без ошибок
   - Проверьте, что сервер запустился успешно

3. **Проверьте, что routes зарегистрированы:**
   - В `backend/src/app.ts` должны быть строки:
     ```typescript
     app.use('/api/flashcards', flashcardRoutes);
     app.use('/api/integrations', integrationRoutes);
     ```

### Если есть ошибки в логах:

- **Prisma ошибки:** Проверьте, что `DATABASE_URL` правильно настроен в Railway
- **TypeScript ошибки:** Убедитесь, что `npm run build` прошел успешно
- **Runtime ошибки:** Проверьте логи сервера в Railway Dashboard

## ✅ Чеклист перед деплоем

- [ ] Изменения закоммичены в git
- [ ] Локально `npm run build` проходит без ошибок (игнорируя ошибки в других файлах)
- [ ] `npx prisma generate` выполнен успешно
- [ ] Railway проект настроен и связан с репозиторием
- [ ] Переменные окружения в Railway настроены правильно

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи Railway
2. Проверьте, что все файлы закоммичены
3. Убедитесь, что Railway имеет доступ к репозиторию

---

**Готово к деплою!** 🚀

После деплоя endpoints `/api/flashcards` и `/api/integrations` должны работать корректно.
