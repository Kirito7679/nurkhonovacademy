# 🗄️ Применение миграций базы данных на Railway

## ⚠️ Проблема

В логах видно ошибку:
```
The table 'main.users' does not exist in the current database.
```

Это означает, что миграции Prisma не были применены к базе данных на Railway.

---

## ✅ Решение: Применить миграции

### Способ 1: Через Railway CLI (рекомендуется)

1. **Установите Railway CLI** (если еще не установлен):
   ```bash
   npm install -g @railway/cli
   ```

2. **Войдите в Railway:**
   ```bash
   railway login
   ```

3. **Подключитесь к проекту:**
   ```bash
   cd backend
   railway link
   ```
   Выберите ваш проект из списка.

4. **Примените миграции:**
   ```bash
   railway run npx prisma migrate deploy
   ```

5. **Проверьте результат:**
   Должно показать что-то вроде:
   ```
   Applying migration `20251210094933_init`
   Applying migration `20251210105625_add_avatar_url`
   ...
   ```

---

### Способ 2: Через Railway Dashboard

1. Откройте ваш проект в Railway
2. Перейдите в **"Deployments"**
3. Найдите последний деплой
4. Нажмите **"View Logs"**
5. Найдите кнопку **"Run Command"** или **"Shell"**
6. Выполните команду:
   ```bash
   npx prisma migrate deploy
   ```

---

### Способ 3: Добавить в package.json (автоматически)

Добавьте скрипт для автоматического применения миграций после сборки:

В `backend/package.json` уже есть:
```json
"postbuild": "npx prisma generate"
```

Можно изменить на:
```json
"postbuild": "npx prisma generate && npx prisma migrate deploy"
```

Но лучше использовать отдельный скрипт для деплоя.

---

## 🔧 Исправление Trust Proxy

Также нужно исправить проблему с `trust proxy`. Это уже исправлено в коде - добавлено:
```typescript
app.set('trust proxy', true);
```

После применения изменений Railway автоматически пересоберет проект.

---

## ✅ После применения миграций

1. **Проверьте логи** в Railway - не должно быть ошибок про отсутствующие таблицы
2. **Попробуйте войти** на сайте - должно работать
3. **Проверьте API:**
   ```
   https://api.academy.dilmurodnurkhonov.uz/api/health
   ```

---

## 🆘 Если миграции не применяются

1. **Проверьте переменную окружения DATABASE_URL:**
   - В Railway → Variables
   - Должно быть: `file:./prisma/dev.db`

2. **Проверьте права доступа:**
   - Railway должен иметь права на создание файла базы данных

3. **Попробуйте вручную:**
   ```bash
   railway run npx prisma migrate deploy --skip-generate
   ```

---

**После применения миграций все должно заработать! 🚀**

