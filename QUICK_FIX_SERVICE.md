# ⚡ БЫСТРОЕ ВОССТАНОВЛЕНИЕ СЕРВИСА

## 🎯 Проблема:
Сервис в Railway удален → все запросы возвращают 404

## 🚀 РЕШЕНИЕ (3 шага):

### 1. Railway Dashboard → New → GitHub Repo
   - Репозиторий: `Kirito7679/nurkhonovacademy`
   - **Root Directory: `backend`** ⚠️ ВАЖНО!

### 2. Настройте переменные:
   - `JWT_SECRET` = ваш ключ
   - `DATABASE_URL` = автоматически (если БД подключена)

### 3. Дождитесь деплоя:
   - Проверьте логи
   - Должна быть строка: `Server is running on port...`

## ✅ Проверка:
```bash
curl https://api.academy.dilmurodnurkhonov.uz/api/health
```

**Готово!** После этого все роуты заработают. 🎉
