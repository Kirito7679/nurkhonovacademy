# 👤 Создание пользователя на Railway

## ⚠️ Важно!

База данных на Railway **пустая** - это новая база данных. Все пользователи из локальной базы данных **не переносятся автоматически**.

---

## ✅ Решение: Создать пользователя на Railway

### Способ 1: Через регистрацию на сайте (самый простой)

1. Откройте: `https://academy.dilmurodnurkhonov.uz/register`
2. Зарегистрируйтесь с вашими данными
3. После регистрации назначьте себе роль ADMIN (см. Способ 2)

---

### Способ 2: Через скрипт (рекомендуется для админа)

#### Шаг 1: Создать пользователя через Railway CLI

1. **Установите Railway CLI** (если еще не установлен):
   ```bash
   npm install -g @railway/cli
   ```

2. **Войдите в Railway:**
   ```bash
   railway login
   ```

3. **Подключитесь к проекту:**
   ```bash
   cd backend
   railway link
   ```
   Выберите ваш проект из списка.

4. **Создайте пользователя:**
   ```bash
   railway run npx tsx scripts/createUser.ts "Ваше Имя" "Ваша Фамилия" "+998901234567" "ваш-пароль" "ADMIN"
   ```

   **Пример:**
   ```bash
   railway run npx tsx scripts/createUser.ts "Дилмурод" "Нурхонов" "+998901234567" "mypassword" "ADMIN"
   ```

5. **Проверьте результат:**
   Должно показать:
   ```
   ✅ Пользователь успешно создан!
   Имя: Дилмурод Нурхонов
   Телефон: +998901234567
   Роль: ADMIN
   ```

---

### Способ 3: Через Railway Dashboard

1. Откройте проект в Railway
2. Перейдите в **Deployments**
3. Найдите последний деплой
4. Откройте **View Logs**
5. Найдите кнопку **"Run Command"** или **"Shell"**
6. Выполните:
   ```bash
   npx tsx scripts/createUser.ts "Ваше Имя" "Ваша Фамилия" "+998901234567" "ваш-пароль" "ADMIN"
   ```

---

## 🔄 Перенос данных из локальной базы

Если у вас есть пользователи в локальной базе данных и вы хотите их перенести:

### Шаг 1: Экспорт из локальной базы

На вашем компьютере:

```bash
cd backend
npx tsx scripts/exportUsers.ts
```

(Нужно создать скрипт экспорта)

### Шаг 2: Импорт на Railway

```bash
railway run npx tsx scripts/importUsers.ts
```

---

## 📝 Быстрое создание админа

**Самый быстрый способ:**

1. Зарегистрируйтесь на сайте: `https://academy.dilmurodnurkhonov.uz/register`
2. Затем через Railway CLI назначьте себе роль ADMIN:
   ```bash
   railway run npx tsx scripts/makeAdmin.ts +998901234567
   ```
   (замените на ваш номер телефона)

---

## ✅ После создания пользователя

1. Войдите на сайте: `https://academy.dilmurodnurkhonov.uz/login`
2. Используйте созданные данные для входа
3. Если роль ADMIN - у вас будут все права

---

## 🆘 Если не работает

1. **Проверьте, что миграции применены:**
   ```bash
   railway run npx prisma migrate deploy
   ```

2. **Проверьте переменные окружения в Railway:**
   - `DATABASE_URL=file:./prisma/dev.db`
   - `JWT_SECRET=ваш-секретный-ключ`

3. **Проверьте логи в Railway:**
   - Deployments → View Logs

---

**После создания пользователя вы сможете войти! 🚀**
