# Инструкция по развертыванию на cPanel

## 📋 Подготовка

1. **Создайте резервную копию** текущего сайта (если он уже работает)
2. Убедитесь, что у вас есть доступ к cPanel и File Manager

## 📤 Загрузка файлов

### Вариант 1: Через File Manager в cPanel

1. Войдите в cPanel
2. Откройте **File Manager**
3. Перейдите в папку `public_html` (или в подпапку, где находится ваш сайт)
4. **ВАЖНО**: Если у вас уже есть файлы, сначала создайте резервную копию
5. Загрузите все файлы из папки `deploy-ready`:
   - `index.html`
   - Папку `assets/` со всеми файлами
   - Файл `.htaccess`

### Вариант 2: Через FTP/SFTP

1. Используйте FTP-клиент (FileZilla, WinSCP и т.д.)
2. Подключитесь к серверу через FTP
3. Перейдите в папку `public_html` (или нужную подпапку)
4. Загрузите все файлы из `deploy-ready`

## ⚙️ Настройка .htaccess

Файл `.htaccess` уже настроен и должен работать на большинстве хостингов с Apache.

**Проверьте:**
- Файл `.htaccess` должен быть в корневой папке сайта (там же, где `index.html`)
- Убедитесь, что в cPanel включен модуль `mod_rewrite`

**Если сайт не работает:**
1. В cPanel найдите раздел **Apache Modules** или **Select PHP Version**
2. Убедитесь, что включен `mod_rewrite`
3. Если модуль недоступен, обратитесь в поддержку хостинга

## 🔧 Настройка путей (если нужно)

Если ваш сайт находится в подпапке (например, `/academy/`), обновите `.htaccess`:

```apache
RewriteBase /academy/
```

## ✅ Проверка после загрузки

1. Откройте сайт в браузере
2. Проверьте, что главная страница загружается
3. Попробуйте перейти на другие страницы (например, `/dashboard`)
4. Проверьте консоль браузера (F12) на наличие ошибок

## 🐛 Решение проблем

### Проблема: Страницы возвращают 404

**Решение:**
- Убедитесь, что файл `.htaccess` загружен
- Проверьте, что `mod_rewrite` включен
- Попробуйте переименовать `.htaccess` в `.htaccess.txt`, затем обратно в `.htaccess`

### Проблема: Файлы не загружаются (CSS/JS)

**Решение:**
- Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)
- Убедитесь, что папка `assets/` загружена полностью
- Проверьте пути в `index.html`

### Проблема: Сайт показывает старую версию

**Решение:**
- Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
- Очистите кеш CDN (если используется)
- Проверьте настройки кеширования в cPanel

## 📝 Структура файлов

После загрузки структура должна быть такой:

```
public_html/
├── index.html
├── .htaccess
└── assets/
    ├── index-*.js
    ├── index-*.css
    ├── vendor-react-*.js
    └── [другие файлы]
```

## 🔐 Безопасность

- Убедитесь, что файл `.htaccess` не доступен для чтения извне
- Проверьте права доступа: файлы - 644, папки - 755
- Не загружайте файлы с расширениями `.env`, `.git` и т.д.

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи ошибок в cPanel (Error Log)
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы загружены полностью

## 🎮 Новые функции в этой версии

### Система геймификации
- **Коины**: Студенты получают 5 коинов за каждый завершенный урок
- **Отображение коинов**: Коины отображаются в header для студентов (кликабельный элемент, ведет на страницу лидеров)
- **Таблица лидеров**: Новая страница `/leaderboard` с рейтингом студентов по количеству коинов
- **Автоматическое обновление**: Коины автоматически обновляются после завершения урока

### Технические изменения
- Добавлен API endpoint `/api/leaderboard` для получения данных таблицы лидеров
- Обновлен endpoint `/api/auth/login` - теперь возвращает поле `coins`
- Обновлен endpoint `/api/lessons/:id/progress` - возвращает обновленные коины после завершения урока

---

**Дата обновления:** 2024-12-19
**Версия:** Последняя версия с системой геймификации (коины и таблица лидеров)


