# 🔧 Исправление ошибки сборки: tsc not found

## 🔍 Проблема:

Ошибка при сборке на Railway:
```
sh: 1: tsc: not found
Build Failed: build daemon returned an error
```

## 🔎 Причина:

В production режиме (`NODE_ENV=production`) npm не устанавливает dev зависимости, включая TypeScript. Но для сборки TypeScript необходим.

## ✅ Решение:

1. **Использование `npx tsc` вместо `tsc`:**
   - Изменено в `backend/package.json`: `"build": "npx tsc"`
   - `npx` найдет TypeScript в node_modules даже если он не в PATH

2. **Установка dev зависимостей для сборки:**
   - Обновлено `backend/nixpacks.toml`: `npm install --include=dev`
   - Обновлено `backend/railway.json`: `npm install --include=dev`
   - Это гарантирует установку TypeScript и других dev зависимостей для сборки

## 📝 Изменения:

### `backend/package.json`:
```json
"build": "npx tsc"  // было: "tsc"
```

### `backend/nixpacks.toml`:
```toml
cmds = ["npm install --include=dev", ...]  // было: "npm install"
```

### `backend/railway.json`:
```json
"buildCommand": "npm install --include=dev && ..."  // было: "npm install && ..."
```

## ✅ Статус:

- ✅ Изменения закоммичены
- ✅ Новый деплой запущен
- ⏳ Ожидание завершения сборки

После завершения деплоя сервер должен запуститься успешно.

---

**Примечание:** `--include=dev` флаг гарантирует установку dev зависимостей даже в production режиме, что необходимо для сборки TypeScript кода.
