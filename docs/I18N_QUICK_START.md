# Быстрый старт: Добавление переводов в компонент

## 📝 Шаг за шагом

### 1. Импортируйте `useTranslation`

```tsx
import { useTranslation } from 'react-i18next';
```

### 2. Используйте хук в компоненте

```tsx
function MyComponent() {
  const { t } = useTranslation();
  // ...
}
```

### 3. Замените тексты на `t('ключ')`

**До:**
```tsx
<h1>Курсы</h1>
<button>Сохранить</button>
<p>Нет курсов</p>
```

**После:**
```tsx
<h1>{t('courses.title')}</h1>
<button>{t('common.save')}</button>
<p>{t('courses.noCourses')}</p>
```

## 🎯 Примеры замены

### Кнопки и действия
```tsx
// Было:
<button>Сохранить</button>
<button>Отмена</button>
<button>Удалить</button>

// Стало:
<button>{t('common.save')}</button>
<button>{t('common.cancel')}</button>
<button>{t('common.delete')}</button>
```

### Заголовки
```tsx
// Было:
<h1>Курсы</h1>
<h2>Создать курс</h2>

// Стало:
<h1>{t('courses.title')}</h1>
<h2>{t('courses.createCourse')}</h2>
```

### Формы
```tsx
// Было:
<label>Название курса</label>
<input placeholder="Введите название" />

// Стало:
<label>{t('courses.courseTitle')}</label>
<input placeholder={t('courses.courseTitle')} />
```

### Сообщения
```tsx
// Было:
<p>Нет курсов</p>
<div>Загрузка...</div>

// Стало:
<p>{t('courses.noCourses')}</p>
<div>{t('common.loading')}</div>
```

## 📋 Чеклист для компонента

- [ ] Добавлен импорт `useTranslation`
- [ ] Добавлен `const { t } = useTranslation();`
- [ ] Все текстовые строки заменены на `t('ключ')`
- [ ] Проверено на всех трех языках (ru, en, uz)

## 🔍 Поиск ключей переводов

Все ключи находятся в:
- `frontend/src/i18n/locales/ru.json`
- `frontend/src/i18n/locales/en.json`
- `frontend/src/i18n/locales/uz.json`

Структура: `категория.ключ`

Примеры:
- `common.save` - общая кнопка "Сохранить"
- `courses.title` - заголовок "Курсы"
- `auth.login` - "Вход"
- `errors.required` - "Это поле обязательно"

## ⚠️ Если ключа нет

1. Добавьте ключ во все три файла переводов
2. Используйте осмысленное имя: `категория.описание`
3. Пример:
   ```json
   // ru.json
   "myFeature": {
     "title": "Моя функция"
   }
   
   // en.json
   "myFeature": {
     "title": "My Feature"
   }
   
   // uz.json
   "myFeature": {
     "title": "Mening funksiyam"
   }
   ```

## 🚀 Готово!

Теперь ваш компонент поддерживает все три языка автоматически!
