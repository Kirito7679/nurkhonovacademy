# Инструкция по обновлению Frontend для уведомлений

## ✅ Что было обновлено

### 1. Улучшена обработка socket-уведомлений
**Файл:** `frontend/src/hooks/useSocket.ts`
- Теперь при получении уведомления сразу выполняется `refetch` (не только `invalidate`)
- Уведомления появляются мгновенно без задержки

### 2. NotificationBell использует socket
**Файл:** `frontend/src/components/NotificationBell.tsx`
- Добавлен вызов `useSocket()` для подключения к socket
- Компонент теперь получает уведомления в реальном времени

---

## 🚀 Как обновить

### Вариант 1: Если используете git
```bash
cd frontend
git pull origin main
npm install  # если были изменения в зависимостях
npm run build  # для проверки
```

### Вариант 2: Ручное обновление

Обновите два файла:

#### 1. `frontend/src/hooks/useSocket.ts`
Найдите строку 47 и замените:
```typescript
// БЫЛО:
socket.on('new-notification', () => {
  queryClient.invalidateQueries('notifications');
});

// СТАЛО:
socket.on('new-notification', (notification) => {
  queryClient.invalidateQueries('notifications');
  queryClient.refetchQueries('notifications');
});
```

#### 2. `frontend/src/components/NotificationBell.tsx`
Добавьте после строки 15:
```typescript
// Use socket hook to listen for real-time notifications
useSocket();
```

---

## ✅ Проверка работы

1. Откройте платформу в двух браузерах:
   - Браузер 1: Учитель
   - Браузер 2: Студент

2. Учитель одобряет доступ студента к курсу

3. **Результат:** Студент должен **мгновенно** увидеть уведомление (без обновления страницы) 🔔

---

## 📝 Что изменилось в Backend

- Уведомления теперь отправляются через socket **после** коммита транзакции
- Это гарантирует, что уведомление отправляется только после успешного сохранения
- Улучшена надежность системы уведомлений

---

**Статус:** Готово к обновлению! ✅
