# Руководство по деплою на поддомен academy.dilmurodnukhonov.uz

## 📋 Шаг 1: Создание поддомена в панели управления хостингом

1. Войдите в панель управления вашего хостинга (cPanel, Plesk или другая)
2. Найдите раздел **"Поддомены"** (Subdomains) или **"Домены"** (Domains)
3. Создайте новый поддомен:
   - **Имя поддомена:** `academy`
   - **Домен:** `dilmurodnukhonov.uz`
   - **Полное имя:** `academy.dilmurodnukhonov.uz`
   - **Директория документа (Document Root):** Обычно автоматически создается как `public_html/academy` или `academy.dilmurodnukhonov.uz`
   - **Запишите путь к директории!** Он понадобится для загрузки файлов

## 📦 Шаг 2: Подготовка фронтенда для продакшена

### 2.1. Сборка фронтенда

Откройте терминал в папке проекта и выполните:

```bash
cd frontend
npm install  # если еще не установлены зависимости
npm run build
```

После сборки будет создана папка `frontend/dist` с готовыми файлами для продакшена.

### 2.2. Настройка API URL для продакшена

Создайте файл `.env.production` в папке `frontend`:

```bash
cd frontend
touch .env.production
```

Добавьте в файл `.env.production`:

```env
VITE_API_URL=https://academy.dilmurodnukhonov.uz/api
```

**Важно:** Замените URL на ваш реальный API endpoint, если бэкенд будет на другом домене/порту.

### 2.3. Пересборка с правильным API URL

```bash
npm run build
```

## 🖥️ Шаг 3: Подготовка бэкенда (если нужен)

Если ваш бэкенд будет работать на том же хостинге:

### 3.1. Сборка бэкенда

```bash
cd backend
npm install
npm run build
```

### 3.2. Настройка переменных окружения

Создайте файл `.env` в папке `backend`:

```env
DATABASE_URL="your_database_url"
JWT_SECRET="your_jwt_secret"
PORT=5001
NODE_ENV=production
```

### 3.3. Запуск миграций Prisma

```bash
npx prisma migrate deploy
npx prisma generate
```

## 📤 Шаг 4: Загрузка файлов через файловый менеджер

### 4.1. Подготовка файлов для загрузки

**Для фронтенда:**
- Скопируйте ВСЕ содержимое папки `frontend/dist` (не саму папку dist, а её содержимое)
- Это должны быть файлы: `index.html`, папка `assets` и другие файлы

**Для бэкенда (если нужен):**
- Загрузите папку `backend/dist`
- Загрузите папку `backend/prisma`
- Загрузите файл `backend/package.json`
- Загрузите файл `backend/.env` (с правильными настройками)

### 4.2. Загрузка через файловый менеджер

1. **Откройте файловый менеджер** в панели управления хостингом
2. **Найдите директорию поддомена:**
   - Обычно это `public_html/academy` или отдельная папка `academy.dilmurodnukhonov.uz`
   - Если не уверены, проверьте настройки поддомена в панели управления
3. **Загрузите файлы фронтенда:**
   - Перейдите в директорию поддомена
   - Используйте кнопку **"Загрузить"** (Upload) или перетащите файлы
   - Загрузите ВСЕ файлы из `frontend/dist`:
     - `index.html`
     - Папку `assets` (со всем содержимым)
     - Все остальные файлы и папки
4. **Структура должна выглядеть так:**
   ```
   academy/ (или public_html/academy/)
   ├── index.html
   ├── assets/
   │   ├── index-[hash].js
   │   ├── index-[hash].css
   │   └── ...
   └── ...
   ```

## ⚙️ Шаг 5: Настройка бэкенда на хостинге (если нужен)

Если ваш бэкенд будет работать на том же хостинге:

### 5.1. Загрузка бэкенда

1. Создайте отдельную папку для бэкенда (например, `backend-api` или `api`)
2. Загрузите туда файлы бэкенда

### 5.2. Настройка Node.js приложения

В панели управления хостингом:
1. Найдите раздел **"Node.js приложения"** или **"Node.js"**
2. Создайте новое приложение:
   - **Путь к файлу:** путь к `dist/app.js` в папке бэкенда
   - **Порт:** обычно автоматически назначается
   - **Переменные окружения:** добавьте переменные из `.env`
3. Запустите приложение

### 5.3. Настройка прокси для API

Если бэкенд работает на отдельном порту, настройте прокси в `.htaccess` или через панель управления:

**Создайте файл `.htaccess` в корне поддомена:**

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Прокси для API запросов
  RewriteCond %{REQUEST_URI} ^/api [NC]
  RewriteRule ^api/(.*)$ http://localhost:PORT/api/$1 [P,L]
  
  # SPA routing - все запросы на index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

**Или используйте настройки в панели управления хостинга для проксирования `/api` на ваш Node.js сервер.**

## 🔧 Шаг 6: Настройка роутинга для SPA

Создайте файл `.htaccess` в корне поддомена (если используете Apache):

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Если файл или директория существуют, используем их
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Иначе перенаправляем на index.html для SPA роутинга
  RewriteRule . /index.html [L]
</IfModule>
```

## ✅ Шаг 7: Проверка деплоя

1. Откройте в браузере: `https://academy.dilmurodnukhonov.uz`
2. Проверьте:
   - ✅ Страница загружается
   - ✅ Стили применяются
   - ✅ JavaScript работает
   - ✅ API запросы проходят (проверьте в DevTools → Network)
   - ✅ Роутинг работает (перейдите на другую страницу и обновите)

## 🐛 Решение проблем

### Проблема: Белая страница
- Проверьте, что `index.html` загружен в корне директории поддомена
- Проверьте консоль браузера на ошибки (F12)
- Убедитесь, что пути к файлам в `index.html` правильные

### Проблема: 404 при переходе на страницы
- Убедитесь, что `.htaccess` настроен для SPA роутинга
- Проверьте, что mod_rewrite включен на сервере

### Проблема: API запросы не работают
- Проверьте URL API в `.env.production`
- Убедитесь, что бэкенд запущен и доступен
- Проверьте CORS настройки на бэкенде
- Проверьте прокси настройки

### Проблема: Стили не применяются
- Проверьте пути к CSS файлам в `index.html`
- Убедитесь, что папка `assets` загружена полностью

## 📝 Дополнительные настройки

### SSL сертификат
Убедитесь, что SSL сертификат установлен для поддомена (обычно автоматически через Let's Encrypt в cPanel).

### База данных
Если используете базу данных:
1. Создайте БД через панель управления хостингом
2. Обновите `DATABASE_URL` в `.env` бэкенда
3. Запустите миграции Prisma

## 🚀 Быстрая команда для сборки и подготовки

```bash
# Фронтенд
cd frontend
npm install
npm run build

# Бэкенд (если нужен)
cd ../backend
npm install
npm run build
npx prisma generate
```

После этого файлы готовы в:
- `frontend/dist/` - для загрузки на хостинг
- `backend/dist/` - для запуска на сервере

