# 📦 Что загрузить на сайт - Полный чеклист

## 🎯 Краткий ответ

**Для фронтенда (обязательно):**
- ✅ Содержимое папки `frontend/dist/`
- ✅ Файл `frontend/.htaccess`

**Для бэкенда (если нужен API на том же сервере):**
- ✅ Папка `backend/dist/`
- ✅ Папка `backend/prisma/`
- ✅ Файл `backend/package.json`
- ✅ Файл `backend/.env` (с настройками)

---

## 📋 Детальный список

### 🌐 ФРОНТЕНД (React приложение)

#### Что загрузить:

1. **Содержимое папки `frontend/dist/`** (после сборки):
   ```
   frontend/dist/
   ├── index.html          ← Главный файл
   ├── assets/             ← Папка со стилями и скриптами
   │   ├── index-[hash].js
   │   ├── index-[hash].css
   │   └── ...
   └── ... (другие файлы если есть)
   ```

2. **Файл `frontend/.htaccess`**:
   ```
   frontend/.htaccess  ← Для правильной работы роутинга
   ```

#### Куда загрузить:

В директорию поддомена `academy.dilmurodnukhonov.uz`:
- Обычно это: `public_html/academy` или `academy.dilmurodnukhonov.uz`

#### Структура на сервере должна быть:

```
academy/ (или public_html/academy/)
├── .htaccess          ← Важно!
├── index.html
└── assets/
    ├── index-[hash].js
    ├── index-[hash].css
    └── ...
```

---

### 🖥️ БЭКЕНД (Node.js API) - если нужен на том же сервере

#### Что загрузить:

1. **Папка `backend/dist/`** (после сборки):
   ```
   backend/dist/
   ├── app.js           ← Главный файл сервера
   ├── controllers/
   ├── routes/
   ├── middleware/
   └── ... (другие файлы)
   ```

2. **Папка `backend/prisma/`**:
   ```
   backend/prisma/
   ├── schema.prisma    ← Схема базы данных
   ├── migrations/      ← Все миграции
   │   ├── 20251210094933_init/
   │   ├── 20251210105625_add_avatar_url/
   │   └── ...
   └── dev.db          ← База данных (если используете SQLite)
   ```

3. **Файл `backend/package.json`**:
   ```
   backend/package.json  ← Список зависимостей
   ```

4. **Файл `backend/.env`** (создайте на сервере):
   ```
   DATABASE_URL="file:./prisma/dev.db"  # или URL вашей БД
   JWT_SECRET="ваш_секретный_ключ"
   PORT=5001
   NODE_ENV=production
   ```

#### Куда загрузить:

В отдельную папку на сервере, например:
- `backend-api/` или `api/` или `node-backend/`

#### Структура на сервере должна быть:

```
backend-api/ (или другая папка)
├── dist/
│   ├── app.js
│   └── ...
├── prisma/
│   ├── schema.prisma
│   ├── migrations/
│   └── dev.db
├── package.json
└── .env
```

---

## 🚀 Пошаговая инструкция

### Шаг 1: Подготовка файлов на компьютере

```bash
# Перейдите в папку проекта
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"

# Соберите фронтенд
cd frontend
npm install
echo "VITE_API_URL=https://academy.dilmurodnukhonov.uz/api" > .env.production
npm run build

# Соберите бэкенд (если нужен)
cd ../backend
npm install
npm run build
npx prisma generate
```

**Готовые файлы будут в:**
- `frontend/dist/` - для фронтенда
- `backend/dist/` - для бэкенда

### Шаг 2: Загрузка фронтенда

1. Откройте файловый менеджер хостинга
2. Перейдите в директорию поддомена (`public_html/academy`)
3. Загрузите **ВСЁ содержимое** папки `frontend/dist/`:
   - `index.html`
   - Папку `assets/` (со всем содержимым)
   - Все остальные файлы
4. Загрузите файл `frontend/.htaccess` в корень

### Шаг 3: Загрузка бэкенда (если нужен)

1. Создайте папку для бэкенда (например, `backend-api`)
2. Загрузите туда:
   - Папку `backend/dist/`
   - Папку `backend/prisma/`
   - Файл `backend/package.json`
3. Создайте файл `.env` с настройками

### Шаг 4: Настройка бэкенда на сервере

```bash
# На сервере в папке бэкенда
cd backend-api
npm install --production
npx prisma migrate deploy
npx prisma generate
```

### Шаг 5: Запуск бэкенда

В панели управления хостингом:
1. Найдите раздел "Node.js приложения"
2. Создайте новое приложение:
   - Путь к файлу: `backend-api/dist/app.js`
   - Порт: обычно автоматически
   - Переменные окружения: из `.env`
3. Запустите приложение

---

## ✅ Чеклист перед загрузкой

### Фронтенд:
- [ ] Выполнена команда `npm run build` в папке `frontend`
- [ ] Создан файл `frontend/.env.production` с правильным API URL
- [ ] Папка `frontend/dist/` содержит файлы
- [ ] Файл `frontend/.htaccess` готов

### Бэкенд (если нужен):
- [ ] Выполнена команда `npm run build` в папке `backend`
- [ ] Выполнена команда `npx prisma generate`
- [ ] Папка `backend/dist/` содержит файлы
- [ ] Папка `backend/prisma/` содержит все миграции
- [ ] Файл `backend/.env` настроен для продакшена

---

## 📁 Структура файлов для загрузки

### Минимальный набор (только фронтенд):

```
📦 Для загрузки:
├── index.html
├── .htaccess
└── assets/
    ├── index-[hash].js
    ├── index-[hash].css
    └── ...
```

### Полный набор (фронтенд + бэкенд):

```
📦 Фронтенд:
├── index.html
├── .htaccess
└── assets/

📦 Бэкенд:
├── dist/
│   └── app.js
├── prisma/
│   ├── schema.prisma
│   └── migrations/
├── package.json
└── .env
```

---

## ⚠️ Важные моменты

1. **НЕ загружайте:**
   - ❌ Папку `node_modules/`
   - ❌ Исходные файлы из `src/`
   - ❌ Файлы `.git/`
   - ❌ Файлы `.env` с локальными настройками
   - ❌ Папку `frontend/src/`

2. **Обязательно загрузите:**
   - ✅ `frontend/dist/` (после сборки)
   - ✅ `frontend/.htaccess`
   - ✅ Если нужен бэкенд: `backend/dist/`, `backend/prisma/`, `backend/package.json`

3. **Настройки:**
   - ✅ Создайте `.env.production` для фронтенда с правильным API URL
   - ✅ Создайте `.env` для бэкенда на сервере с правильными настройками

---

## 🎯 Быстрая команда

Используйте скрипт для автоматической подготовки:

```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"
./prepare-deploy.sh
```

После этого загрузите:
- `frontend/dist/` → в директорию поддомена
- `frontend/.htaccess` → в корень поддомена
- `backend/dist/`, `backend/prisma/`, `backend/package.json` → в папку бэкенда (если нужен)

---

## 📞 Что делать после загрузки

1. ✅ Проверьте сайт: `https://academy.dilmurodnukhonov.uz`
2. ✅ Проверьте API: `https://academy.dilmurodnukhonov.uz/api` (если бэкенд на том же домене)
3. ✅ Проверьте консоль браузера на ошибки (F12)
4. ✅ Проверьте работу функций

---

## 🆘 Если что-то не работает

1. Проверьте пути к файлам
2. Проверьте настройки `.env`
3. Проверьте консоль браузера (F12)
4. Проверьте логи сервера (если бэкенд)

---

**Готово! Теперь вы знаете точно, что загружать! 🚀**
