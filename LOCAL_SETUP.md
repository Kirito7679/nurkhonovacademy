# 💻 Настройка локальной версии платформы

Этот файл содержит инструкции для настройки и запуска платформы Nurkhonov Academy локально.

## 📋 Требования

- Node.js 18+
- npm или yarn
- PostgreSQL (для production) или SQLite (для разработки)

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"
```

### 2. Настройка Backend

```bash
# Перейдите в папку backend
cd backend

# Установите зависимости
npm install

# Создайте файл .env
cp .env.example .env

# Настройте переменные окружения в .env:
# DATABASE_URL="file:./dev.db"  # для SQLite (локально)
# JWT_SECRET="your-secret-key-here"
# PORT=5001

# Сгенерируйте Prisma Client
npx prisma generate

# Примените миграции (для SQLite)
npx prisma db push

# Запустите сервер
npm run dev
```

Backend будет доступен на `http://localhost:5001`

### 3. Настройка Frontend

```bash
# Откройте новый терминал
cd frontend

# Установите зависимости
npm install

# Создайте файл .env
echo "VITE_API_URL=http://localhost:5001/api" > .env

# Запустите dev сервер
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

---

## 🔧 Детальная настройка

### Backend конфигурация

#### Файл `.env` для локальной разработки:

```env
# Database
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Server
PORT=5001
NODE_ENV=development

# CORS (опционально)
CORS_ORIGIN="http://localhost:5173"
```

#### Prisma схема для локальной разработки:

В `backend/prisma/schema.prisma` для локальной разработки можно использовать SQLite:

```prisma
datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}
```

⚠️ **ВНИМАНИЕ:** Для production используйте PostgreSQL!

### Frontend конфигурация

#### Файл `.env` для локальной разработки:

```env
VITE_API_URL=http://localhost:5001/api
```

#### Файл `.env.production` для production:

```env
VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api
```

---

## 📦 Установка зависимостей

### Backend

```bash
cd backend
npm install
```

### Frontend

```bash
cd frontend
npm install
```

---

## 🗄️ Работа с базой данных

### SQLite (локальная разработка)

```bash
# Применить изменения схемы
npx prisma db push

# Открыть Prisma Studio (GUI для БД)
npm run prisma:studio
```

### PostgreSQL (production)

```bash
# Создать миграцию
npx prisma migrate dev --name migration_name

# Применить миграции
npx prisma migrate deploy

# Открыть Prisma Studio
npm run prisma:studio
```

---

## 🧪 Тестирование

### Backend тесты

```bash
cd backend
npm test
```

### Frontend тесты

```bash
cd frontend
npm test
```

---

## 🔍 Полезные команды

### Backend

```bash
# Запуск в dev режиме
npm run dev

# Сборка для production
npm run build

# Запуск production версии
npm start

# Prisma команды
npm run prisma:generate    # Генерация Prisma Client
npm run prisma:migrate      # Создать миграцию
npm run prisma:studio       # Открыть Prisma Studio
npm run clear-db            # Очистить базу данных
```

### Frontend

```bash
# Запуск в dev режиме
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview
```

---

## 🐛 Решение проблем

### Backend не запускается

1. **Проверьте, что порт 5001 свободен:**
```bash
lsof -i :5001
```

2. **Проверьте переменные окружения:**
```bash
cat backend/.env
```

3. **Проверьте, что Prisma Client сгенерирован:**
```bash
cd backend
npx prisma generate
```

### Frontend не подключается к API

1. **Проверьте, что backend запущен:**
   - Откройте http://localhost:5001/api/health

2. **Проверьте переменную окружения:**
```bash
cat frontend/.env
```

3. **Проверьте CORS настройки в backend:**
   - Убедитесь, что `http://localhost:5173` добавлен в CORS origins

### Проблемы с базой данных

1. **Очистите базу данных:**
```bash
cd backend
npm run clear-db
```

2. **Пересоздайте базу данных:**
```bash
rm backend/prisma/dev.db
npx prisma db push
```

---

## 📝 Структура проекта

```
Nurkhonov Academy/
├── backend/
│   ├── src/
│   │   ├── controllers/    # Контроллеры API
│   │   ├── routes/         # Роуты API
│   │   ├── middleware/     # Middleware
│   │   ├── services/       # Сервисы
│   │   ├── utils/          # Утилиты
│   │   └── app.ts          # Главный файл приложения
│   ├── prisma/
│   │   ├── schema.prisma   # Схема базы данных
│   │   └── migrations/     # Миграции
│   ├── scripts/            # Скрипты
│   └── package.json
│
└── frontend/
    ├── src/
    │   ├── components/     # React компоненты
    │   ├── pages/          # Страницы
    │   ├── services/       # API сервисы
    │   ├── store/          # State management
    │   ├── utils/          # Утилиты
    │   └── App.tsx         # Главный компонент
    ├── public/             # Статические файлы
    └── package.json
```

---

## ✅ Чеклист для локальной разработки

- [ ] Node.js 18+ установлен
- [ ] Зависимости установлены (backend и frontend)
- [ ] Файлы .env созданы и настроены
- [ ] Prisma Client сгенерирован
- [ ] База данных создана и миграции применены
- [ ] Backend запускается без ошибок
- [ ] Frontend запускается без ошибок
- [ ] API запросы работают
- [ ] Авторизация работает

---

## 🎯 Следующие шаги

После настройки локальной версии:

1. Создайте первого пользователя (админа)
2. Создайте тестовые данные
3. Протестируйте все функции
4. Подготовьте к деплою (см. `FULL_REDEPLOY_GUIDE.md`)

---

**Готово!** Локальная версия должна работать отлично. 🎉
