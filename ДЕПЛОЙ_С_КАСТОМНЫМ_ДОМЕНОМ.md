# 🌐 Деплой с кастомным доменом academy.dilmurodnurkhonov.uz

## ✅ Да, можно использовать Railway/Vercel с вашим доменом!

Вы можете:
- ✅ Разместить API на Railway/Vercel
- ✅ Использовать ваш домен `academy.dilmurodnurkhonov.uz`
- ✅ Настроить поддомен для API или использовать основной домен

---

## 🎯 Варианты настройки

### Вариант 1: API на поддомене (рекомендуется)

```
academy.dilmurodnurkhonov.uz        → Фронтенд (на текущем хостинге)
api.academy.dilmurodnurkhonov.uz    → API (на Railway/Vercel)
```

### Вариант 2: API на основном домене

```
academy.dilmurodnurkhonov.uz        → Фронтенд (на текущем хостинге)
academy.dilmurodnurkhonov.uz/api    → API (на Railway/Vercel через прокси)
```

---

## 🚀 Вариант 1: Railway + кастомный домен

### Шаг 1: Деплой на Railway

1. **Зарегистрируйтесь:** https://railway.app
2. **Создайте новый проект**
3. **Загрузите бэкенд:**
   - Через GitHub (создайте репозиторий с папкой `backend/`)
   - Или через Railway CLI
4. **Настройте переменные окружения:**
   ```
   DATABASE_URL=file:./prisma/dev.db
   JWT_SECRET=ваш-секретный-ключ
   NODE_ENV=production
   PORT=5001
   ```

### Шаг 2: Настроить кастомный домен

1. **В Railway:**
   - Откройте ваш проект
   - Перейдите в "Settings" → "Networking"
   - Нажмите "Custom Domain"
   - Введите: `api.academy.dilmurodnurkhonov.uz`
   - Railway покажет DNS записи для настройки

2. **В панели управления доменом:**
   - Создайте поддомен `api` для `academy.dilmurodnurkhonov.uz`
   - Или добавьте CNAME запись:
     ```
     Тип: CNAME
     Имя: api.academy
     Значение: [значение от Railway]
     ```

3. **Дождитесь распространения DNS** (5-30 минут)

### Шаг 3: Обновить фронтенд

```bash
cd frontend
echo "VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api" > .env.production
npm run build:skip-check
```

Загрузите обновленный фронтенд на текущий хостинг.

---

## 🚀 Вариант 2: Vercel + кастомный домен

### Шаг 1: Деплой на Vercel

1. **Зарегистрируйтесь:** https://vercel.com
2. **Создайте новый проект**
3. **Подключите GitHub репозиторий** с бэкендом
4. **Настройте:**
   - Framework Preset: Other
   - Root Directory: `backend`
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

### Шаг 2: Настроить переменные окружения

В Vercel:
- Settings → Environment Variables
- Добавьте все переменные из `.env`

### Шаг 3: Настроить кастомный домен

1. **В Vercel:**
   - Settings → Domains
   - Добавьте домен: `api.academy.dilmurodnurkhonov.uz`
   - Vercel покажет DNS записи

2. **В панели управления доменом:**
   - Добавьте CNAME запись как указано в Vercel

### Шаг 4: Обновить фронтенд

```bash
cd frontend
echo "VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api" > .env.production
npm run build:skip-check
```

---

## 📋 Настройка DNS записей

В панели управления доменом `dilmurodnurkhonov.uz`:

### Для Railway:

```
Тип: CNAME
Имя: api.academy (или просто api)
Значение: [значение от Railway, например: xxxxxx.up.railway.app]
TTL: 3600
```

### Для Vercel:

```
Тип: CNAME
Имя: api.academy
Значение: cname.vercel-dns.com
TTL: 3600
```

---

## 🔧 Альтернатива: Использовать основной домен

Если хотите использовать `academy.dilmurodnurkhonov.uz` для API:

1. **Настройте DNS:**
   ```
   Тип: A или CNAME
   Имя: academy
   Значение: [IP или домен от Railway/Vercel]
   ```

2. **Настройте прокси в .htaccess** на текущем хостинге:
   ```apache
   RewriteEngine On
   RewriteCond %{REQUEST_URI} ^/api [NC]
   RewriteRule ^api/(.*)$ https://api-academy.railway.app/api/$1 [P,L]
   ```

Но проще использовать поддомен `api.academy.dilmurodnurkhonov.uz`.

---

## ✅ Рекомендуемая структура

```
academy.dilmurodnurkhonov.uz        → Фронтенд (текущий хостинг)
api.academy.dilmurodnurkhonov.uz    → API (Railway/Vercel)
```

**Преимущества:**
- ✅ Четкое разделение фронтенда и API
- ✅ Легко настроить
- ✅ Легко обновлять независимо

---

## 📝 Чеклист

- [ ] Зарегистрировался на Railway/Vercel
- [ ] Загрузил бэкенд
- [ ] Настроил переменные окружения
- [ ] Добавил кастомный домен в Railway/Vercel
- [ ] Настроил DNS записи в панели домена
- [ ] Дождался распространения DNS
- [ ] Обновил фронтенд с новым URL API
- [ ] Загрузил обновленный фронтенд
- [ ] Проверил работу: `https://api.academy.dilmurodnurkhonov.uz/api/health`

---

**Готов помочь с настройкой любого из вариантов! 🚀**
