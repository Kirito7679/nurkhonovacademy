# 🔧 Решение проблемы: Сайт не обновляется

## ✅ Что проверено:

1. **API работает** - `https://api.academy.dilmurodnurkhonov.uz/api/health` отвечает ✅
2. **Frontend собран** - файлы в `frontend/dist/` свежие (сегодня) ✅
3. **Изменения НЕ закоммичены** - Railway не обновился автоматически ⚠️

---

## 🎯 Проблема: Изменения не закоммичены

**Все изменения находятся в рабочей директории, но не запушены в GitHub.**

Railway обновляется автоматически только когда вы делаете `git push`.

---

## ✅ Решение: Закоммитить и запушить изменения

### Шаг 1: Закоммить изменения

```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"

# Добавить все изменения
git add .

# Закоммитить
git commit -m "Phase 2: Replace system dialogs, improve typing, remove console.log, update frontend build"

# Запушить в GitHub
git push origin main
```

### Шаг 2: Railway обновится автоматически

После `git push` Railway:
- Обнаружит изменения
- Соберет проект
- Применит миграции (если есть)
- Перезапустит сервер

**Время обновления:** 2-5 минут

---

## 🌐 Обновление Frontend на cPanel

### Важно: Frontend обновляется вручную!

Railway обновляет только backend. Frontend нужно загружать на cPanel вручную.

### Шаг 1: Убедитесь, что сборка свежая

```bash
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy/frontend"

# Пересобрать (если нужно)
rm -rf dist
npm run build:skip-check
```

### Шаг 2: Загрузите на cPanel

1. **Войдите в cPanel**
2. **Откройте File Manager**
3. **Перейдите в директорию поддомена:**
   - `public_html/academy` или `academy.dilmurodnurkhonov.uz`
4. **Удалите старые файлы:**
   - Выберите все файлы в `assets/`
   - Удалите их
5. **Загрузите новые файлы:**
   - Загрузите `frontend/dist/index.html`
   - Загрузите всю папку `frontend/dist/assets/`
   - Загрузите `frontend/.htaccess` (обновленный)

### Шаг 3: Очистите кэш браузера

- **Windows/Linux:** `Ctrl + Shift + R`
- **Mac:** `Cmd + Shift + R`

Или откройте в режиме инкогнито.

---

## 🔍 Проверка после обновления

### 1. Проверьте Railway Dashboard:

1. Откройте: https://railway.app
2. Найдите ваш проект
3. Проверьте раздел **"Deployments"**
4. Должен быть новый деплой со статусом "Active"

### 2. Проверьте API:

```bash
curl https://api.academy.dilmurodnurkhonov.uz/api/health
```

Должен вернуть: `{"success":true,"message":"Server is running"}`

### 3. Проверьте Frontend:

1. Откройте: `https://academy.dilmurodnurkhonov.uz`
2. Откройте DevTools (F12) → Network
3. Обновите страницу (Ctrl+Shift+R)
4. Проверьте, что загружаются файлы с новыми именами:
   - `index-FK4ubfmL.js` (новый)
   - НЕ `index-BZjozkdM.js` (старый)

---

## ⚠️ Если все еще не работает

### Проблема 1: Railway не обновился

**Решение:**
1. Откройте Railway Dashboard
2. Перейдите в **"Deployments"**
3. Нажмите **"Redeploy"** на последнем деплое
4. Или создайте новый деплой через **"Deploy"** → **"Deploy Latest"**

### Проблема 2: Frontend не обновился

**Решение:**
1. Убедитесь, что удалили старые файлы из `assets/`
2. Убедитесь, что загрузили новые файлы
3. Очистите кэш браузера
4. Проверьте файл `.htaccess` - он должен быть в корне

### Проблема 3: Кэш браузера

**Решение:**
1. Откройте сайт в режиме инкогнито
2. Или очистите кэш полностью:
   - Chrome: Settings → Privacy → Clear browsing data
   - Firefox: Settings → Privacy → Clear Data
   - Safari: Develop → Empty Caches

---

## 📋 Чеклист

### Backend (Railway):
- [ ] Изменения закоммичены (`git commit`)
- [ ] Изменения запушены (`git push`)
- [ ] Railway деплой успешен
- [ ] API отвечает на запросы

### Frontend (cPanel):
- [ ] Сборка выполнена (`npm run build`)
- [ ] Старые файлы удалены с сервера
- [ ] Новые файлы загружены на сервер
- [ ] `.htaccess` обновлен
- [ ] Кэш браузера очищен

---

## 🚀 Быстрое решение (прямо сейчас)

```bash
# 1. Закоммитить и запушить изменения
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"
git add .
git commit -m "Phase 2: Update frontend and backend"
git push origin main

# 2. Подождите 2-5 минут пока Railway обновится

# 3. Загрузите frontend/dist/ на cPanel

# 4. Очистите кэш браузера (Ctrl+Shift+R)
```

---

## ✅ После выполнения всех шагов

1. **Railway обновится автоматически** (2-5 минут)
2. **Frontend обновится после загрузки файлов**
3. **Сайт заработает с новыми функциями**

**Готово! 🎉**

