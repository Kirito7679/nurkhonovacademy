# ✅ Проверка работы платформы

## 📋 Что нужно проверить:

### 1. Проверка API (Railway)

Откройте в браузере:
```
https://api.academy.dilmurodnurkhonov.uz/api/health
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "message": "Server is running"
}
```

**Если не работает:**
- Проверьте DNS записи (должна быть CNAME для `api.academy`)
- Подождите 5-30 минут для распространения DNS
- Проверьте логи в Railway (Deployments → View Logs)

---

### 2. Проверка фронтенда

Откройте в браузере:
```
https://academy.dilmurodnurkhonov.uz
```

**Что проверить:**
- ✅ Сайт открывается
- ✅ Нет ошибок в консоли браузера (F12)
- ✅ Запросы идут на `api.academy.dilmurodnurkhonov.uz`

**Если не работает:**
- Проверьте, что все файлы загружены в папку
- Проверьте наличие `.htaccess` файла
- Проверьте права доступа к файлам (644 для файлов, 755 для папок)

---

### 3. Проверка структуры файлов

В File Manager должны быть:

```
academy.dilmurodnurkhonov.uz/
├── index.html          ✅ (есть)
├── assets/             ✅ (есть)
│   ├── index-*.css
│   └── index-*.js
└── .htaccess           ⚠️ (нужно проверить)
```

---

### 4. Проверка .htaccess файла

**Важно:** Для правильной работы SPA роутинга нужен `.htaccess` файл.

**Проверьте:**
1. Есть ли файл `.htaccess` в папке `academy.dilmurodnurkhonov.uz`?
2. Если нет - создайте его с содержимым:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

---

### 5. Проверка DNS

Проверьте DNS запись для `api.academy`:

```bash
dig api.academy.dilmurodnurkhonov.uz
```

**Должно показать:**
```
api.academy.dilmurodnurkhonov.uz. CNAME 02odqour.up.railway.app.
```

---

## ✅ Чеклист проверки:

- [ ] API отвечает на `/api/health`
- [ ] Фронтенд открывается
- [ ] Нет ошибок в консоли браузера
- [ ] Файлы фронтенда загружены
- [ ] `.htaccess` файл создан
- [ ] DNS запись настроена правильно
- [ ] Можно войти в систему
- [ ] Можно просмотреть курсы

---

## 🆘 Если что-то не работает:

### Проблема: API не отвечает

**Решение:**
1. Проверьте DNS записи в Zone Editor
2. Убедитесь, что имя `api.academy` (без домена)
3. Проверьте логи в Railway

### Проблема: Фронтенд показывает ошибку

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Убедитесь, что запросы идут на правильный URL
4. Проверьте `.htaccess` файл

### Проблема: Страницы не открываются (404)

**Решение:**
1. Создайте `.htaccess` файл
2. Проверьте права доступа к файлам
3. Убедитесь, что `index.html` в корне папки

---

**Проверьте все пункты и сообщите, если что-то не работает! 🚀**

