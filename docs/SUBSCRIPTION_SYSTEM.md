# 💳 Система подписок с периодами

## Обзор

Реализована система подписок с поддержкой разных периодов (30 дней, 3 месяца, 6 месяцев, 1 год) и разными ценами для каждого периода.

---

## 🎯 Основные возможности

1. **Установка цен для разных периодов** при создании курса
2. **Выбор периода подписки** при одобрении доступа учителем
3. **Автоматическое истечение подписки** по истечении выбранного периода
4. **Продление подписки** студентом через оплату
5. **Уведомления** о истечении подписки

---

## 📋 Как использовать

### Для учителей: Создание курса с подпиской

1. При создании курса выберите **"Платный"** в поле "Тип подписки"
2. Заполните цены для нужных периодов:
   - **Цена за 30 дней** (например, 500,000 сум)
   - **Цена за 3 месяца** (например, 1,200,000 сум)
   - **Цена за 6 месяцев** (например, 2,000,000 сум)
   - **Цена за год** (например, 3,500,000 сум)
3. Сохраните курс

**Примечание:** Можно установить цены только для нужных периодов. Например, только 30 дней и 3 месяца.

---

### Для учителей: Одобрение доступа с периодом

При одобрении доступа студента к платному курсу:

1. Перейдите в раздел "Студенты"
2. Найдите студента с запросом доступа
3. При одобрении можно указать период подписки в запросе:
   ```json
   {
     "action": "approve",
     "subscriptionPeriod": "30_DAYS" // или "3_MONTHS", "6_MONTHS", "1_YEAR"
   }
   ```

Если период не указан, студент получит бессрочный доступ (legacy поведение).

---

### Для студентов: Продление подписки

1. На странице курса, если подписка истекла или скоро истечет, появится уведомление
2. Нажмите кнопку **"Продлить подписку"**
3. Выберите период подписки
4. Нажмите **"Продлить"**
5. Система обновит дату окончания подписки

**Примечание:** В текущей реализации оплата не обрабатывается автоматически. Нужно интегрировать платежную систему (см. раздел "Интеграция платежей").

---

## 🔧 Технические детали

### Поля в базе данных

```prisma
model Course {
  // ... другие поля
  subscriptionType String? // FREE, TRIAL, PAID
  price30Days      Float? // Цена за 30 дней
  price3Months     Float? // Цена за 3 месяца
  price6Months     Float? // Цена за 6 месяцев
  price1Year       Float? // Цена за год
}
```

### API Endpoints

#### Продление подписки (для студентов)
```
POST /api/courses/:courseId/extend
Body: {
  "subscriptionPeriod": "30_DAYS" | "3_MONTHS" | "6_MONTHS" | "1_YEAR"
}
```

#### Одобрение доступа с периодом (для учителей)
```
PUT /api/students/:studentId/courses/:courseId
Body: {
  "action": "approve",
  "subscriptionPeriod": "30_DAYS" | "3_MONTHS" | "6_MONTHS" | "1_YEAR" (опционально)
}
```

---

## 📊 Логика работы

### Установка даты окончания подписки

При одобрении доступа или продлении подписки:

- **30_DAYS**: +30 дней от текущей даты (или от даты окончания текущей подписки)
- **3_MONTHS**: +3 месяца
- **6_MONTHS**: +6 месяцев
- **1_YEAR**: +1 год

Если подписка еще активна, новый период добавляется к текущей дате окончания.
Если подписка истекла, новый период добавляется к текущей дате.

### Проверка доступа

Система автоматически проверяет:
- Есть ли `accessEndDate`
- Не истекла ли подписка (`accessEndDate < now`)
- Если подписка истекла, доступ блокируется

---

## 🔄 Миграция базы данных

Для применения изменений нужно выполнить миграцию:

```bash
cd backend
npx prisma migrate dev --name add_subscription_prices
```

Или использовать `prisma db push` для применения изменений без создания миграции:

```bash
cd backend
npx prisma db push
```

---

## 💰 Интеграция платежей

**Текущее состояние:** Продление подписки обновляет даты, но не обрабатывает оплату.

**Что нужно сделать:**

1. В функции `extendCourseSubscription` (backend/src/controllers/courseController.ts):
   - Создать запись о платеже
   - Интегрировать с платежной системой (Click, Payme, Uzum)
   - Обновлять подписку только после успешной оплаты

2. Пример интеграции:
```typescript
// Создать платеж
const payment = await createPayment({
  userId: studentId,
  courseId: courseId,
  amount: price,
  period: subscriptionPeriod,
});

// Обработать оплату через платежную систему
const paymentResult = await processPayment(payment);

// Только после успешной оплаты обновить подписку
if (paymentResult.success) {
  await updateSubscription(...);
}
```

---

## 🎨 UI компоненты

### ExtendSubscriptionModal

Компонент для продления подписки:
- Отображает доступные периоды с ценами
- Позволяет выбрать период
- Отправляет запрос на продление

**Использование:**
```tsx
<ExtendSubscriptionModal
  isOpen={showModal}
  onClose={() => setShowModal(false)}
  courseId={course.id}
  courseTitle={course.title}
  prices={{
    price30Days: course.price30Days,
    price3Months: course.price3Months,
    price6Months: course.price6Months,
    price1Year: course.price1Year,
  }}
  currentEndDate={course.accessEndDate}
/>
```

---

## 📝 Примеры использования

### Пример 1: Курс с подпиской на 30 дней и 3 месяца

**Создание курса:**
```
Тип подписки: Платный
Цена за 30 дней: 500,000 сум
Цена за 3 месяца: 1,200,000 сум
```

**Одобрение доступа:**
```json
{
  "action": "approve",
  "subscriptionPeriod": "30_DAYS"
}
```

Студент получит доступ на 30 дней.

---

### Пример 2: Продление подписки

1. Студент имеет подписку до 2025-02-01
2. Подписка истекает через 5 дней
3. Студент нажимает "Продлить подписку"
4. Выбирает период "3 месяца"
5. Новая дата окончания: 2025-05-01 (3 месяца от текущей даты окончания)

---

## ⚠️ Важные замечания

1. **Обратная совместимость:** Старые курсы без цен для периодов продолжат работать (бессрочный доступ при одобрении без периода)

2. **Платежи:** В текущей реализации оплата не обрабатывается. Нужно интегрировать платежную систему перед использованием в production.

3. **Уведомления:** Система создает уведомления при продлении подписки, но не уведомляет о скором истечении (можно добавить).

4. **Миграция:** Не забудьте выполнить миграцию базы данных после обновления кода.

---

## 🚀 Следующие шаги

1. ✅ Интеграция с платежными системами (Click, Payme, Uzum)
2. ✅ Автоматические уведомления о скором истечении подписки
3. ✅ История платежей и подписок
4. ✅ Статистика по подпискам для учителей
5. ✅ Автоматическое продление подписки (опционально)

---

**Дата создания:** 2025-01-27  
**Версия:** 1.0
