# 🛠️ Что можно сделать с Supabase CLI

## 📋 Основные возможности:

### 1. 🔐 Авторизация и управление проектами

#### Авторизация:
```bash
cd backend
npx supabase login
```
- Вход в Supabase аккаунт
- Сохранение токена для последующих команд

#### Связывание с проектом:
```bash
npx supabase link --project-ref czmkyqkibxrryjpjfdsb
```
- Связывает локальную директорию с вашим Supabase проектом
- Позволяет выполнять команды без указания project-ref каждый раз

#### Просмотр проектов:
```bash
npx supabase projects list
```
- Список всех ваших Supabase проектов
- Показывает ID, название, регион, статус

---

### 2. 📦 Управление Storage (файлами)

#### Просмотр bucket:
```bash
npx supabase storage ls
```
- Список всех bucket в проекте
- Показывает размер, количество файлов

#### Просмотр файлов в bucket:
```bash
npx supabase storage ls avatars
npx supabase storage ls lessons
```
- Список всех файлов в конкретном bucket
- Показывает размер, дату создания

#### Загрузка файла:
```bash
npx supabase storage cp ./local-file.jpg avatars/remote-file.jpg
```
- Загружает файл с локального компьютера в Supabase Storage
- Можно загружать фото профиля, файлы уроков, видео

#### Копирование файлов:
```bash
npx supabase storage cp avatars/file1.jpg avatars/file2.jpg
```
- Копирует файл внутри Storage
- Полезно для создания резервных копий

#### Перемещение файлов:
```bash
npx supabase storage mv avatars/old-file.jpg lessons/new-file.jpg
```
- Перемещает файл между bucket или внутри bucket

#### Удаление файла:
```bash
npx supabase storage rm avatars/file.jpg
```
- Удаляет файл из Storage
- Полезно для очистки старых файлов

#### Массовое удаление:
```bash
npx supabase storage rm avatars/ --recursive
```
- Удаляет все файлы в bucket (осторожно!)

---

### 3. 🗄️ Управление базой данных

#### Просмотр таблиц:
```bash
npx supabase db list
```
- Список всех таблиц в базе данных
- Показывает схему, размер

#### Выполнение SQL запросов:
```bash
npx supabase db execute "SELECT * FROM users LIMIT 10;"
```
- Выполняет SQL запросы напрямую
- Полезно для быстрых проверок данных

#### Создание миграций:
```bash
npx supabase migration new add_new_table
```
- Создает новый файл миграции
- Для изменений схемы базы данных

#### Применение миграций:
```bash
npx supabase db push
```
- Применяет все миграции к базе данных

#### Откат миграций:
```bash
npx supabase migration repair
```
- Восстанавливает состояние миграций

---

### 4. 🔑 Управление API ключами

#### Просмотр API ключей:
```bash
npx supabase projects api-keys --project-ref czmkyqkibxrryjpjfdsb
```
- Показывает все API ключи проекта
- anon key, service_role key

#### Создание нового ключа:
```bash
npx supabase projects api-keys create --project-ref czmkyqkibxrryjpjfdsb
```
- Создает новый API ключ
- Полезно для ротации ключей

---

### 5. 📊 Мониторинг и статус

#### Статус проекта:
```bash
npx supabase status
```
- Показывает статус всех сервисов
- База данных, Storage, API

#### Информация о проекте:
```bash
npx supabase projects get --project-ref czmkyqkibxrryjpjfdsb
```
- Детальная информация о проекте
- Регион, статус, лимиты

---

### 6. 🔧 Локальная разработка

#### Инициализация локального проекта:
```bash
npx supabase init
```
- Создает локальную копию Supabase
- Для разработки без интернета

#### Запуск локального Supabase:
```bash
npx supabase start
```
- Запускает все сервисы локально
- База данных, Storage, API

#### Остановка локального Supabase:
```bash
npx supabase stop
```
- Останавливает локальные сервисы

---

## 🎯 Практические примеры для вашего проекта:

### Проверка Storage bucket:
```bash
cd backend
npx supabase login
npx supabase link --project-ref czmkyqkibxrryjpjfdsb
npx supabase storage ls
```

### Загрузка тестового файла:
```bash
cd backend
npx supabase storage cp ./test-avatar.jpg avatars/test-avatar.jpg
```

### Просмотр всех файлов в bucket:
```bash
cd backend
npx supabase storage ls avatars
npx supabase storage ls lessons
```

### Проверка пользователей в базе:
```bash
cd backend
npx supabase db execute "SELECT id, phone, role FROM users LIMIT 10;"
```

### Очистка старых файлов:
```bash
cd backend
# Сначала посмотреть файлы
npx supabase storage ls avatars
# Затем удалить ненужные
npx supabase storage rm avatars/old-file.jpg
```

### Резервное копирование файлов:
```bash
cd backend
# Скачать все файлы из bucket (через API или вручную)
npx supabase storage ls avatars > avatars-backup-list.txt
```

---

## ⚠️ Важные команды:

### Безопасность:
- ⚠️ **Не коммитьте** API ключи в git
- ⚠️ **Не публикуйте** service_role key
- ✅ Используйте переменные окружения

### Ограничения:
- Некоторые операции требуют авторизации
- Некоторые команды работают только с linked проектом
- Storage bucket создаются через Dashboard, не через CLI

---

## 📝 Полезные команды для ежедневной работы:

```bash
# Быстрая проверка Storage
npx supabase storage ls

# Проверка количества файлов
npx supabase storage ls avatars | wc -l

# Проверка размера bucket (через Dashboard или API)

# Удаление тестовых файлов
npx supabase storage rm avatars/test-*.jpg
```

---

## ✅ Готово к использованию!

Теперь вы можете управлять Supabase проектом полностью через командную строку!
