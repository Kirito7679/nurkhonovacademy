# Инструкция по обновлению Frontend на cPanel

## 📋 Что нужно обновить

Нужно заменить **2 файла** через cPanel File Manager:

1. `frontend/src/hooks/useSocket.ts`
2. `frontend/src/components/NotificationBell.tsx`

---

## 🚀 Быстрый способ (рекомендуется)

### Готовые файлы находятся в папке `cpanel-update/`:

1. **`cpanel-update/useSocket.ts`** → заменить `frontend/src/hooks/useSocket.ts`
2. **`cpanel-update/NotificationBell.tsx`** → заменить `frontend/src/components/NotificationBell.tsx`

---

## 📝 Пошаговая инструкция для cPanel File Manager

### Шаг 1: Заменить `useSocket.ts`

1. Войдите в **cPanel**
2. Откройте **File Manager**
3. Перейдите в папку: `frontend/src/hooks/`
4. Найдите файл `useSocket.ts`
5. Нажмите **Edit** (редактировать)
6. **УДАЛИТЕ ВЕСЬ СОДЕРЖИМОЕ** файла
7. **СКОПИРУЙТЕ ВЕСЬ КОД** из файла `cpanel-update/useSocket.ts`
8. **ВСТАВЬТЕ** в редактор
9. Нажмите **Save Changes**

### Шаг 2: Заменить `NotificationBell.tsx`

1. В **File Manager** перейдите в папку: `frontend/src/components/`
2. Найдите файл `NotificationBell.tsx`
3. Нажмите **Edit**
4. **УДАЛИТЕ ВЕСЬ СОДЕРЖИМОЕ** файла
5. **СКОПИРУЙТЕ ВЕСЬ КОД** из файла `cpanel-update/NotificationBell.tsx`
6. **ВСТАВЬТЕ** в редактор
7. Нажмите **Save Changes**

---

## 🔧 Альтернативный способ (через FTP)

1. Подключитесь через FTP клиент (FileZilla, WinSCP и т.д.)
2. Скачайте готовые файлы из папки `cpanel-update/`:
   - `useSocket.ts`
   - `NotificationBell.tsx`
3. Загрузите их, заменив существующие файлы:
   - `useSocket.ts` → `frontend/src/hooks/useSocket.ts`
   - `NotificationBell.tsx` → `frontend/src/components/NotificationBell.tsx`

---

## ✅ Что изменилось

### В `useSocket.ts`:
- Добавлен `refetchQueries` для мгновенного обновления уведомлений
- Уведомления теперь появляются сразу, без задержки

### В `NotificationBell.tsx`:
- Добавлен вызов `useSocket()` для подключения к socket
- Компонент теперь получает уведомления в реальном времени

---

## ✅ Проверка

После обновления файлов:

1. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
2. Откройте платформу в двух браузерах:
   - Браузер 1: Учитель
   - Браузер 2: Студент
3. Учитель одобряет доступ студента к курсу
4. **Результат:** Студент должен **мгновенно** получить уведомление 🔔

---

## ⚠️ Важно

- Не забудьте сохранить изменения в обоих файлах
- После обновления может потребоваться перезагрузка страницы
- Если уведомления не работают, проверьте консоль браузера (F12) на наличие ошибок

---

**Готово!** После замены этих двух файлов уведомления будут работать в реальном времени. 🎉

---

## 📝 Пошаговая инструкция для cPanel

### Вариант 1: Через File Manager

1. Войдите в **cPanel**
2. Откройте **File Manager**
3. Перейдите в папку с проектом: `frontend/src/hooks/`
4. Найдите файл `useSocket.ts`
5. Нажмите **Edit** (редактировать)
6. Найдите строку 47 и замените код (см. выше)
7. Нажмите **Save Changes**
8. Перейдите в `frontend/src/components/`
9. Найдите файл `NotificationBell.tsx`
10. Нажмите **Edit**
11. Добавьте строку `useSocket();` после строки 15
12. Нажмите **Save Changes**

### Вариант 2: Через FTP

1. Подключитесь через FTP клиент (FileZilla, WinSCP и т.д.)
2. Скачайте файлы:
   - `frontend/src/hooks/useSocket.ts`
   - `frontend/src/components/NotificationBell.tsx`
3. Отредактируйте локально
4. Загрузите обратно (замените существующие файлы)

---

## ✅ Проверка

После обновления файлов:

1. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
2. Откройте платформу в двух браузерах:
   - Браузер 1: Учитель
   - Браузер 2: Студент
3. Учитель одобряет доступ студента к курсу
4. **Результат:** Студент должен мгновенно получить уведомление 🔔

---

## ⚠️ Важно

- Не забудьте сохранить изменения в обоих файлах
- После обновления может потребоваться перезагрузка страницы
- Если уведомления не работают, проверьте консоль браузера (F12) на наличие ошибок

---

**Готово!** После обновления этих двух файлов уведомления будут работать в реальном времени. 🎉
