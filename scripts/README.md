# Скрипты деплоя

Эта папка содержит скрипты для автоматизации процесса деплоя проекта.

## 📜 Доступные скрипты

### prepare-complete-package.sh
Подготавливает полный пакет для загрузки на сервер, включая все необходимые файлы frontend и backend.

**Использование:**
```bash
./scripts/prepare-complete-package.sh
```

### prepare-deploy.sh
Подготавливает проект для деплоя (сборка, минификация и т.д.).

**Использование:**
```bash
./scripts/prepare-deploy.sh
```

### prepare-upload-package.sh
Создает пакет для загрузки на хостинг (обычно для frontend).

**Использование:**
```bash
./scripts/prepare-upload-package.sh
```

### update-production.sh
Обновляет production окружение без потери данных.

**Использование:**
```bash
./scripts/update-production.sh
```

## ⚠️ Важно

Перед использованием скриптов убедитесь, что:
1. Все зависимости установлены (`npm install` в `frontend/` и `backend/`)
2. Переменные окружения настроены правильно
3. У вас есть права на выполнение скриптов (`chmod +x scripts/*.sh`)

## 📝 Примечания

- Скрипты могут требовать настройки под ваше окружение
- Проверьте пути и переменные в скриптах перед использованием
- Рекомендуется делать backup перед выполнением скриптов обновления
