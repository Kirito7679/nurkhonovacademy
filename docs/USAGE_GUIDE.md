# Руководство по использованию новых функций

## 🎯 Быстрый старт

### 1. Применение миграций

```bash
cd backend
npx prisma generate
# Для разработки:
npx prisma migrate dev
# Для production:
npx prisma migrate deploy
```

### 2. Запуск приложения

```bash
# Backend
cd backend
npm run dev

# Frontend
cd frontend
npm run dev
```

---

## 📚 Флеш-карточки

### Для студентов

1. Перейдите в раздел **"Флеш-карточки"** в меню
2. Выберите колоду для изучения
3. Нажмите **"Изучать"**
4. Просматривайте карточки, нажимая на них для переворота
5. Оценивайте сложность: Легко / Средне / Сложно
6. Система автоматически напомнит о повторении карточек

### Для учителей

1. Перейдите в **"Флеш-карточки"**
2. Нажмите **"Создать колоду"**
3. Заполните название и описание
4. Добавьте карточки (лицевая и обратная сторона)
5. Сохраните колоду

**API Endpoints:**
- `GET /api/flashcards` - список колод
- `POST /api/flashcards` - создать колоду
- `GET /api/flashcards/:id/review` - получить карточки для повторения
- `PUT /api/flashcards/:deckId/cards/:cardId/progress` - обновить прогресс

---

## ✍️ Практические упражнения

### Для студентов

1. Откройте урок с упражнениями
2. Нажмите **"Выполнить упражнение"**
3. Прочитайте инструкции
4. Введите ответ
5. Отправьте на проверку
6. Просмотрите результаты и обратную связь

### Для учителей

1. Откройте урок для редактирования
2. Перейдите на вкладку **"Практические упражнения"**
3. Нажмите **"Создать упражнение"**
4. Выберите тип: CODING, WRITING, SPEAKING, LISTENING
5. Укажите инструкции и решение (для автопроверки)
6. Установите лимит попыток
7. Сохраните упражнение

**API Endpoints:**
- `GET /api/practice/lessons/:lessonId` - получить упражнения
- `POST /api/practice/lessons/:lessonId` - создать упражнение
- `POST /api/practice/:exerciseId/submit` - отправить ответ
- `PUT /api/practice/results/:resultId/review` - проверить ответ

---

## 🎮 Интерактивные задания

### Создание интерактивных вопросов в тестах

1. Откройте урок → **"Тест к уроку"**
2. Добавьте вопрос
3. Выберите тип:
   - **Перетаскивание (Drag & Drop)**
   - **Сопоставление (Matching)**
   - **Заполнение пропусков (Fill Blank)**

### Формат метаданных

**DRAG_DROP:**
```json
{
  "items": ["Элемент 1", "Элемент 2", "Элемент 3"],
  "targets": ["Цель 1", "Цель 2", "Цель 3"],
  "correctMapping": {
    "Элемент 1": "Цель 1",
    "Элемент 2": "Цель 2",
    "Элемент 3": "Цель 3"
  }
}
```

**MATCHING:**
```json
{
  "leftItems": ["Слово 1", "Слово 2"],
  "rightItems": ["Word 1", "Word 2"],
  "correctPairs": [
    {"left": "Слово 1", "right": "Word 1"},
    {"left": "Слово 2", "right": "Word 2"}
  ]
}
```

**FILL_BLANK:**
```json
{
  "text": "The capital of France is ___",
  "blanks": [
    {"position": 3, "correctAnswer": "Paris"}
  ]
}
```

---

## 🔗 Внешние интеграции

### Добавление интеграции

1. Перейдите в **"Интеграции"**
2. Нажмите **"Добавить интеграцию"**
3. Выберите тип: Quizlet, Google Docs, YouTube
4. Введите внешний ID и URL
5. Сохраните

**Примеры:**

**Quizlet:**
- ID: `12345678`
- URL: `https://quizlet.com/12345678/vocabulary-set`

**Google Docs:**
- ID: `1a2b3c4d5e6f7g8h9i0j`
- URL: `https://docs.google.com/document/d/1a2b3c4d5e6f7g8h9i0j/edit`

**API Endpoints:**
- `GET /api/integrations` - список интеграций
- `POST /api/integrations` - создать интеграцию
- `DELETE /api/integrations/:id` - удалить интеграцию

---

## 📊 Логи активности

### Для администраторов и модераторов

1. Перейдите в **"Логи активности"** (в меню учителя)
2. Используйте фильтры:
   - Поиск по ID пользователя
   - Фильтр по действию (CREATE, UPDATE, DELETE, etc.)
   - Фильтр по типу сущности (COURSE, LESSON, etc.)
3. Просматривайте детальную информацию о действиях

**API Endpoints:**
- `GET /api/activity-logs` - получить логи (только админы/модераторы)
- `GET /api/activity-logs/user/:userId` - логи конкретного пользователя

---

## 🌍 Многоязычность

### Настройка языка

Язык определяется автоматически из настроек браузера или может быть установлен в профиле пользователя.

**Поддерживаемые языки:**
- `ru` - Русский (по умолчанию)
- `en` - English
- `uz` - O'zbek

**В будущем:** Добавление переключателя языка в интерфейсе

---

## 💾 Резервное копирование

### Создание бэкапа

```bash
cd backend
npm run backup
```

Бэкап будет создан в папке `backups/` с именем `backup-YYYY-MM-DDTHH-MM-SS.zip`

### Восстановление из бэкапа

```bash
cd backend
npm run restore <path-to-backup-file>
```

### Автоматическое резервное копирование

Добавьте в cron (Linux/Mac):
```bash
# Ежедневно в 2:00 ночи
0 2 * * * cd /path/to/backend && npm run backup
```

---

## 🔐 Система ролей

### Новые роли

- **MODERATOR** - Модератор
  - Может проверять упражнения
  - Может просматривать логи
  - Может создавать контент
  - Не может удалять курсы

- **ASSISTANT** - Ассистент
  - Может помогать учителям
  - Ограниченный доступ к функциям

### Назначение ролей

Только администраторы могут назначать роли через API или напрямую в базе данных.

---

## 📝 Примеры использования API

### Создание флеш-карточек

```javascript
const deck = await api.post('/api/flashcards', {
  title: 'Английские слова',
  description: 'Базовый словарь',
  isPublic: false,
  flashcards: [
    { front: 'Hello', back: 'Привет' },
    { front: 'Goodbye', back: 'До свидания' }
  ]
});
```

### Создание практического упражнения

```javascript
const exercise = await api.post('/api/practice/lessons/lesson-id', {
  title: 'Напишите эссе',
  type: 'WRITING',
  instructions: 'Напишите 200 слов о своем дне',
  autoCheck: false,
  maxAttempts: 3
});
```

### Создание интерактивного вопроса

```javascript
const question = {
  question: 'Сопоставьте слова',
  type: 'MATCHING',
  metadata: JSON.stringify({
    leftItems: ['Hello', 'Goodbye'],
    rightItems: ['Привет', 'До свидания'],
    correctPairs: [
      { left: 'Hello', right: 'Привет' },
      { left: 'Goodbye', right: 'До свидания' }
    ]
  }),
  correctAnswer: JSON.stringify({
    'Hello': 'Привет',
    'Goodbye': 'До свидания'
  })
};
```

---

## 🐛 Решение проблем

### Миграции не применяются

1. Убедитесь, что база данных существует
2. Проверьте `DATABASE_URL` в `.env`
3. Для SQLite: убедитесь, что файл `dev.db` существует
4. Попробуйте: `npx prisma migrate reset` (⚠️ удалит все данные)

### Ошибки при создании интерактивных заданий

1. Проверьте формат JSON в метаданных
2. Убедитесь, что все поля заполнены
3. Проверьте консоль браузера на ошибки

### Флеш-карточки не отображаются

1. Убедитесь, что колода создана
2. Проверьте, что в колоде есть карточки
3. Проверьте права доступа (публичная/приватная)

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Проверьте логи сервера
3. Убедитесь, что все миграции применены
4. Проверьте права доступа пользователя

---

## 🚀 Следующие шаги

После внедрения всех функций рекомендуется:

1. Протестировать все функции
2. Обучить пользователей работе с новыми функциями
3. Настроить автоматическое резервное копирование
4. Добавить больше переводов для i18n
5. Расширить функциональность по мере необходимости
