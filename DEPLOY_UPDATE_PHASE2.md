# 🚀 Инструкция по обновлению на Production (Этап 2)

## 📋 Что было обновлено

### ✅ Выполненные изменения:
1. **Заменены все системные диалоги** на кастомные модальные окна
2. **Улучшена типизация** - добавлены типы для ошибок API
3. **Убраны console.log** из production кода
4. **Удалена страница тестирования** (TestFeatures)
5. **Исправлены все ошибки линтера**

### 📁 Измененные файлы:
- Frontend: 20+ файлов обновлено
- Backend: без изменений (не требовалось)

---

## 🔄 Процесс обновления

### Шаг 1: Подготовка кода на локальной машине

```bash
# Перейдите в папку проекта
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"

# 1. Обновите зависимости (если нужно)
cd frontend
npm install

# 2. Создайте файл .env.production с правильным API URL
echo "VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api" > .env.production

# 3. Соберите фронтенд для production
npm run build

# Готовые файлы будут в папке: frontend/dist/
```

**Проверьте:**
- ✅ Папка `frontend/dist/` содержит файлы
- ✅ Файл `frontend/.htaccess` существует

---

### Шаг 2: Обновление Backend на Railway

Backend обновится автоматически через GitHub, если вы используете автоматический деплой.

#### Если используете GitHub деплой:

1. **Закоммитьте и запушьте изменения:**
   ```bash
   cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"
   git add .
   git commit -m "Phase 2: Replace system dialogs, improve typing, remove console.log"
   git push origin main
   ```

2. **Railway автоматически:**
   - Обнаружит изменения
   - Соберет проект
   - Применит миграции (если есть)
   - Перезапустит сервер

3. **Проверьте деплой:**
   - Откройте Railway Dashboard
   - Проверьте статус последнего деплоя
   - Убедитесь, что нет ошибок

#### Если нужно применить миграции вручную:

```bash
# Установите Railway CLI (если еще не установлен)
npm install -g @railway/cli

# Войдите в Railway
railway login

# Подключитесь к проекту
cd backend
railway link

# Примените миграции
railway run npx prisma migrate deploy
```

---

### Шаг 3: Обновление Frontend на cPanel

#### Вариант A: Через File Manager (cPanel)

1. **Войдите в cPanel**
2. **Откройте File Manager**
3. **Перейдите в директорию поддомена:**
   - Обычно: `public_html/academy` или `academy.dilmurodnurkhonov.uz`
4. **Удалите старые файлы:**
   - Выберите все файлы в корне (кроме `.htaccess`)
   - Удалите их
5. **Загрузите новые файлы:**
   - Загрузите **ВСЁ содержимое** папки `frontend/dist/`:
     - `index.html`
     - Папку `assets/` (со всем содержимым)
     - Все остальные файлы
6. **Проверьте файл `.htaccess`:**
   - Убедитесь, что файл `.htaccess` существует в корне
   - Если его нет, загрузите из `frontend/.htaccess`

#### Вариант B: Через FTP

1. **Подключитесь к серверу через FTP**
2. **Перейдите в директорию поддомена**
3. **Удалите старые файлы** (кроме `.htaccess`)
4. **Загрузите новые файлы** из `frontend/dist/`

---

### Шаг 4: Проверка работы

#### 1. Проверьте Frontend:
- Откройте: `https://academy.dilmurodnurkhonov.uz`
- Убедитесь, что сайт загружается
- Проверьте консоль браузера (F12) - не должно быть ошибок

#### 2. Проверьте Backend API:
- Откройте: `https://api.academy.dilmurodnurkhonov.uz/api/health`
- Должен вернуться статус 200

#### 3. Проверьте функции:
- ✅ Вход в систему
- ✅ Создание курсов
- ✅ Работа модальных окон (вместо системных диалогов)
- ✅ Все основные функции работают

---

## 📦 Что загружать на сервер

### Frontend (обязательно):
```
frontend/dist/
├── index.html          ← Главный файл
├── assets/             ← Папка со стилями и скриптами
│   ├── index-[hash].js
│   ├── index-[hash].css
│   └── ...
└── ... (другие файлы если есть)

frontend/.htaccess      ← Файл для правильной работы роутинга
```

### Backend (автоматически через Railway):
- Обновляется автоматически при push в GitHub
- Миграции применяются автоматически (если настроено)

---

## ⚠️ Важные моменты

### ✅ Что НЕ нужно делать:
- ❌ НЕ нужно обновлять базу данных вручную
- ❌ НЕ нужно удалять старые данные
- ❌ НЕ нужно менять переменные окружения (если не добавляли новые)

### ✅ Что нужно проверить:
- ✅ Файл `.htaccess` существует в корне frontend
- ✅ API URL в `.env.production` правильный
- ✅ Railway деплой прошел успешно
- ✅ Нет ошибок в консоли браузера

---

## 🔍 Проверка перед деплоем

### Чеклист:

#### Frontend:
- [ ] Выполнена команда `npm run build` в папке `frontend`
- [ ] Создан файл `.env.production` с правильным API URL
- [ ] Папка `frontend/dist/` содержит файлы
- [ ] Файл `frontend/.htaccess` готов

#### Backend:
- [ ] Изменения закоммичены и запушены в GitHub
- [ ] Railway деплой прошел успешно
- [ ] Нет ошибок в логах Railway

---

## 🆘 Решение проблем

### Проблема 1: Сайт не загружается
- Проверьте, что файлы загружены в правильную директорию
- Проверьте файл `.htaccess`
- Проверьте консоль браузера (F12)

### Проблема 2: API не работает
- Проверьте Railway деплой
- Проверьте переменные окружения в Railway
- Проверьте логи Railway

### Проблема 3: Модальные окна не работают
- Очистите кэш браузера (Ctrl+Shift+R)
- Проверьте консоль браузера на ошибки
- Убедитесь, что все файлы загружены

---

## 📝 Быстрая команда для подготовки

```bash
# Перейдите в папку проекта
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"

# Соберите фронтенд
cd frontend
npm install
echo "VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api" > .env.production
npm run build

# Готово! Файлы в frontend/dist/ готовы к загрузке
```

---

## ✅ После обновления

1. **Проверьте сайт:** `https://academy.dilmurodnurkhonov.uz`
2. **Проверьте API:** `https://api.academy.dilmurodnurkhonov.uz/api/health`
3. **Протестируйте основные функции:**
   - Вход/выход
   - Создание курсов
   - Работа с модальными окнами
   - Все новые функции этапа 2

---

## 🎉 Готово!

После выполнения всех шагов ваша платформа будет обновлена с новыми функциями этапа 2!

**Все данные сохранятся** - обновление не затрагивает базу данных.





