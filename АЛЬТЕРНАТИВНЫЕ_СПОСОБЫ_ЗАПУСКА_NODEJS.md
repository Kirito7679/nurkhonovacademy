# 🔧 Альтернативные способы запуска Node.js без менеджера в cPanel

## ❌ Проблема

В вашем cPanel нет раздела "Node.js приложения", значит нужно использовать альтернативные способы.

---

## ✅ Решение 1: Через SSH/Terminal (рекомендуется)

### Шаг 1: Найти Terminal в cPanel

1. В cPanel найдите раздел **"Advanced"**
2. Ищите **"Terminal"** или **"SSH Access"**
3. Если нет - обратитесь в поддержку хостинга для включения SSH

### Шаг 2: Подключиться через SSH

Если SSH доступен:

```bash
# Подключитесь к серверу через SSH клиент (Terminal на Mac, PuTTY на Windows)
ssh ваш_логин@ваш_сервер

# Или используйте Terminal в cPanel (если есть)
```

### Шаг 3: Установить зависимости и запустить

```bash
# Перейдите в папку бэкенда
cd ~/academy.dilmurodnurkhonov.uz/backend-api

# Или если другой путь:
cd ~/public_html/academy/backend-api

# Установите зависимости
npm install --production

# Примените миграции
npx prisma migrate deploy

# Сгенерируйте Prisma Client
npx prisma generate

# Запустите сервер
node dist/app.js
```

### Шаг 4: Запустить в фоне (PM2)

Если PM2 установлен:

```bash
# Установите PM2 глобально (если нет)
npm install -g pm2

# Запустите приложение
cd ~/academy.dilmurodnurkhonov.uz/backend-api
pm2 start dist/app.js --name academy-api

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск
pm2 startup
```

---

## ✅ Решение 2: Использовать Git Version Control

Если в cPanel есть **"Git™ Version Control"**:

1. **Создайте репозиторий** в Git Version Control
2. **Добавьте скрипт запуска** в репозиторий
3. **Настройте post-receive hook** для автоматического деплоя

Но это сложнее и требует настройки Git.

---

## ✅ Решение 3: Использовать внешний сервер для API

Если Node.js недоступен на текущем хостинге:

### Вариант A: Бесплатные сервисы

1. **Railway.app** (railway.app)
   - Подключите GitHub репозиторий
   - Автоматически определит Node.js
   - Бесплатный тариф доступен

2. **Render.com** (render.com)
   - Создайте Web Service
   - Подключите репозиторий или загрузите код
   - Бесплатный тариф доступен

3. **Fly.io** (fly.io)
   - Поддержка Node.js
   - Бесплатный тариф

### Вариант B: Настройка фронтенда

Если API на внешнем сервере:

1. **Измените URL API** в `.env.production`:
   ```
   VITE_API_URL=https://ваш-api.railway.app/api
   ```

2. **Пересоберите фронтенд:**
   ```bash
   cd frontend
   npm run build
   ```

3. **Загрузите обновленный фронтенд** на текущий хостинг

---

## ✅ Решение 4: Обратиться в поддержку хостинга

Напишите в поддержку хостинга:

```
Здравствуйте!

Мне нужно запустить Node.js приложение на моем хостинге.
В cPanel нет раздела "Node.js приложения".

Можете ли вы:
1. Включить Node.js поддержку в моем аккаунте?
2. Или предоставить SSH доступ для запуска приложения?

Спасибо!
```

---

## 📋 Что делать прямо сейчас

### Вариант 1: Проверить Terminal/SSH

1. В cPanel найдите раздел **"Advanced"**
2. Ищите **"Terminal"** или **"SSH Access"**
3. Если есть - используйте инструкцию выше

### Вариант 2: Использовать внешний сервер

1. Зарегистрируйтесь на Railway.app или Render.com
2. Загрузите код бэкенда
3. Получите URL API
4. Обновите фронтенд с новым URL

### Вариант 3: Обратиться в поддержку

Напишите в поддержку хостинга с просьбой включить Node.js или SSH.

---

## 🎯 Рекомендация

**Лучший вариант:** Использовать **Railway.app** или **Render.com** для API:
- ✅ Бесплатно
- ✅ Простая настройка
- ✅ Автоматический деплой
- ✅ Не нужно настраивать на текущем хостинге

Фронтенд останется на текущем хостинге, API будет на внешнем сервере.

---

**Какой вариант вы хотите использовать?** Могу помочь с настройкой любого из них.
