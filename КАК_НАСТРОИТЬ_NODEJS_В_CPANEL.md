# 🖥️ Как настроить Node.js в cPanel - Пошаговая инструкция

## 📋 Шаг 1: Найти раздел Node.js в cPanel

### Вариант 1: Поиск в cPanel
1. Войдите в cPanel
2. В строке поиска (вверху справа) введите: **"Node.js"**
3. Нажмите на результат **"Node.js приложения"** или **"Node.js"**

### Вариант 2: Ручной поиск
1. Найдите раздел **"Программное обеспечение"** (Software)
2. Найдите **"Node.js приложения"** (Node.js App) или **"Setup Node.js App"**

---

## 📋 Шаг 2: Создать новое Node.js приложение

1. **Нажмите кнопку "Создать приложение"** или **"Create Application"**

2. **Заполните форму:**

   **Node.js версия:**
   - Выберите последнюю стабильную версию (например, 18.x или 20.x)
   - Обычно есть несколько вариантов

   **Режим приложения:**
   - Выберите **"Production"** или **"Production Mode"**

   **Путь к файлу запуска (Application Root):**
   - Введите: `backend-api`
   - Или полный путь: `/home/nurkh106/academy.dilmurodnurkhonov.uz/backend-api`
   - Это папка, где находится ваш бэкенд

   **Путь к файлу запуска (Application URL):**
   - Обычно автоматически: `/backend-api` или `/api`
   - Или оставьте по умолчанию

   **Путь к файлу запуска (Application Startup File):**
   - Введите: `dist/app.js`
   - Это главный файл вашего сервера

   **Загрузочный файл (Load File):**
   - Оставьте пустым или введите: `dist/app.js`

3. **Нажмите "Создать"** или **"Create"**

---

## 📋 Шаг 3: Настроить переменные окружения

После создания приложения:

1. **Найдите ваше приложение** в списке
2. **Нажмите на иконку карандаша** (редактировать) или **"Edit"**
3. **Найдите раздел "Environment Variables"** или **"Переменные окружения"**
4. **Добавьте переменные:**

   Нажмите **"Add Variable"** или **"Добавить переменную"** для каждой:

   ```
   Имя: DATABASE_URL
   Значение: file:./prisma/dev.db
   ```

   ```
   Имя: JWT_SECRET
   Значение: nurkhonov-academy-secret-key-2024-change-this-in-production
   ```

   ```
   Имя: PORT
   Значение: 5001
   ```

   ```
   Имя: NODE_ENV
   Значение: production
   ```

5. **Сохраните изменения**

---

## 📋 Шаг 4: Установить зависимости через Terminal

### Вариант 1: Terminal в cPanel

1. **Найдите раздел "Terminal"** или **"SSH Access"** в cPanel
2. **Откройте Terminal**
3. **Выполните команды:**

```bash
# Перейдите в папку бэкенда
cd ~/academy.dilmurodnurkhonov.uz/backend-api

# Или если другой путь:
cd ~/public_html/academy/backend-api

# Установите зависимости
npm install --production

# Примените миграции базы данных
npx prisma migrate deploy

# Сгенерируйте Prisma Client
npx prisma generate
```

### Вариант 2: Если Terminal недоступен

В некоторых версиях cPanel можно установить зависимости прямо в интерфейсе:

1. В настройках Node.js приложения найдите кнопку **"Run NPM Install"**
2. Или используйте **"Shell Access"** если доступен

---

## 📋 Шаг 5: Запустить приложение

1. **В списке Node.js приложений** найдите ваше приложение
2. **Нажмите кнопку "Запустить"** или **"Start"**
3. **Дождитесь запуска** (обычно несколько секунд)
4. **Проверьте статус** - должно быть "Running" или "Запущено"

---

## 📋 Шаг 6: Проверка работы

1. **Откройте в браузере:**
   ```
   https://academy.dilmurodnukhonov.uz/api/health
   ```

2. **Должно вернуться:**
   ```json
   {"success": true, "message": "Server is running"}
   ```

3. **Если не работает:**
   - Проверьте логи в cPanel (кнопка "View Logs" или "Логи")
   - Убедитесь, что приложение запущено
   - Проверьте переменные окружения

---

## 🔧 Альтернативный способ: Если нет раздела Node.js

Если в вашем cPanel нет раздела Node.js, возможно:

1. **Обратитесь в поддержку хостинга** - попросите включить Node.js
2. **Используйте внешний сервер** для API (Railway, Render, Heroku)
3. **Используйте SSH доступ** напрямую и запустите через PM2 или systemd

---

## 📝 Чеклист

- [ ] Нашел раздел Node.js в cPanel
- [ ] Создал новое приложение
- [ ] Указал путь: `backend-api/dist/app.js`
- [ ] Добавил переменные окружения из `.env`
- [ ] Установил зависимости (`npm install --production`)
- [ ] Применил миграции (`npx prisma migrate deploy`)
- [ ] Сгенерировал Prisma Client (`npx prisma generate`)
- [ ] Запустил приложение
- [ ] Проверил работу API (`/api/health`)

---

## 🆘 Решение проблем

### Проблема: Не могу найти раздел Node.js
- ✅ Проверьте версию cPanel (может быть старая)
- ✅ Обратитесь в поддержку хостинга
- ✅ Используйте SSH для запуска вручную

### Проблема: Приложение не запускается
- ✅ Проверьте логи в cPanel
- ✅ Убедитесь, что путь к файлу правильный
- ✅ Проверьте переменные окружения

### Проблема: Ошибки при установке зависимостей
- ✅ Убедитесь, что находитесь в правильной папке
- ✅ Проверьте права доступа к папке
- ✅ Попробуйте через SSH

---

**Готово! После выполнения этих шагов ваш бэкенд должен заработать! 🚀**
