# 🔍 Проверка запросов при логине

## ⚠️ Проблема: Не могу войти, нет ошибок

Если нет ошибок в консоли, возможно запрос не отправляется или не доходит до сервера.

---

## 📋 Что проверить

### Шаг 1: Проверка Network запросов (ВАЖНО!)

1. Откройте сайт: `https://academy.dilmurodnurkhonov.uz/login`
2. Нажмите **F12** (или Cmd+Option+I)
3. Откройте вкладку **Network** (Сеть)
4. **Очистите список** (кнопка с крестиком)
5. Попробуйте войти (введите данные и нажмите "Войти")
6. **Что вы видите?**
   - Есть ли запрос к `/api/auth/login`?
   - Какой статус у запроса? (200, 401, 500, и т.д.)
   - Есть ли вообще запросы?

**Если запроса НЕТ:**
- Проблема на фронтенде - форма не отправляется
- Проверьте консоль на ошибки JavaScript

**Если запрос ЕСТЬ:**
- Нажмите на запрос `/api/auth/login`
- Посмотрите вкладку **Response** - что там?
- Посмотрите вкладку **Request** - что отправляется?

---

### Шаг 2: Проверка API напрямую

Попробуйте отправить запрос напрямую к API:

1. Откройте консоль браузера (F12 → Console)
2. Выполните команду:
   ```javascript
   fetch('https://api.academy.dilmurodnurkhonov.uz/api/auth/login', {
     method: 'POST',
     headers: {
       'Content-Type': 'application/json',
     },
     body: JSON.stringify({
       phone: 'ваш-номер-телефона',
       password: 'ваш-пароль'
     })
   })
   .then(r => r.json())
   .then(console.log)
   .catch(console.error)
   ```

**Что возвращается?** Запишите результат.

---

### Шаг 3: Проверка базы данных

Возможно, база данных не создана. Проверьте логи в Railway:

1. Откройте проект в Railway
2. Deployments → последний деплой → View Logs
3. Найдите строки про миграции:
   ```
   Applying migration `20251210094933_init`
   ```

**Если миграций НЕТ:**
- Нужно применить миграции вручную

---

### Шаг 4: Проверка health endpoint

Откройте в браузере:
```
https://api.academy.dilmurodnurkhonov.uz/api/health
```

**Должно вернуть:**
```json
{
  "success": true,
  "message": "Server is running"
}
```

**Если не работает:**
- Проблема с API или DNS

---

## 🔧 Быстрое решение: Применить миграции

Если миграции не применены:

**Через Railway CLI:**
```bash
railway login
railway link
railway run npx prisma migrate deploy
```

**Или через Railway Dashboard:**
1. Откройте проект
2. Deployments → последний деплой → View Logs
3. Найдите кнопку "Run Command"
4. Выполните: `npx prisma migrate deploy`

---

## 📝 Что нужно сообщить:

1. ✅ Есть ли запрос к `/api/auth/login` в Network tab?
2. ✅ Какой статус у запроса? (200, 401, 500, и т.д.)
3. ✅ Что в Response у запроса?
4. ✅ Что возвращает `/api/health`?
5. ✅ Есть ли миграции в логах Railway?

---

**Проверьте Network tab - это самое важное! 🔍**
