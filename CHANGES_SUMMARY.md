# Сводка изменений: Функционал управления учителями

## ✅ Выполненные задачи

### 1. Переименование "Кураторы" в "Учителя"
- Страница `/admin/curators` теперь называется "Учителя"
- Функционал создания кураторов изменен на создание учителей
- Учителя создаются с ролью `TEACHER` и автоматически получают оплаченную регистрацию

### 2. Права доступа учителей

#### ✅ Создание студентов
- Учителя могут создавать студентов через интерфейс `/teacher/students`
- При создании сохраняется информация о создателе (`createdBy`)

#### ✅ Создание курсов
- Учителя могут создавать только свои курсы
- Учителя видят только свои курсы (не видят курсы других учителей)
- Нет доступа к старым курсам других учителей

#### ✅ Создание классов
- Учителя могут создавать групповые классы (уже было реализовано)

#### ✅ Назначение курсов
- Учителя могут назначать студентам только свои курсы
- Админ может назначать любые курсы

### 3. Отслеживание создателя студента
- Добавлено поле `createdBy` в модель `User`
- В списке студентов отображается информация о создателе:
  - **Создан учителем/админом**: Показывается имя создателя и его роль
  - **Самостоятельная регистрация**: Показывается "Самостоятельная регистрация"

### 4. Просмотр студентов учителями
- Учителя видят:
  1. Всех студентов, которых они создали
  2. Всех студентов, записанных на их курсы

## 📝 Измененные файлы

### Backend
- `backend/prisma/schema.prisma` - добавлено поле `createdBy` и отношение `creator`
- `backend/src/controllers/curatorController.ts` - изменен на создание учителей
- `backend/src/controllers/studentController.ts` - обновлена логика создания и просмотра студентов
- `backend/src/utils/validation.ts` - улучшена валидация (уже было сделано ранее)

### Frontend
- `frontend/src/pages/Curators.tsx` - переименовано в "Учителя", обновлена валидация
- `frontend/src/pages/Students.tsx` - добавлена колонка "Создан", кнопка создания для учителей
- `frontend/src/components/Sidebar.tsx` - обновлена метка меню
- `frontend/src/types/index.ts` - добавлены поля `createdBy` и `creator` в тип `User`

## ⚠️ Важно: Миграция базы данных

**Необходимо применить миграцию** для добавления поля `createdBy` в таблицу `users`.

См. файл `MIGRATION_INSTRUCTIONS.md` для подробных инструкций.

### Быстрая миграция (SQL):
```sql
ALTER TABLE "users" ADD COLUMN IF NOT EXISTS "createdBy" TEXT;
ALTER TABLE "users" ADD CONSTRAINT "users_createdBy_fkey" 
  FOREIGN KEY ("createdBy") REFERENCES "users"("id") 
  ON DELETE SET NULL ON UPDATE CASCADE;
CREATE INDEX IF NOT EXISTS "users_createdBy_idx" ON "users"("createdBy");
```

## 🧪 Тестирование

После применения миграции проверьте:

1. ✅ Админ может создавать учителей через `/admin/curators`
2. ✅ Учителя могут создавать студентов через `/teacher/students`
3. ✅ В списке студентов (`/teacher/students`) отображается информация о создателе
4. ✅ Учителя видят только свои курсы в `/teacher/courses`
5. ✅ Учителя могут назначать только свои курсы студентам
6. ✅ Учителя видят студентов, которых они создали, и студентов на своих курсах

## 📋 Логика работы

### Создание учителей (Админ)
```
Админ → /admin/curators → Создать учителя
→ Учитель создан с isPaidTeacher: true
→ Учитель может сразу создавать курсы и студентов
```

### Создание студентов (Учитель)
```
Учитель → /teacher/students → Создать студента
→ Студент создан с createdBy: teacherId
→ Учитель видит этого студента в списке
```

### Просмотр студентов (Учитель)
```
Учитель → /teacher/students
→ Видит:
  1. Студентов, которых он создал (createdBy = teacherId)
  2. Студентов, записанных на его курсы
```

### Назначение курсов (Учитель)
```
Учитель → /teacher/students/:id → Назначить курс
→ Может выбрать только свои курсы
→ Админ может выбрать любые курсы
```

## 🔒 Безопасность

- Учителя не могут видеть курсы других учителей
- Учителя не могут назначать чужие курсы
- Учителя не могут редактировать чужие курсы
- Учителя видят только своих студентов (созданных ими или записанных на их курсы)
