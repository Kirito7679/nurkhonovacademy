# 🎓 Настройка системы групповых классов

## ✅ Что реализовано

### Backend:
1. ✅ Модели Prisma: `Class`, `ClassStudent`
2. ✅ API контроллеры: создание, редактирование, удаление классов
3. ✅ Управление студентами: добавление, удаление, изменение статуса
4. ✅ Роуты: `/api/classes`

### Frontend:
1. ✅ Типы TypeScript для классов
2. ✅ Страница списка классов (`/teacher/classes`)
3. ✅ Модальное окно создания/редактирования класса
4. ✅ Интеграция в Sidebar и роутинг

## 🚀 Применение миграции базы данных

### Вариант 1: Локальная разработка (SQLite)

```bash
cd backend
npx prisma db push
```

### Вариант 2: Production (PostgreSQL на Railway)

```bash
cd backend
npx prisma migrate deploy
```

Или через Railway CLI:
```bash
npx @railway/cli run --service backend npx prisma migrate deploy
```

## 📋 API Endpoints

### Классы:
- `GET /api/classes` - Список классов (с фильтрацией по статусу)
- `GET /api/classes/:id` - Детали класса
- `POST /api/classes` - Создать класс (TEACHER, ADMIN)
- `PUT /api/classes/:id` - Обновить класс (TEACHER, ADMIN)
- `DELETE /api/classes/:id` - Удалить класс (TEACHER, ADMIN)

### Студенты в классе:
- `POST /api/classes/:id/students` - Добавить студента (TEACHER, ADMIN)
- `DELETE /api/classes/:id/students/:studentId` - Удалить студента (TEACHER, ADMIN)
- `PUT /api/classes/:id/students/:studentId/status` - Изменить статус (PENDING/APPROVED/REJECTED)

## 🎯 Использование

### Для учителя:

1. **Создать класс:**
   - Перейдите в `/teacher/classes`
   - Нажмите "Создать класс"
   - Заполните форму:
     - Название (обязательно)
     - Описание (опционально)
     - Максимум студентов (1-50, по умолчанию 10)
     - Уровень (Beginner/Intermediate/Advanced)
     - Язык обучения

2. **Добавить студентов:**
   - Откройте класс
   - Перейдите в раздел "Студенты"
   - Нажмите "Добавить студента"
   - Выберите студента из списка

3. **Управление статусом:**
   - Можно одобрить/отклонить заявки студентов
   - Можно удалить студента из класса

## 📝 Следующие шаги

1. **Страница детального просмотра класса:**
   - Просмотр всех студентов
   - Управление студентами
   - Статистика класса

2. **Календарь занятий:**
   - Планирование занятий
   - Google Calendar интеграция

3. **Google Meet интеграция:**
   - Автоматическое создание ссылок
   - Связь с календарем

4. **Групповой чат:**
   - Чат для каждого класса
   - Файлы в чате

5. **Домашние задания:**
   - Создание заданий
   - Загрузка решений
   - Проверка и оценка

## 🔧 Технические детали

### Модели базы данных:

**Class:**
- `id`, `name`, `description`
- `teacherId` (связь с User)
- `maxStudents`, `level`, `language`
- `status` (ACTIVE, COMPLETED, CANCELLED, ARCHIVED)

**ClassStudent:**
- `id`, `classId`, `studentId`
- `status` (PENDING, APPROVED, REJECTED)
- `enrolledAt`, `approvedAt`

### Валидация:
- Название класса: обязательно, минимум 1 символ
- Максимум студентов: 1-50
- Уровень: Beginner, Intermediate, Advanced (опционально)
- Проверка лимита студентов при добавлении

## ✅ Готово к использованию!

После применения миграции базы данных система готова к использованию. Учителя могут создавать классы и управлять студентами.





