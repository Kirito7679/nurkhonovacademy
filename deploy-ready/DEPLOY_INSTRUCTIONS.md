# Инструкции по развертыванию Frontend

## 📦 Содержимое папки deploy-ready

Эта папка содержит собранную версию frontend приложения, готовую для развертывания на сервере.

## 🚀 Развертывание на cPanel

1. **Подключитесь к cPanel** через FTP или File Manager

2. **Создайте резервную копию** текущей версии (если есть)

3. **Загрузите все файлы** из папки `deploy-ready/` в корневую директорию вашего домена (обычно `public_html/` или `academy.dilmurodnurkhonov.uz/`)

4. **Проверьте файл `.htaccess`** (если его нет, создайте):
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteBase /
     RewriteRule ^index\.html$ - [L]
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```

5. **Проверьте настройки API** в файле `index.html` - убедитесь, что `VITE_API_URL` указывает на правильный backend URL

## 📋 Чеклист после развертывания

- [ ] Проверить работу главной страницы
- [ ] Проверить авторизацию
- [ ] Проверить загрузку файлов с прогрессом
- [ ] Проверить переключение темы
- [ ] Проверить переключение языков (включая казахский)
- [ ] Проверить пагинацию в комментариях и уведомлениях
- [ ] Проверить фильтры курсов
- [ ] Проверить аналитику флеш-карточек

## 🔄 Версия

**Дата сборки:** 2024-12-15  
**Версия:** 1.1.0  
**Commit:** a859c74

## ⚠️ Важно

- Убедитесь, что backend уже обновлен и работает
- Проверьте переменные окружения на backend
- Убедитесь, что CORS настроен правильно на backend
