# 🔧 Как переименовать htaccess в .htaccess

## Проблема

Файл `htaccess` (без точки) не работает. Нужно переименовать его в `.htaccess` (с точкой), но cPanel File Manager не позволяет это сделать.

---

## ✅ Решение через Terminal (cPanel)

### Шаг 1: Откройте Terminal в cPanel

1. В cPanel найдите раздел **"Terminal"** или **"Advanced" → "Terminal"**
2. Откройте его

### Шаг 2: Перейдите в папку домена

```bash
cd academy.dilmurodnurkhonov.uz
```

### Шаг 3: Проверьте существующие файлы

```bash
ls -la
```

Вы должны увидеть:
- `htaccess` (без точки) - файл, который нужно переименовать
- Возможно `.htaccess` (с точкой) - если он уже существует

### Шаг 4: Переименуйте файл

**Если файла `.htaccess` НЕТ:**
```bash
mv htaccess .htaccess
```

**Если файла `.htaccess` УЖЕ ЕСТЬ:**
1. Удалите старый файл без точки:
   ```bash
   rm htaccess
   ```
2. Проверьте содержимое существующего `.htaccess`:
   ```bash
   cat .htaccess
   ```
3. Если содержимое неправильное, отредактируйте:
   ```bash
   nano .htaccess
   ```
   Вставьте:
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```
   Сохраните: Ctrl+O, Enter, Ctrl+X

### Шаг 5: Проверьте права доступа

```bash
chmod 644 .htaccess
```

### Шаг 6: Проверьте результат

```bash
ls -la | grep htaccess
```

Должен показать только `.htaccess` (с точкой).

---

## ✅ Альтернатива: Через FTP

Если Terminal недоступен:

1. Используйте FTP клиент (FileZilla, Cyberduck)
2. Подключитесь к серверу
3. Перейдите в папку `academy.dilmurodnurkhonov.uz/`
4. Удалите файл `htaccess` (без точки)
5. Загрузите файл `.htaccess` из папки `frontend/dist/` на вашем компьютере

---

## ✅ Альтернатива: Создать новый файл через Terminal

Если переименование не работает:

1. Откройте Terminal в cPanel
2. Выполните:
   ```bash
   cd academy.dilmurodnurkhonov.uz
   nano .htaccess
   ```
3. Вставьте содержимое:
   ```apache
   <IfModule mod_rewrite.c>
     RewriteEngine On
     RewriteCond %{REQUEST_FILENAME} !-f
     RewriteCond %{REQUEST_FILENAME} !-d
     RewriteRule . /index.html [L]
   </IfModule>
   ```
4. Сохраните: Ctrl+O, Enter, Ctrl+X
5. Установите права:
   ```bash
   chmod 644 .htaccess
   ```
6. Удалите старый файл:
   ```bash
   rm htaccess
   ```

---

## ✅ Проверка после исправления

1. Очистите кеш браузера (Ctrl+Shift+R)
2. Откройте: `https://academy.dilmurodnurkhonov.uz/login`
3. Должно работать!

---

## 🆘 Если Terminal недоступен

Обратитесь в поддержку хостинга и попросите:
- Включить Terminal доступ
- Или переименовать файл `htaccess` в `.htaccess` вручную

---

**После переименования файла все должно заработать! 🚀**
