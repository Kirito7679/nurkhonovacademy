# 🔧 Исправление ошибки загрузки аватара (500)

## Проблема

При попытке загрузить фото профиля возникает ошибка **500 Internal Server Error** на эндпоинте `/api/auth/me/avatar`.

## Причины

1. **Bucket 'avatars' не создан в Supabase Storage**
   - Это самая частая причина ошибки 500
   - Supabase требует, чтобы bucket был создан заранее

2. **Отсутствуют переменные окружения**
   - `SUPABASE_URL` или `SUPABASE_SERVICE_KEY` не установлены

3. **Ошибка при загрузке файла в Supabase**
   - Проблемы с сетью или правами доступа

## ✅ Решение

### 1. Создайте bucket 'avatars' в Supabase

**Важно:** Это обязательный шаг!

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Storage** (в левом меню)
4. Нажмите **"New bucket"** или **"Create bucket"**
5. Введите имя: `avatars`
6. Настройки:
   - **Public bucket**: ✅ Включите (чтобы файлы были доступны публично)
   - **File size limit**: 5 MB (или больше, если нужно)
   - **Allowed MIME types**: `image/jpeg, image/png, image/gif, image/webp`
7. Нажмите **"Create bucket"**

### 2. Проверьте переменные окружения на Railway

Убедитесь, что в Railway установлены:

```bash
SUPABASE_URL=https://czmkyqkibxrryjpjfdsb.supabase.co
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Проверка через Railway CLI:**
```bash
cd backend
npx @railway/cli variables
```

### 3. Улучшенная обработка ошибок

Код теперь:
- ✅ Проверяет существование bucket перед загрузкой
- ✅ Выдает понятные сообщения об ошибках
- ✅ Корректно обрабатывает ошибки Supabase
- ✅ Не падает при ошибках удаления старого аватара

## 🧪 Тестирование

После создания bucket:

1. **Откройте профиль** в приложении
2. **Нажмите "Загрузить фото"**
3. **Выберите изображение** (JPEG, PNG, GIF, WebP, до 5 MB)
4. **Дождитесь загрузки**

Если всё настроено правильно, вы увидите:
- ✅ Успешное сообщение
- ✅ Новое фото профиля
- ✅ Фото сохранится в Supabase Storage

## 🔍 Диагностика

Если ошибка сохраняется:

### Проверьте логи Railway:

```bash
# В Railway Dashboard → Deployments → View Logs
# Или через CLI:
npx @railway/cli logs
```

Ищите сообщения:
- `Bucket 'avatars' does not exist` → Создайте bucket
- `Supabase not configured` → Проверьте переменные окружения
- `Failed to upload to Supabase` → Проверьте права доступа

### Проверьте через Supabase CLI:

```bash
cd backend
npx supabase storage ls
```

Должен показать bucket `avatars`.

## 📝 Что было исправлено:

1. ✅ Добавлена проверка существования bucket перед загрузкой
2. ✅ Улучшена обработка ошибок с понятными сообщениями
3. ✅ Добавлена проверка переменных окружения
4. ✅ Ошибки удаления старого аватара не блокируют загрузку нового
5. ✅ Лучшее логирование для диагностики

## ✅ После исправления:

- ✅ Загрузка аватара работает
- ✅ Файлы сохраняются в Supabase Storage
- ✅ Понятные сообщения об ошибках
- ✅ Автоматическое удаление старого аватара

## 🚀 Следующие шаги:

1. **Создайте bucket 'avatars'** в Supabase Dashboard
2. **Проверьте переменные окружения** на Railway
3. **Попробуйте загрузить фото** в профиле
4. **Проверьте логи** если что-то не работает

---

**Важно:** Bucket должен быть создан **до** первой загрузки аватара!
