# Функции удаления в системе

## ✅ Реализованные функции удаления

### 1. Удаление курсов
- **Функция:** `deleteCourse`
- **Контроллер:** `courseController.ts`
- **Маршрут:** `DELETE /api/courses/:id`
- **Доступ:** TEACHER (только свои курсы), ADMIN (все курсы)
- **Описание:** Удаляет курс и все связанные данные (каскадное удаление)

### 2. Удаление уроков
- **Функция:** `deleteLesson`
- **Контроллер:** `lessonController.ts`
- **Маршрут:** `DELETE /api/lessons/:id`
- **Доступ:** TEACHER (только свои уроки), ADMIN (все уроки)
- **Описание:** Удаляет урок и все связанные данные

### 3. Удаление модулей
- **Функция:** `deleteModule`
- **Контроллер:** `moduleController.ts`
- **Маршрут:** `DELETE /api/modules/:id`
- **Доступ:** TEACHER (только свои модули), ADMIN (все модули)
- **Описание:** Удаляет модуль. Уроки остаются, но без модуля

### 4. Удаление студентов ⭐ НОВОЕ
- **Функция:** `deleteStudent`
- **Контроллер:** `studentController.ts`
- **Маршрут:** `DELETE /api/students/:id`
- **Доступ:** ADMIN (только администраторы)
- **Описание:** Удаляет студента и все связанные данные (каскадное удаление)

### 5. Удаление практических упражнений ⭐ НОВОЕ
- **Функция:** `deletePracticeExercise`
- **Контроллер:** `practiceController.ts`
- **Маршрут:** `DELETE /api/practice/exercises/:exerciseId`
- **Доступ:** TEACHER (только свои упражнения), ADMIN, MODERATOR (все)
- **Описание:** Удаляет упражнение и все связанные результаты

### 6. Удаление результатов упражнений ⭐ НОВОЕ
- **Функция:** `deletePracticeResult`
- **Контроллер:** `practiceController.ts`
- **Маршрут:** `DELETE /api/practice/results/:resultId`
- **Доступ:** STUDENT (только свои результаты), TEACHER/ADMIN/MODERATOR (все)
- **Описание:** Удаляет результат выполнения упражнения

### 7. Удаление тестов ⭐ НОВОЕ
- **Функция:** `deleteQuiz`
- **Контроллер:** `quizController.ts`
- **Маршрут:** `DELETE /api/quizzes/lesson/:lessonId`
- **Доступ:** TEACHER (только свои тесты), ADMIN, MODERATOR (все)
- **Описание:** Удаляет тест, все вопросы, варианты ответов и результаты

### 8. Удаление сообщений ⭐ НОВОЕ
- **Функция:** `deleteMessage`
- **Контроллер:** `messageController.ts`
- **Маршрут:** `DELETE /api/messages/:messageId`
- **Доступ:** Отправитель сообщения
- **Описание:** Удаляет сообщение и связанный файл (если есть)

### 9. Удаление уведомлений
- **Функция:** `deleteAllNotifications` (удаление всех)
- **Маршрут:** `DELETE /api/notifications/all`
- **Доступ:** Владелец уведомлений

### 10. Удаление одного уведомления ⭐ НОВОЕ
- **Функция:** `deleteNotification`
- **Контроллер:** `notificationController.ts`
- **Маршрут:** `DELETE /api/notifications/:notificationId`
- **Доступ:** Владелец уведомления
- **Описание:** Удаляет одно конкретное уведомление

### 11. Удаление комментариев
- **Функция:** `deleteComment`
- **Контроллер:** `commentController.ts`
- **Маршрут:** `DELETE /api/comments/:commentId`
- **Доступ:** Автор комментария, TEACHER (комментарии в своих курсах), ADMIN

### 12. Удаление интеграций
- **Функция:** `deleteIntegration`
- **Контроллер:** `integrationController.ts`
- **Маршрут:** `DELETE /api/integrations/:id`
- **Доступ:** Владелец интеграции, ADMIN

### 13. Удаление колод флеш-карточек
- **Функция:** `deleteFlashcardDeck`
- **Контроллер:** `flashcardController.ts`
- **Маршрут:** `DELETE /api/flashcards/:id`
- **Доступ:** Владелец колоды, ADMIN

### 14. Удаление файлов
- **Функция:** `deleteFileById`
- **Контроллер:** `fileController.ts`
- **Маршрут:** `DELETE /api/files/:fileId`
- **Доступ:** TEACHER (файлы в своих курсах), ADMIN

### 15. Открепление курса от студента
- **Функция:** `detachCourse`
- **Контроллер:** `studentController.ts`
- **Маршрут:** `DELETE /api/students/:id/courses/:courseId`
- **Доступ:** TEACHER (только свои курсы), ADMIN

## 🔄 Дополнительные функции, которые могут понадобиться

### Массовые операции
1. **Массовое удаление студентов**
   - `DELETE /api/students/bulk` - удаление нескольких студентов одновременно
   - Полезно для очистки неактивных аккаунтов

2. **Массовое удаление курсов**
   - `DELETE /api/courses/bulk` - удаление нескольких курсов
   - С подтверждением и проверкой зависимостей

3. **Массовое удаление уроков**
   - `DELETE /api/lessons/bulk` - удаление нескольких уроков
   - Полезно при реорганизации курса

### Обновление данных
1. **Обновление практических упражнений**
   - `PUT /api/practice/exercises/:exerciseId` - обновление упражнения
   - Сейчас есть только создание

2. **Обновление сообщений**
   - `PUT /api/messages/:messageId` - редактирование сообщения
   - Полезно для исправления опечаток

3. **Обновление результатов упражнений**
   - `PUT /api/practice/results/:resultId` - обновление результата
   - Может быть полезно для корректировки оценок

### Восстановление (Soft Delete)
1. **Архивация вместо удаления**
   - Добавить поле `deletedAt` в модели
   - Функции восстановления: `restoreCourse`, `restoreStudent`, etc.
   - Полезно для восстановления случайно удаленных данных

### Очистка данных
1. **Очистка старых уведомлений**
   - `DELETE /api/notifications/cleanup?days=30` - удаление уведомлений старше N дней
   - Автоматическая очистка

2. **Очистка старых сообщений**
   - `DELETE /api/messages/cleanup?days=90` - удаление старых сообщений
   - Полезно для освобождения места

3. **Очистка неактивных студентов**
   - `DELETE /api/students/inactive?days=365` - удаление студентов без активности
   - С уведомлением перед удалением

### Проверка зависимостей
1. **Проверка перед удалением**
   - `GET /api/courses/:id/dependencies` - показать зависимости перед удалением
   - Полезно для понимания последствий удаления

2. **Удаление с предупреждением**
   - Показывать количество связанных записей перед удалением
   - Требовать подтверждение для удаления с большим количеством зависимостей

## 📋 Резюме

### ✅ Реализовано: 15 функций удаления
- Курсы, уроки, модули, студенты
- Практические упражнения и результаты
- Тесты, сообщения, уведомления
- Комментарии, интеграции, флеш-карточки, файлы

### 🔄 Рекомендуется добавить:
1. Массовое удаление (bulk delete)
2. Обновление практических упражнений
3. Обновление сообщений
4. Soft delete (архивация) для критичных данных
5. Очистка старых данных
6. Проверка зависимостей перед удалением





