# 🚀 Инструкция по загрузке проекта на поддомен academy.dilmurodnukhonov.uz

## ✅ Да, файловый менеджер подходит для загрузки!

Но сначала нужно создать поддомен, а потом загрузить файлы.

---

## 📝 Пошаговая инструкция

### Шаг 1: Создание поддомена

1. Войдите в **панель управления хостингом** (cPanel или другая)
2. Найдите раздел **"Поддомены"** (Subdomains)
3. Создайте новый поддомен:
   - **Имя:** `academy`
   - **Домен:** `dilmurodnukhonov.uz`
   - **Полное имя:** `academy.dilmurodnukhonov.uz`
4. **Запишите путь к директории!** (обычно `public_html/academy`)

---

### Шаг 2: Подготовка файлов на компьютере

Откройте терминал в папке проекта и выполните:

```bash
# Перейдите в папку фронтенда
cd frontend

# Установите зависимости (если еще не установлены)
npm install

# Создайте файл .env.production с настройками API
echo "VITE_API_URL=https://academy.dilmurodnukhonov.uz/api" > .env.production

# Соберите проект для продакшена
npm run build
```

После этого в папке `frontend/dist` будут готовые файлы для загрузки.

---

### Шаг 3: Загрузка файлов через файловый менеджер

1. **Откройте файловый менеджер** в панели управления хостингом
2. **Перейдите в директорию поддомена:**
   - Обычно это `public_html/academy` или `academy.dilmurodnukhonov.uz`
   - Если не видите папку, создайте её вручную
3. **Загрузите файлы:**
   - Нажмите кнопку **"Загрузить"** (Upload)
   - Выберите **ВСЕ файлы и папки** из папки `frontend/dist`:
     - `index.html`
     - Папку `assets` (со всем содержимым)
     - Все остальные файлы
   - **Важно:** Загружайте содержимое папки `dist`, а не саму папку!
4. **Загрузите файл `.htaccess`:**
   - Скопируйте файл `frontend/.htaccess` из проекта
   - Загрузите его в корень директории поддомена (туда же, где `index.html`)

---

### Шаг 4: Структура файлов на сервере

После загрузки структура должна выглядеть так:

```
academy/ (или public_html/academy/)
├── .htaccess          ← Важно! Для правильной работы роутинга
├── index.html         ← Главный файл
└── assets/            ← Папка со стилями и скриптами
    ├── index-[hash].js
    ├── index-[hash].css
    └── ...
```

---

### Шаг 5: Проверка

1. Откройте в браузере: `https://academy.dilmurodnukhonov.uz`
2. Если видите белый экран:
   - Откройте консоль браузера (F12)
   - Проверьте ошибки
   - Убедитесь, что все файлы загружены

---

## ⚡ Быстрый способ (используя скрипт)

В терминале выполните:

```bash
# Перейдите в папку проекта
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"

# Запустите скрипт подготовки
./prepare-deploy.sh
```

Скрипт автоматически:
- ✅ Проверит зависимости
- ✅ Соберет фронтенд
- ✅ Подготовит файлы для загрузки

---

## 🔧 Настройка API

Если ваш бэкенд будет работать на том же домене:

1. В файле `frontend/.env.production` укажите:
   ```
   VITE_API_URL=https://academy.dilmurodnukhonov.uz/api
   ```

2. Пересоберите проект:
   ```bash
   cd frontend
   npm run build
   ```

3. Загрузите новые файлы из `frontend/dist`

---

## ❓ Частые проблемы

### Белая страница
- ✅ Проверьте, что `index.html` загружен
- ✅ Проверьте консоль браузера (F12) на ошибки
- ✅ Убедитесь, что папка `assets` загружена полностью

### Страницы не работают (404)
- ✅ Убедитесь, что файл `.htaccess` загружен в корень
- ✅ Проверьте, что mod_rewrite включен на сервере

### API не работает
- ✅ Проверьте URL в `.env.production`
- ✅ Убедитесь, что бэкенд запущен и доступен

---

## 📞 Нужна помощь?

Подробная инструкция в файле: `DEPLOYMENT_GUIDE.md`

