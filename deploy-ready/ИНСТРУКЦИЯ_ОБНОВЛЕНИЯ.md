# 📦 Инструкция по обновлению Frontend

**Дата обновления:** 2025-01-27  
**Версия:** С категориями курсов и улучшенными обложками

---

## 🆕 Что нового в этом обновлении

### 1. Система категорий курсов
- ✅ Добавлены категории: Языковые, Бизнес, IT, Дизайн, Маркетинг, Финансы, Здоровье, Образование, Другое
- ✅ Фильтрация курсов по категориям
- ✅ Теги категорий на обложках курсов

### 2. Улучшенные обложки курсов
- ✅ Теги с языком курса (флаг + название)
- ✅ Теги с категорией курса (цветные)
- ✅ Отображение на всех страницах с курсами

### 3. Титульный лист курса
- ✅ Полная информация о курсе
- ✅ Статистика (модули, уроки, часы, студенты)
- ✅ Преимущества курса
- ✅ Структура курса (видна всем)

### 4. Система подписок с периодами
- ✅ Подписка на 30 дней, 3 месяца, 6 месяцев, 1 год
- ✅ Разные цены для разных периодов
- ✅ Продление подписки

---

## 📤 Как загрузить обновление

### Шаг 1: Резервная копия (ВАЖНО!)

1. Войдите в cPanel
2. Откройте **File Manager**
3. Перейдите в папку вашего сайта (обычно `public_html` или `public_html/academy`)
4. **Создайте резервную копию** текущих файлов:
   - Выделите все файлы
   - Нажмите "Compress" (Сжать)
   - Сохраните архив как `backup_$(date +%Y%m%d).zip`

### Шаг 2: Удаление старых файлов

**ВАЖНО:** Удалите старые файлы перед загрузкой новых!

1. В File Manager удалите:
   - Папку `assets/` (если есть)
   - Файл `index.html` (если есть)
   - Старые JS файлы (если есть отдельно)

**НЕ удаляйте:**
- `.htaccess` (если он настроен специально)
- Другие важные файлы

### Шаг 3: Загрузка новых файлов

1. Загрузите **ВСЕ** файлы из папки `deploy-ready`:
   - `index.html`
   - Папку `assets/` (со всем содержимым)
   - Файл `.htaccess` (если он есть)

2. **Важно:** Загружайте файлы напрямую в корневую папку сайта, а не в подпапку!

### Шаг 4: Проверка .htaccess

Убедитесь, что файл `.htaccess` содержит:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Если файл или директория существуют, используем их
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Иначе перенаправляем на index.html для SPA роутинга
  RewriteRule . /index.html [L]
</IfModule>
```

Если ваш сайт в подпапке (например, `/academy/`), добавьте:

```apache
RewriteBase /academy/
```

### Шаг 5: Очистка кеша

1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Или откройте сайт в режиме инкогнито
3. Проверьте работу сайта

---

## ✅ Проверка после обновления

1. ✅ Откройте главную страницу - должна загрузиться
2. ✅ Войдите в систему
3. ✅ Проверьте страницу курсов - должны быть видны теги с языком и категорией
4. ✅ Откройте любой курс - должен отображаться титульный лист с полной информацией
5. ✅ Проверьте фильтр по категориям
6. ✅ Проверьте консоль браузера (F12) - не должно быть ошибок

---

## 🔧 Если что-то не работает

### Проблема: Сайт не загружается

**Решение:**
1. Проверьте файл `.htaccess` - он должен быть в корне
2. Проверьте, что `mod_rewrite` включен в cPanel
3. Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### Проблема: Ошибки 404 при навигации

**Решение:**
1. Проверьте `.htaccess` - должен содержать правила для SPA
2. Убедитесь, что `RewriteEngine On` включен

### Проблема: Не отображаются теги категорий

**Решение:**
1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что все файлы из папки `assets/` загружены
3. Очистите кеш браузера

### Проблема: Ошибки API

**Решение:**
1. Проверьте, что backend работает
2. Проверьте URL API в настройках (должен быть `https://academy.dilmurodnurkhonov.uz/api`)

---

## 📝 Структура файлов для загрузки

```
deploy-ready/
├── index.html          ← Главный файл
├── .htaccess          ← Правила для SPA роутинга
└── assets/            ← Все JS и CSS файлы
    ├── index-*.css
    ├── index-*.js
    ├── vendor-*.js
    └── [другие файлы компонентов]
```

---

## 🚀 Быстрая загрузка через FTP

Если используете FTP:

1. Подключитесь к серверу через FTP-клиент
2. Перейдите в папку сайта
3. Удалите старую папку `assets/` и файл `index.html`
4. Загрузите все файлы из `deploy-ready/`
5. Убедитесь, что `.htaccess` загружен

---

## ⚠️ Важные замечания

1. **Всегда делайте резервную копию** перед обновлением
2. **Не удаляйте** `.htaccess` если он настроен специально
3. **Загружайте все файлы** из папки `assets/` - они все нужны
4. **Очистите кеш браузера** после загрузки

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте Network вкладку (F12 → Network)
3. Убедитесь, что все файлы загружены правильно

---

**Готово к загрузке!** 🎉
