# 🚀 Полное руководство по передеплою платформы

## 📋 Обзор

Это руководство поможет вам полностью передеплоить платформу Nurkhonov Academy:
1. Очистить все данные пользователей
2. Передеплоить backend на Railway
3. Подготовить и задеплоить frontend на cPanel

---

## 🔴 ЭТАП 1: Очистка базы данных

### Вариант A: Через Railway CLI (рекомендуется)

```bash
# 1. Подключитесь к Railway
railway login
railway link

# 2. Откройте базу данных в Railway Dashboard
# 3. Используйте Railway CLI для выполнения скрипта
railway run npm run clear-db
```

### Вариант B: Через Railway Dashboard

1. Откройте Railway Dashboard
2. Выберите ваш проект → Database
3. Откройте PostgreSQL консоль
4. Выполните SQL команды для очистки (см. `CLEAR_DATABASE.sql`)

### Вариант C: Локально (если используете локальную БД)

```bash
cd backend
npm run clear-db
```

---

## 🔵 ЭТАП 2: Полный передеплой Backend на Railway

### Шаг 1: Подготовка локального кода

```bash
# 1. Убедитесь, что все изменения закоммичены
cd "/Users/macplus/Desktop/Projects/Nurkhonov Academy"
git status

# 2. Если есть незакоммиченные изменения, закоммитьте их
git add .
git commit -m "Prepare for full redeploy"

# 3. Отправьте на GitHub
git push origin main
```

### Шаг 2: Проверка конфигурации Railway

Убедитесь, что файлы настроены правильно:

1. **`backend/railway.json`** - должен содержать:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && npx prisma generate && npx prisma db push --skip-generate --accept-data-loss"
  },
  "deploy": {
    "startCommand": "node dist/app.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

2. **`backend/prisma/schema.prisma`** - должен использовать PostgreSQL:
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

3. **Переменные окружения в Railway:**
   - `DATABASE_URL` - должен быть установлен автоматически Railway
   - `JWT_SECRET` - должен быть установлен
   - `PORT` - Railway установит автоматически
   - Другие необходимые переменные

### Шаг 3: Очистка и передеплой на Railway

#### Вариант A: Через Railway Dashboard (рекомендуется)

1. **Откройте Railway Dashboard:**
   - Перейдите на https://railway.app
   - Выберите ваш проект

2. **Удалите текущий сервис (опционально):**
   - Settings → Delete Service
   - ⚠️ ВНИМАНИЕ: Это удалит все данные! Убедитесь, что у вас есть бэкап!

3. **Создайте новый сервис:**
   - New → GitHub Repo
   - Выберите ваш репозиторий
   - Root Directory: `backend`
   - Railway автоматически определит настройки

4. **Настройте переменные окружения:**
   - Variables → Add Variable
   - Добавьте все необходимые переменные

5. **Подождите деплоя:**
   - Railway автоматически запустит сборку
   - Проверьте логи на наличие ошибок

#### Вариант B: Через Railway CLI

```bash
# 1. Установите Railway CLI (если еще не установлен)
npm install -g @railway/cli

# 2. Войдите в Railway
railway login

# 3. Создайте новый проект (или используйте существующий)
railway init

# 4. Свяжите с GitHub репозиторием
railway link

# 5. Установите переменные окружения
railway variables set JWT_SECRET=your-secret-key

# 6. Деплой
railway up
```

### Шаг 4: Проверка деплоя

После завершения деплоя проверьте:

1. **Логи Railway:**
   - Убедитесь, что нет ошибок
   - Проверьте, что сервер запустился

2. **Health check:**
```bash
curl https://api.academy.dilmurodnurkhonov.uz/api/health
```

3. **Проверка endpoints:**
```bash
# Проверка авторизации
curl -X POST https://api.academy.dilmurodnurkhonov.uz/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"firstName":"Test","lastName":"User","phone":"+998901234567","password":"test123"}'
```

---

## 🟢 ЭТАП 3: Подготовка и деплой Frontend

### Шаг 1: Подготовка локального кода

```bash
# 1. Перейдите в папку frontend
cd frontend

# 2. Установите зависимости (если нужно)
npm install

# 3. Создайте файл .env.production
echo "VITE_API_URL=https://api.academy.dilmurodnurkhonov.uz/api" > .env.production

# 4. Соберите проект для production
npm run build

# 5. Проверьте, что папка dist/ создана
ls -la dist/
```

### Шаг 2: Подготовка файлов для загрузки

Файлы для загрузки на cPanel находятся в папке `frontend/dist/`:

```
frontend/dist/
├── index.html
├── assets/
│   ├── index-*.js
│   ├── index-*.css
│   └── ...
└── .htaccess (если есть)
```

### Шаг 3: Загрузка на cPanel

1. **Войдите в cPanel:**
   - Откройте https://your-cpanel-url.com
   - Войдите с вашими учетными данными

2. **Откройте File Manager:**
   - Найдите "File Manager" в cPanel
   - Перейдите в папку `public_html` (или вашу корневую папку сайта)

3. **Удалите старые файлы (опционально):**
   - ⚠️ Сделайте бэкап перед удалением!
   - Удалите все файлы из `public_html` (кроме важных, например `.htaccess`)

4. **Загрузите новые файлы:**
   - Выберите все файлы из `frontend/dist/`
   - Загрузите их в `public_html`
   - Убедитесь, что `index.html` находится в корне

5. **Настройте .htaccess:**
   - Убедитесь, что файл `.htaccess` существует
   - Если нет, создайте его (см. `frontend/.htaccess`)

### Шаг 4: Проверка деплоя

1. **Откройте сайт в браузере:**
   - https://academy.dilmurodnurkhonov.uz
   - Проверьте, что сайт загружается

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Проверьте, что нет ошибок загрузки
   - Проверьте, что API запросы идут на правильный URL

3. **Проверьте работу функций:**
   - Вход/регистрация
   - Просмотр курсов
   - Отправка сообщений
   - И т.д.

---

## 📝 ЭТАП 4: Создание первого пользователя

После передеплоя нужно создать первого пользователя (админа):

### Вариант A: Через API

```bash
curl -X POST https://api.academy.dilmurodnurkhonov.uz/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Admin",
    "lastName": "User",
    "phone": "+998901234567",
    "password": "secure-password-123"
  }'
```

Затем вручную измените роль на ADMIN через базу данных или создайте скрипт.

### Вариант B: Через скрипт

Создайте скрипт `backend/scripts/createAdmin.ts` и запустите его:

```bash
railway run npm run create-admin
```

---

## ✅ Чеклист перед деплоем

- [ ] Все изменения закоммичены в git
- [ ] Backend код проверен локально
- [ ] Frontend код проверен локально
- [ ] Переменные окружения настроены в Railway
- [ ] DATABASE_URL правильно настроен
- [ ] JWT_SECRET установлен
- [ ] .env.production создан для frontend
- [ ] Frontend собран (npm run build)
- [ ] Бэкап базы данных сделан (если нужно сохранить данные)

---

## 🐛 Решение проблем

### Backend не запускается

1. Проверьте логи Railway
2. Убедитесь, что DATABASE_URL правильный
3. Проверьте, что Prisma схема применена (`npx prisma db push`)

### Frontend показывает ошибки

1. Проверьте, что API URL правильный в `.env.production`
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что все файлы загружены на cPanel

### 404 ошибки на API

1. Проверьте, что backend запущен
2. Проверьте, что роуты правильно зарегистрированы
3. Проверьте CORS настройки

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи Railway
2. Проверьте логи cPanel
3. Проверьте консоль браузера
4. Убедитесь, что все файлы правильно загружены

---

**Готово!** После выполнения всех этапов ваша платформа должна работать в онлайн режиме. 🎉
