# 🚨 СРОЧНО: Восстановление сервиса в Railway

## 🔴 Текущая проблема:

Сервис в Railway показывает статус **"Removed"**, поэтому все API запросы возвращают 404.

## ✅ Что нужно сделать ПРЯМО СЕЙЧАС:

### ШАГ 1: Откройте Railway Dashboard

1. Перейдите на https://railway.app
2. Войдите в ваш аккаунт
3. Выберите проект **"nurkhonovacademy"**

### ШАГ 2: Проверьте статус сервиса

- Если сервис показывает **"Removed"** или его нет в списке
- Нужно создать новый сервис

### ШАГ 3: Создайте новый сервис

1. **Нажмите "New"** (или "+" кнопку)
2. **Выберите "GitHub Repo"**
3. **Выберите репозиторий:** `Kirito7679/nurkhonovacademy`
4. **Настройте сервис:**
   - **Root Directory:** `backend` ⚠️ ВАЖНО!
   - Railway автоматически определит остальные настройки

### ШАГ 4: Настройте переменные окружения

1. Откройте настройки сервиса → **"Variables"**
2. Добавьте переменные (если их нет):
   - `JWT_SECRET` = ваш секретный ключ
   - `DATABASE_URL` - Railway установит автоматически, если база данных подключена

### ШАГ 5: Подключите базу данных

1. Если база данных не подключена:
   - Нажмите "New" → "Database" → "PostgreSQL"
   - Railway создаст новую базу данных
   - `DATABASE_URL` установится автоматически

2. Если база данных уже есть:
   - Убедитесь, что она подключена к сервису
   - Проверьте, что `DATABASE_URL` установлен

### ШАГ 6: Дождитесь деплоя

1. Railway автоматически запустит сборку
2. Проверьте вкладку **"Logs"**
3. Дождитесь сообщения: `Server is running on port...`

### ШАГ 7: Проверьте работу

1. **Health check:**
   ```bash
   curl https://api.academy.dilmurodnurkhonov.uz/api/health
   ```
   Должен вернуть: `{"success":true,"message":"Server is running"}`

2. **Проверьте в браузере:**
   - Откройте DevTools (F12)
   - Проверьте, что нет 404 ошибок

## ⚠️ ВАЖНЫЕ НАСТРОЙКИ:

### Root Directory:
**ОБЯЗАТЕЛЬНО** укажите: `backend`

### Build Command:
Railway автоматически использует из `railway.json`:
```
npm install && npm run build && npx prisma generate && npx prisma db push --skip-generate --accept-data-loss
```

### Start Command:
Railway автоматически использует из `railway.json`:
```
node dist/app.js
```

## 🔍 Проверка после восстановления:

### 1. Проверьте логи Railway:
- Должна быть строка: `Server is running on port...`
- Не должно быть ошибок при запуске

### 2. Проверьте endpoints:
- `/api/health` - должен работать
- `/api/messages/teacher/chats` - должен работать (с токеном)
- `/api/modules/courses/:id/modules` - должен работать (с токеном)

### 3. Проверьте базу данных:
- Убедитесь, что база данных подключена
- Проверьте, что `DATABASE_URL` установлен

## 🐛 Если проблема сохраняется:

### Проблема 1: Сервис не запускается

**Проверьте логи:**
- Есть ли ошибки при запуске?
- Правильно ли установлены переменные окружения?

**Решение:**
- Проверьте `DATABASE_URL`
- Проверьте `JWT_SECRET`
- Проверьте логи на ошибки

### Проблема 2: Роуты все еще возвращают 404

**Проверьте:**
- Запущен ли сервер? (проверьте логи)
- Правильно ли загружены файлы? (проверьте, что последний коммит виден)

**Решение:**
- Перезапустите сервис: Settings → Restart
- Или передеплойте: Redeploy

### Проблема 3: База данных не подключена

**Решение:**
- Создайте новую базу данных: New → Database → PostgreSQL
- Или подключите существующую базу данных

---

## ✅ Чеклист:

- [ ] Сервис создан в Railway
- [ ] Root Directory установлен как `backend`
- [ ] Переменные окружения настроены (`JWT_SECRET`, `DATABASE_URL`)
- [ ] База данных подключена
- [ ] Деплой завершился успешно
- [ ] Сервер запущен (проверьте логи)
- [ ] Health endpoint работает
- [ ] API endpoints работают

---

**СЛЕДУЮЩИЙ ШАГ:** Восстановите сервис в Railway Dashboard СЕЙЧАС! 🚀

После восстановления все должно заработать.
