# 💻 Как открыть CLI в Railway

## Способ 1: Railway CLI на вашем компьютере (рекомендуется)

### Шаг 1: Установите Railway CLI

**На Mac/Linux:**
```bash
npm install -g @railway/cli
```

**Или через Homebrew (Mac):**
```bash
brew install railway
```

**Проверьте установку:**
```bash
railway --version
```

### Шаг 2: Войдите в Railway

```bash
railway login
```

Откроется браузер для авторизации. Разрешите доступ.

### Шаг 3: Подключитесь к проекту

```bash
cd backend
railway link
```

Выберите ваш проект из списка (например: `nurkhonov-academy-backend`).

### Шаг 4: Выполните команды

Теперь вы можете выполнять команды:

```bash
# Создать пользователя
railway run npx tsx scripts/createUser.ts "Имя" "Фамилия" "+998901234567" "пароль" "ADMIN"

# Применить миграции
railway run npx prisma migrate deploy

# Назначить админа
railway run npx tsx scripts/makeAdmin.ts +998901234567

# Просмотр логов
railway logs
```

---

## Способ 2: Через Railway Dashboard (если доступно)

### Шаг 1: Откройте проект в Railway

1. Откройте: https://railway.app
2. Войдите в аккаунт
3. Откройте ваш проект

### Шаг 2: Найдите Terminal/Shell

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Откройте **View Logs**
4. Найдите кнопку **"Run Command"**, **"Shell"** или **"Terminal"**

**Или:**

1. Перейдите в **Settings**
2. Найдите раздел **"Shell"** или **"Terminal"**
3. Откройте его

### Шаг 3: Выполните команды

В открывшемся терминале выполните команды:

```bash
# Создать пользователя
npx tsx scripts/createUser.ts "Имя" "Фамилия" "+998901234567" "пароль" "ADMIN"

# Применить миграции
npx prisma migrate deploy

# Назначить админа
npx tsx scripts/makeAdmin.ts +998901234567
```

---

## 📋 Быстрая команда для создания админа

После установки Railway CLI:

```bash
# 1. Войдите
railway login

# 2. Подключитесь к проекту
cd backend
railway link

# 3. Создайте админа
railway run npx tsx scripts/createUser.ts "Дилмурод" "Нурхонов" "+998901234567" "ваш-пароль" "ADMIN"
```

**Замените:**
- `"Дилмурод"` - ваше имя
- `"Нурхонов"` - ваша фамилия
- `"+998901234567"` - ваш номер телефона
- `"ваш-пароль"` - ваш пароль

---

## 🆘 Если Railway CLI не работает

### Проблема: Команда `railway` не найдена

**Решение:**
```bash
# Установите через npm
npm install -g @railway/cli

# Или используйте npx
npx @railway/cli login
npx @railway/cli link
npx @railway/cli run npx tsx scripts/createUser.ts ...
```

### Проблема: Не могу найти Terminal в Dashboard

**Решение:**
- Не все планы Railway имеют встроенный терминал
- Используйте Railway CLI на вашем компьютере (Способ 1)

---

## ✅ После создания пользователя

1. Войдите на сайте: `https://academy.dilmurodnurkhonov.uz/login`
2. Используйте созданные данные
3. Должно работать!

---

**Выберите удобный способ и создайте пользователя! 🚀**
